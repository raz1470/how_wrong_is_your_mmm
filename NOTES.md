# Project notes & handoff

Working notes on direction, decisions, and where to pick up.
Read this first if you (or a fresh Claude session) are coming back after a break.

## Goal

A Python package that diagnoses collinearity in Marketing Mix Model spend data, quantifies how much it biases elasticity estimates, and recommends budget perturbations to fix it — with Bayesian uncertainty throughout.

**PyPI eventually.** Author: Ryan O'Sullivan.

## The problem this solves

Real marketing data is generated by business decisions, not experiments. TV and digital budgets move together because the same planning cycle controls both. This means an MMM can't reliably distinguish individual channel effects — the elasticities are biased, and the direction of bias is unknown without simulation.

Most practitioners either ignore collinearity or run a VIF check and shrug. Neither tells them *how wrong* the elasticities actually are, or what to *do* about it.

## The three-part solution

**Part 1 — DGP simulation (prove the problem)**
Generate synthetic correlated spend data with *known* true elasticities. Fit an MMM. Show the estimated elasticities are wrong. Quantify the bias as a function of the correlation structure. This is the diagnostic: "here is how wrong your MMM probably is, given the collinearity in your data."

**Part 2 — Budget phasing recommendation (design the fix)**
Given the current correlation structure of spend, recommend future budget phasing that deliberately de-correlates channels week-to-week. The goal is to introduce independent variation that lets the MMM distinguish individual channel effects. This is the experiment-design step — the equivalent of Vaver & Koehler's geo-experiments but applied to channel mix rather than geography.

**Part 3 — Sampling weights (speed up the benefit)**
Upweight recent de-correlated observations in the MMM so the model benefits quickly without waiting years for the new data to dominate the history. Equivalent to a form of importance sampling — we give more weight to informative (low-collinearity) periods.

## Positioning

This is in the same spirit as the Vaver & Koehler geo-experiment literature from Google: using deliberate variation to identify causal effects. The difference is:
- **Their instrument:** geographic variation
- **Our instrument:** channel mix variation over time
- **Our twist:** Bayesian uncertainty on both the diagnosis and the recommendation

The collinearity/budget adjuster idea is strong enough to be an **academic paper**, not just a package. Could drive significant LinkedIn traction in the MMM / causal inference community.

## Context in the broader stack

This is **package #4** in the Bayesian marketing science stack:
1. `bayesian_vecm` — long-run brand effects (in progress, nearly at PyPI)
2. Bayesian MMM — short-run channel attribution integrated with VECM baseline
3. Bayesian budget optimiser — propagates posterior uncertainty into spend recommendations
4. **`how_wrong_is_your_mmm` (this package)** — collinearity diagnostic + budget perturbation

Packages 2–4 can be developed in parallel with `bayesian_vecm` reaching PyPI. This one (#4) is the most original and the best paper candidate.

## API we're aiming for

```python
from how_wrong_is_your_mmm import CollinearityDiagnostic, BudgetPhaser

# Part 1: diagnose
diag = CollinearityDiagnostic(spend_df, true_elasticities=None)
diag.fit(n_sims=500)          # simulate DGP + fit MMMs
diag.summary()                # bias table: channel, true_elast, estimated_elast, bias_%
diag.plot_bias()              # fan chart of estimated vs true elasticities

# Part 2: recommend budget phasing
phaser = BudgetPhaser(spend_df, budget_total=1_000_000)
phaser.recommend()            # budget phasing that minimises collinearity
phaser.plot()                 # before/after correlation matrix + recommended spend split

# Part 3: sampling weights (used internally by the MMM, or standalone)
from how_wrong_is_your_mmm import CollinearityWeighter
weighter = CollinearityWeighter(spend_df)
weights = weighter.fit()      # returns array of per-observation weights
```

## Decisions locked in

| Area | Choice | Why |
|---|---|---|
| Package layout | `src/how_wrong_is_your_mmm/` | Modern best practice; forces testing against installed package. |
| Build backend | `hatchling` | Lightweight, modern — same as `bayesian_vecm`. |
| Env / deps | `uv` + `uv.lock` | Fast, reproducible — same as `bayesian_vecm`. |
| Lint + format | `ruff` | Same as `bayesian_vecm`. |
| Tests | `pytest` + `pytest-cov` | Same as `bayesian_vecm`. |
| Min Python | 3.12 | Consistency with `bayesian_vecm`. |
| License | MIT | Permissive, standard for OSS Python. |
| MMM for simulation loop | OLS (log-log) | Fast, interpretable, enough to demonstrate bias. PyMC-Marketing added later as an option. |
| DGP collinearity mechanism | Demand-driven | Channels correlated because they follow a latent demand signal + noise. More realistic than a fixed correlation matrix; maps to how practitioners actually experience collinearity. Expose `correlation_matrix` as an alternative parameterisation for users who want to specify correlations directly. |
| Notebook outputs | Committed (no nbstripout) | Outputs are the value — practitioners browsing GitHub should see the bias tables and charts without running the notebook. |
| Notebook CI | `FAST_MODE` flag | Drop sim count from 500 → 50 for CI. Add from day one, not retrofitted later. |
| Variable naming | lowercase throughout | `n_channels`, `n_obs`, `beta` etc. Ruff N806 rejects uppercase in function scope (`K`, `T`, `B`). |

## Documentation strategy

Two complementary artifacts, maintained in parallel from sprint 1:

**Notebooks** (`notebooks/NN_<topic>.ipynb`) — code-first, hands-on. Audience is a practitioner who wants to run it. Outputs committed to GitHub so the bias tables and charts are visible without running. Written in internal voice first (building intuition), then rewritten in practitioner voice at the end of each sprint.

**HTML guide** (`docs/guide.html`) — visual, theory-led, more like a white paper. Audience is someone reading a LinkedIn link or a client deck. Makes the case for *why* collinearity matters before showing *how* to diagnose it. SVG diagrams, polished charts, no code cells. Title: *"How Wrong Is Your MMM? A Practitioner's Guide to Collinearity Bias"*. Built section by section alongside the notebooks — not a post-launch chore.

The notebook is the "how." The HTML guide is the "why." Both grow with each sprint.

## Planned module structure

```
src/how_wrong_is_your_mmm/
    __init__.py           # public exports
    _dgp.py               # DGP simulation: generate correlated spend + known elasticities
    _mmm.py               # lightweight MMM fitting (OLS or Bayesian) for the simulation loop
    _diagnostic.py        # CollinearityDiagnostic class
    _phaser.py            # BudgetPhaser class
    _weights.py           # CollinearityWeighter class
    _plot.py              # shared plotting helpers
```

## Notebook plan

One notebook per public-API slice, `notebooks/NN_<topic>.ipynb`:
- `01_dgp_walkthrough.ipynb` — synthetic correlated spend DGP, show bias
- `02_diagnostic_walkthrough.ipynb` — `CollinearityDiagnostic` end to end
- `03_perturber_walkthrough.ipynb` — `BudgetPerturber` recommendation
- `04_weights_walkthrough.ipynb` — `CollinearityWeighter`, before/after MMM comparison

## Session learnings (to carry forward from bayesian_vecm)

- **DGP signal must dominate by design.** Make the latent demand signal strong relative to noise so collinearity is clearly visible in every sim, not just some seeds. Don't rely on lucky seeds.
- **FAST_MODE from day one.** Add a `FAST_MODE = True` flag at the top of every notebook that runs a simulation loop. Default True (50 sims) for CI; False (500 sims) for publication runs. Do not retrofit later.
- **Notebook JSON editing.** Edit `.ipynb` files via Python `json` manipulation, not the Edit tool (which rejects `.ipynb` files). Use `ruff format` + `ruff check --fix` on notebooks before committing.
- **No nbstripout.** Outputs committed — they are the value. Practitioners browsing GitHub should see results without running.
- **nbstripout as pre-commit hook if ever needed** — not as a git clean filter. The parallel filter crashes when multiple notebooks are staged.
- **Git operations from local terminal.** The Cowork sandbox cannot unlink files (including `.git/index.lock`). All `git commit`, `git switch`, `git branch -d` etc. must be run from the local terminal. Workaround for stale lock: `mv .git/index.lock .git/index.lock.OLD`.
- **Pre-push checklist.** `uv run ruff format . && uv run ruff check . && uv run pytest` before every push.
- **Pin dep floors immediately** after confirming they work. Open lower bounds + `uv sync` = silent major-version drift.
- **Notebook CI catches API drift that unit tests miss.** Add notebook execution to CI early.
- **ruff N806.** Uppercase variable names in function scope fail (`K`, `T`, `B`). Use lowercase from the start.

## Key open questions (to resolve during build)

- **What collinearity metric drives the budget recommendation?** VIF is intuitive but has known pathologies at high K. Condition number of the design matrix is more stable. Could offer both.
- **How to define "budget perturbation" formally?** Constrained optimisation: minimise condition number (or max VIF) subject to total budget = constant and per-channel bounds. `scipy.optimize` is the natural tool; a Bayesian version would put uncertainty on the channel response curves first.
- **Sampling weights scheme?** Inverse-VIF weighting per observation period? Or model-based: fit a time-varying collinearity measure (rolling condition number) and use it as the weight?
- **Paper angle?** Simulation study showing coefficient instability as a function of correlation strength, sample size, and number of channels. Propose the perturbation + weighting scheme. Validate on synthetic and (anonymised) real data.

## Core pipeline design (locked in session 2)

One pipeline, two entry points — everything from synthetic sales onwards is identical:

**Entry point A — fully synthetic:**
Generate synthetic spend (demand-driven DGP) → create synthetic sales using known elasticities → fit OLS → compare estimated to true

**Entry point B — real spend:**
User supplies actual spend DataFrame → create synthetic sales using known elasticities applied to real spend → fit OLS → compare estimated to true

The key insight: we never simulate spend when real data is available. We simulate only the sales column (using known elasticities × real spend + noise). The collinearity in the real spend is already baked into the data — no need to model it.

**Why this is powerful:** the diagnostic is personalised. You're not showing a generic collinearity problem, you're showing *their* problem with *their* spend data. "Given how your channels actually moved over the last 2 years, here's how unreliable your MMM elasticities are."

**The "how wrong" number:** coefficient of variation of the elasticity estimate across many noise seeds. Not wrong vs unknowable truth, but "how much would your answer change if you'd measured in a slightly different period?"

## Notebook structure (locked in session 2)

`notebooks/01_dgp_diagnostic_walkthrough.ipynb`

**Section 1 — sweep correlation (synthetic spend)**
Loop over correlation levels 0.1 → 0.9. At each level run N seeds, collect estimated elasticities. Plot distribution of estimates vs true value across correlation levels. Shows: higher correlation → wider distribution → less reliable. Educational, builds intuition.

**Section 2 — fixed correlation, many seeds (synthetic spend)**
Fix correlation at 0.7 (realistic). Run 50 seeds. Show distribution of estimated elasticities. This is the "your MMM could have said anything" moment — same market, same truth, different answers depending on which period of data you happened to use.

**Section 3 — real spend (later sprint)**
User supplies spend DataFrame. Same pipeline as above. Personalised diagnostic.

Sections 1 and 2 are the teaching sections. Section 3 is the product.

## `CollinearityDiagnostic` API (locked in session 2)

```python
# Entry point A — synthetic spend
diag = CollinearityDiagnostic(correlation=0.7, seed=0)
diag.fit()
diag.summary()

# Entry point B — real spend
diag = CollinearityDiagnostic(spend_df=my_spend_data)
diag.fit()
diag.summary()
```

The correlation sweep in Section 1 of the notebook is done via a for loop in the notebook, not baked into the class. The class does one clean run. The notebook does the looping and visualisation.

## What "wrong" means in this package (locked in session 2)

Standard OLS collinearity does not cause **bias** — it causes **variance**. OLS estimates are still centred on the truth even with correlated predictors (as long as the model is correctly specified). The problem is **unreliability**: the estimates swing wildly across different realisations of the data.

This maps directly to what practitioners experience: "we refit with 3 more months of data and TV elasticity halved." The model isn't broken — the data is uninformative. Collinearity means you can't trust the number you got, because a slightly different dataset would give a very different number.

The diagnostic quantifies this unreliability. The budget perturbation reduces it.

## Status as of session 1 (2026-06-21, project setup + sprint 1 planning)

- Repo created. Scaffold in place: `pyproject.toml`, `src/how_wrong_is_your_mmm/__init__.py`, `.gitignore`, `NOTES.md`.
- Direction confirmed: start with Part 1 (DGP simulation + diagnostic) as the first slice.
- Running in parallel with `bayesian_vecm` Monzo validation.
- Documentation strategy agreed: notebooks (committed outputs, no nbstripout) + HTML guide in parallel.
- DGP design agreed: demand-driven collinearity (latent demand → correlated spend), not fixed correlation matrix.
- MMM agreed: OLS (linear, no log transform — saturation transforms come later with PyMC-Marketing).
- Build approach: notebook and package code built in lockstep. Internal voice first, practitioner voice rewrite at end of each sprint.
- Long-term: real Monzo spend data as validation story.

## Status as of session 2 (2026-06-21, sprint 1 complete)

- Core pipeline design locked: one pipeline, two entry points (synthetic spend vs real spend).
- "How wrong" framing settled: unreliability (variance) not bias. Coefficient instability across seeds is the story.
- `_dgp.py`, `_mmm.py`, `_diagnostic.py` built and ruff-clean.
- Notebook 01 built and all cells execute cleanly.
- Key result: at correlation 0.7, TV elasticity 80% range is [0.22, 0.54] against true value of 0.3.
- Attribution shift visible in the data: as correlation rises, TV mean drifts up and Meta drifts down — channels steal credit from each other in the noise. Worth calling out in the practitioner rewrite.
- `pyproject.toml` ruff config updated to `[tool.ruff.lint]` (fixes deprecation warning).
- Branch: `feat/dgp-diagnostic`. **Ryan to review code, run notebook, then commit and open PR.**

## Status as of session 3 (2026-06-22, sprints 2 + 3 complete)

- GitHub repo created: https://github.com/raz1470/how_wrong_is_your_mmm
- Branch protection ruleset on `main` (require PR before merging).
- `pyproject.toml` updated: runtime deps pinned (`numpy>=1.26`, `pandas>=2.0`); notebook/dev tools moved to `[dependency-groups] dev` (uv native, consistent with `bayesian_vecm`). Setup: `uv venv --python 3.12 && uv sync`.
- **Sprint 2 complete:**
  - 28 tests added covering `simulate_spend`, `simulate_sales`, `fit_ols`, `CollinearityDiagnostic` (both entry points).
  - Notebook 01 rewritten in practitioner voice throughout.
  - Section 3 added to notebook 01: `CollinearityDiagnostic(spend_df=...)` personalised diagnostic.
- **Sprint 3 complete:**
  - `_perturber.py` built: `BudgetPerturber` with grid search over independent TV/Meta split variation.
  - 16 tests passing (44 total).
  - Exported from `__init__.py`.
  - Merged to main.

## BudgetPerturber design (locked session 3)

The perturbation mechanism: add independent random variation to the TV/Meta split each week, with total spend preserved. As perturbation amplitude increases, inter-channel correlation drops and elasticity CV narrows. Grid search over perturbation amplitude (0 → 50% of mean weekly total spend). Recommend the amplitude that minimises max CV across channels.

Output to practitioner: "vary your TV/Meta split by an additional ±£X per week, independent of your campaign calendar."

Two-channel only for now. K-channel generalisation requires scipy.optimize or sampling (later sprint).

## Status as of session 4 (2026-06-23, BudgetPhaser v2 + rename)

- **Terminology change:** "perturbation" → "phasing" throughout. `_perturber.py` → `_phaser.py`, `BudgetPerturber` → `BudgetPhaser`, `_generate_perturbed_schedule` → `_generate_phased_schedule`, `max_channel_weekly_deviation_pct` → `max_weekly_deviation_pct`. More natural; "budget phasing" is existing media agency vocabulary.
- **BudgetPhaser v2 complete** (replaces v1 two-channel approach):
  - N-channel, takes a 52-week spend plan with DatetimeIndex.
  - Grid search over phasing amplitude α ∈ [0, 1]. For each α: generate phased schedule (monthly totals preserved per channel via rescaling), run CollinearityDiagnostic, record max CV.
  - Recommended α minimises max CV (conservative: worst-case channel, not average).
  - Monthly total preservation guaranteed by rescaling: `new_weeks * (monthly_total / new_weeks.sum())`.
  - 59 tests passing.
- **Notebook 02 complete** (`notebooks/02_phaser_walkthrough.ipynb`):
  - CV curve + inter-channel correlation vs phasing amplitude.
  - **New: elasticity fan chart** — runs CollinearityDiagnostic at α=0, recommended α, α=1. Plots 10th–90th pct band + median estimate vs true value per channel. Makes CV improvement tangible: band visibly narrows as phasing increases.
  - Monthly comparison table confirms exact preservation.
  - Weekly spend chart: original vs recommended for each channel.

## BudgetPhaser design (locked session 4)

The phasing mechanism: for each month, redistribute each channel's weekly spend randomly within the month, then rescale to preserve the monthly total exactly. This introduces independent weekly variation across channels without touching monthly or annual commitments.

Grid search over amplitude α ∈ [0, 1]. α=0 = no change. α=1 = maximum allowed weekly deviation (default 40% per channel). Recommended α = argmin(max CV across channels).

Output to practitioner: a concrete 52×N weekly spend schedule to hand to the media agency, with monthly totals unchanged.

## Architecture change needed for Part 3 (agreed session 4)

**The data reality:** MMMs are fit on 3-5 years of weekly data, not just the plan year. The practitioner only controls next year's spend. The rest is history — correlated, fixed, out of their hands.

**Implication for CollinearityWeighter:** it should operate on the full historical dataset (3-5 years) with sampling weights that upweight the latest year. The phased plan is only applied to the latest year. Collinearity in the history is accepted; the weighter and phasing together make the latest year do more work in the model fit.

**Revised architecture:**
- `CollinearityDiagnostic` and `BudgetPhaser` accept a full multi-year spend DataFrame with a DatetimeIndex.
- A `plan_year` parameter (or the last 52 weeks by default) identifies which period is adjustable.
- `CollinearityWeighter` takes the full spend + weights and upweights recent de-correlated observations.
- The diagnostic is evaluated against the weighted full dataset, not just the plan year.

This is the design to implement in Part 3. Don't build `CollinearityWeighter` without this framing — the 52-week-only design would understate the benefit of phasing (history dilutes it) and overstate how much control the practitioner has.

## Status as of session 5 (2026-06-24, BudgetPhaser v3 + weighted OLS)

- **`CollinearityWeighter` cancelled as a separate class.** Weighting is now internal to `BudgetPhaser`. The three-part structure is preserved but Part 3 is implemented as the evaluation mechanism inside Part 2, not a standalone public API.
- **`_mmm.py`:** `fit_ols` now accepts optional `weights` array. WLS via `sqrt(w)` pre-multiplication. Backward compatible — `None` gives plain OLS.
- **`_diagnostic.py`:** `CollinearityDiagnostic` accepts optional `weights`, threads into every `fit_ols` call in the sim loop.
- **`_phaser.py`:** Full rearchitecture. `spend_df` replaced by `history_df` + `plan_df`. New `_compute_weights` helper with three schemes:
  - `"uniform"` — all observations equal (baseline)
  - `"binary"` — plan year gets `plan_weight` (default 5.0), history gets 1 (default)
  - `"decay"` — exponential decay from most recent week, parameterised by `half_life` (default 52 weeks)
  Grid search concatenates history + phased plan, evaluates with weighted diagnostic. `recommended_schedule_` is still only the 52-week plan year.
- **83 tests passing** (up from 59). New: `TestComputeWeights`, `TestFitWls`, `TestWeightedPath`.
- **Notebook 02** updated for new API: `history` (208 weeks) + `plan` (52 weeks), weighted OLS fan chart.
- **`pyproject.toml`:** added `per-file-ignores` for notebooks (`E501`, `RUF001`).
- **Branch:** `feat/weighted-phaser`. Ryan to commit notebook and open PR.

## BudgetPhaser v3 design (locked session 5)

`BudgetPhaser(history_df, plan_df, weighting="binary", plan_weight=5.0, half_life=52)`

Only `plan_df` is phased. `history_df` is fixed and only used as context for the diagnostic evaluation. The phaser concatenates history + each candidate phased plan, computes weights, and evaluates under weighted OLS. `recommended_schedule_` is only the plan year — what gets handed to the media agency.

## CV curve noise (resolved session 6)

Fixed by adding `n_phasing_seeds: int = 3` to `BudgetPhaser.fit()`. For each alpha, `n_phasing_seeds` independent phased schedules are generated and their CVs are averaged before recording `max_cv`. Smooths the grid search curve without touching `n_sims`. `fast_mode` sets `n_phasing_seeds=1`. Seeding: `self.seed + i * n_phasing_seeds + j` for alpha `i`, phasing seed `j`. `recommended_schedule_` uses `self.seed + grid_steps * n_phasing_seeds` (distinct from all grid search seeds).

## Fan chart observation (session 5)

With 4 years of correlated history and only 1 year of phasing, the elasticity bands barely narrow. This is correct and honest — the history dilutes the plan year even with 5× binary upweighting. The practitioner message: phasing helps, but it takes time to accumulate de-correlated data. Worth calling out explicitly in the notebook and HTML guide.

## Status as of session 6 (2026-06-25, CV curve smoothing)

- **CV curve smoothing complete:** `n_phasing_seeds: int = 3` added to `BudgetPhaser.fit()`. CVs averaged across seeds per alpha. `fast_mode` sets `n_phasing_seeds=1`.
- **Notebook ruff ignores extended:** added `I001`, `F401`, `F811`, `F541`, `E741` to `per-file-ignores` for notebooks in `pyproject.toml` (preexisting issues surfaced by ruff check).
- All tests passing (87 total).
- Branch: `feat/smooth-cv-curve`. Ryan to verify notebook CV curve is smoother, then commit and open PR.

## Status as of session 7 (2026-06-25, research study notebook + CV smoothing improvements)

- **Notebook 02 update:** `n_phasing_seeds` raised to 10 (FAST_MODE=1), `grid_steps` raised to 25 (FAST_MODE=10). CV curve visibly smoother.
- **Notebook 03 built:** `notebooks/03_time_to_benefit.ipynb` — research study answering "how long does budget phasing need to run before elasticity uncertainty meaningfully reduces?" Four sections, all using uniform weighting.
- Branch: `feat/smooth-cv-curve` (notebook 03 is untracked — add it before committing). Ryan to commit and open PR.

## Notebook 03 structure and findings (session 7)

**Section 1 — Plan period sweep (main result)**
Sweep plan length 4 → 260 weeks at max phasing (α=1), uniform weighting. Key results:
- Phased curve monotonically improves — ~30% reduction at 52 weeks, ~63% at 260 weeks.
- Unphased new data does not reliably help — sometimes makes things worse. The benefit is specifically from de-correlated spend, not just more data. Strong publishable result.
- Elbow around 78–104 weeks (~1.5–2 years) where improvement starts to flatten.

**Section 2 — Correlation sensitivity**
Fix 52-week plan, sweep correlation 0.3 → 0.9, uniform weighting.
- Phasing is **always beneficial** — no negative results. Previous negative bars at low correlation (from session 6 binary-weighting run) were a weighting artefact, not a real property of phasing.
- Benefit scales with correlation: 1.9% at 0.3, 15.9% at 0.5, 29.8% at 0.7, 51.3% at 0.9.
- Fine-grained sweep (0.4 → 0.75) confirms no crossover — all bars green.
- **Implication: no correlation gate needed.** Phasing is always neutral-to-helpful; just more valuable at higher correlation.

**Section 3 — Weighting sensitivity**
Compare uniform, binary (plan_weight=5), decay weighting at fixed 52-week plan.
- **Uniform wins** (0.323 max band width vs 0.357 binary, 0.349 decay).
- Binary upweighting makes things worse — amplifies variance from a small amount of phased data against a large correlated history.
- **Implication: BudgetPhaser default should change from `weighting="binary"` to `weighting="uniform"`.** The plan_weight sweep (1 → 20) was removed from the notebook as it just showed "lower binary weight is always better", i.e. converging back to uniform.

**Section 4 — Spend amplitude sensitivity**
- **Part A** (fixed 52w, sweep dev 10% → 100%): roughly linear relationship — every 10pp more deviation gives roughly equal additional reduction. 30% at 40% dev, 61% at 100% dev. No plateau in this range.
- **Part B** (plan period sweep at dev=20%, 40%, 80%): higher deviation shifts the elbow left dramatically. dev=80% hits 50% reduction in ~26–39 weeks (under 1 year). dev=40% needs ~130 weeks (2.5 years). Time and aggressiveness are substitutes.
- **Part C** (per-channel at optimal dev=80%, 52w, uniform): TV −47%, META −54%, SEARCH −53%. Bar chart + stacked weekly spend time series (original vs phased, GBP k, same style as notebook 02).

## Key decisions from session 7 research study

| Decision | Locked |
|---|---|
| Default weighting | Change `BudgetPhaser` default from `"binary"` to `"uniform"` |
| No correlation gate | Phasing helps at all correlation levels; no need to warn users off it at low correlation |
| `max_weekly_deviation_pct=40` is conservative | Practitioners who can tolerate more weekly volatility should increase it; payoff is roughly linear |
| Optimal config for 1-year plan | dev=80%, uniform weighting — ~50% reduction in band width |

## HTML guide plan (session 7)

`docs/guide.html` — self-contained, SVG charts with real numbers from research study. Four sections:

1. **The problem** — why channels move together, why MMM can't distinguish them, unreliability framing (variance not bias)
2. **How wrong is your model?** — diagnostic concept, fan chart result, "same market, different answers" story
3. **The fix** — budget phasing, how it works, monthly totals preserved
4. **How long does it take?** — time-to-benefit curve, deviation amplitude lever, key numbers from research

Target audience: LinkedIn / practitioners. Theory-led, no code cells, polished SVG diagrams. Title: *"How Wrong Is Your MMM? A Practitioner's Guide to Collinearity Bias"*.

## App idea (session 7)

Streamlit app: practitioners upload their own spend CSV → personalised diagnostic → time-to-benefit and deviation sensitivity curves calibrated to their data → slider to pick `max_weekly_deviation_pct` → download recommended weekly schedule. Host on Streamlit Community Cloud. Post-HTML-guide work.

## Next slice

- **HTML guide section 1** (`docs/guide.html`) — the problem statement. Theory-led, no code, SVG diagrams.

## Status as of session 8 (2026-06-29, HTML guide section 1 + housekeeping)

- **`BudgetPhaser` default weighting changed** from `"binary"` to `"uniform"` (locked decision from session 7, implemented in `feat/uniform-weighting-default`, merged).
- **`pyproject.toml`:** added `E402`, `RUF002` to notebook `per-file-ignores`.
- **HTML guide section 1 complete** (`docs/guide.html`): "The problem" — why channels correlate, variance-not-bias framing, correlated spend time series chart, elasticity estimates strip chart (50 seeds, corr=0.7), CV key-number boxes (18% / 36% / 60% at corr 0.3 / 0.7 / 0.9). Stubs in place for sections 2–4.
- Branch: `feat/html-guide-section-1`. Sections 2–4 to be built in the same branch.

## Status as of session 9 (2026-06-29, HTML guide sections 2 + 3)

- **HTML guide section 2 complete** (`docs/guide.html`): "The Diagnostic" — CV as the unreliability metric, CV vs correlation bar chart (colour-coded green/amber/red), personalised interval forest plot (per-channel 80% ranges with mean diamonds, using real-spend entry-point data from notebook 01 cell 18-19).
- **HTML guide section 3 complete** (`docs/guide.html`): "The Fix" — budget phasing mechanism, before/after weekly spend two-panel chart, CV reduction vs deviation amplitude line chart (exact data from notebook 03 cell 16).
- **HTML guide section 4 complete** (`docs/guide.html`): "Time to Benefit" — multi-line time-to-benefit chart (dev=20/40/80% + unphased, exact data from notebook 03 cells 4+19), key-number boxes (53% / 30% / 55%), closing narrative.
- **HTML guide complete.** All four sections, seven charts, all numbers from notebooks. Nav links active. Uncommitted on `feat/html-guide-section-1` — Ryan to push tomorrow.
- **README.md written.** Hook paragraph, three-part solution, quick start, key result table, notebook links, guide link, dev instructions. Test count (87) and PyPI link to update when live.

## Next slice

- Push `feat/html-guide-section-1` (docs/guide.html + README.md + NOTES.md), open PR, merge.
- Consider LinkedIn post based on guide, or next package feature.

## Status as of session 10 (2026-06-30, GitHub Pages + guide copy pass + CV-to-£ design discussion)

- **GitHub Pages enabled:** Settings → Pages → Deploy from branch → `main` / `docs`. Live at `https://raz1470.github.io/how_wrong_is_your_mmm/guide.html`.
- **README.md:** guide link updated to the live Pages URL, with the guide's actual title as link text (was a relative `docs/guide.html` path that only worked on GitHub itself).
- **`docs/guide.html` copy pass** (feedback from Ryan's read-through, branch `feat/readme-pages-link`):
  - New TL;DR box at the top of `<main>` — what the page is, what we're showing, how the analysis is built, why it's worth reading. Defines MMM, elasticity (explicitly tied to ROI/budget-allocation decisions), and collinearity inline.
  - First-use bracket definitions added for OLS, mean pairwise correlation, standard error.
  - Terminology fix: "coefficient estimates" → "elasticity estimates" throughout (CV keeps its own distinct name — not interchangeable with elasticity).
  - "2–4 years" → "2–5 years" of weekly observations (some practitioners run 5-year windows).
  - CV-at-0.70, correlation-scaling, amplitude-linearity, and time-to-benefit claims now explicitly attributed ("our simulation"/"our research") so it's clear these are results from this package's own study, not generic claims.
  - Section 3 methodology clarified: phasing amplitude is chosen by grid search to minimise the *worst-case* (max) CV across channels, not the average — matches `BudgetPhaser`'s locked design (session 4).
  - Section 4 honesty fix: unphased (more correlated) data does eventually reduce CV, just far more slowly and unreliably than phasing — takes ~5 years to reach what the lightest phasing level achieves in 1.
  - "Python package" → "python package".
- **CV-to-CMO discussion (design only, not yet implemented):**
  - Confirmed via `_dgp.py` (`simulate_sales`) that the package's "elasticity" is actually a *linear* marginal-response coefficient — `sales = base + Σ elasticity_c × spend_c + noise` — not a true log-log %/% elasticity. This matters for any £-translation feature: a value-per-unit multiplier is exact here (no baseline spend/sales level needed to anchor it, unlike a log-log elasticity would require).
  - Discussed an LTV-per-new-customer parameter to convert elasticity (and its uncertainty interval) into £ terms for non-revenue targets — e.g., Monzo's real MMM target is plausibly new accounts opened, not revenue, since revenue is realised later via interest/fees/subscriptions.
  - **Scoping decision: ship the simple case first, no LTV param in v1.** Default assumption is that "sales" is already £ value (revenue) — the common case for most practitioner MMMs, and consistent with how the guide already frames things ("what does an extra pound of spend do to sales?"). This needs zero new parameters: multiply elasticity (and its 80% interval) by planned spend to get a £ incremental-revenue range directly. CV itself is scale-invariant (unchanged by the multiplication) but becomes far more legible to a CMO as "£X–£Y incremental revenue for your planned spend" than "36% CV".
  - LTV-per-unit (`value_per_unit` param) deferred as an optional extension for volume-target use cases (signups/conversions instead of revenue) — relevant specifically for the Monzo validation story later. A proper Bayesian treatment would model LTV itself as uncertain rather than a fixed point multiplier, compounding with elasticity uncertainty — flagged as stretch scope, not v1.

## Next slice

- Push `feat/readme-pages-link` (README.md guide link + docs/guide.html copy pass + NOTES.md), open PR, merge.
- £-translation feature (v1, no LTV): add a `planned_spend` argument to `CollinearityDiagnostic.summary()` (or similar) that outputs an incremental-revenue range alongside elasticity, assuming sales is already £ value.
- Consider LinkedIn post based on guide, or next package feature.

## Session learnings (session 2)

- **Revenue noise must be substantial relative to channel signal.** With noise=50 and channel contributions ~£70k, OLS recovers coefficients near-perfectly regardless of collinearity. Set noise to £20k (same order of magnitude as channel contributions) to make unreliability visible. In general: if the story isn't showing up, check the SNR first.
- **The `_noise_std_from_correlation` derivation.** If TV = demand + noise, Meta = demand + noise (all N(0,1)), then Corr(TV, Meta) = 1/(1 + sigma²). Solve: sigma = sqrt((1-corr)/corr). This gives target correlation ≈ actual correlation for large n_obs but diverges at small samples — the actual correlation reported by `actual_correlation` is what matters for the notebook.
- **Actual correlation ≠ target correlation at n_obs=104.** The target is a population parameter; the actual is a sample statistic. Always report actual in the notebook, not target.

## Status as of session 11 (2026-07-01, README + guide copy pass round 2)

- **Branch:** `feat/guide-copy-pass-2`. Note: `feat/readme-pages-link` (session 10) merged to `main` on 2026-06-30, ahead of when this branch was cut, so it needed a rebase onto `main` before its own PR.
- **README.md:**
  - Part 3 reframed from "Weight" (upweighting, which research showed wasn't worthwhile, see session 5/7) to **"Retrain"**: phase the plan, then refit the MMM on the new de-correlated data for measurably tighter estimates. Matches how the package actually works, no standalone weighting class exists.
  - Removed "Outputs committed, view without running" line above the notebooks table (no longer needed).
  - Practitioner guide link trimmed to just the linked title, dropped the trailing description.
- **docs/guide.html:**
  - Eyebrow changed from "Bayesian Marketing Science" to "Marketing Science".
  - TL;DR rewritten as a 4-item bulleted list (was 4 paragraphs).
  - New pipeline figure added right after the TL;DR: left-to-right SVG diagram, Diagnose → Phase → Retrain, icons + one-line captions per step.
  - New top-right header graphic: broken gauge (cracked, skewed needle) with a wrench fixing it, inline SVG, no external image dependency.
  - Full copy pass converting em-dash constructions to Ryan's preferred comma/colon/parenthetical style throughout all four sections (title tag, HTML comment section markers, "Next:" nav labels, and JS code comments left as-is, those are titles/code not prose).
- Both new SVG pieces rendered and visually checked (cairosvg) before handoff.
- Not yet committed. Ryan to review, commit, and push from local terminal (sandbox can't reliably git commit, see below).

## Next slice (locked session 11 — remaining focus, in order)

1. **Remove sampling weights entirely.** Confirmed not worthwhile (session 7 research: uniform weighting always wins, binary/decay actively hurt). Strip `weighting`/`plan_weight`/`half_life` params and `_compute_weights` from `BudgetPhaser`, and the `weights` argument from `fit_ols`/`CollinearityDiagnostic`. Back to plain OLS throughout. Update tests and notebooks (02, 03) that reference weighting schemes.
2. **K-channel realism sweep.** Run simulations at 5, 10, 15, 20 channels — everything so far has used the 3-channel TV/Meta/Search toy example. `BudgetPhaser` v2 is already N-channel generalised (session 4); this is about actually validating diagnostic + phasing behaviour at realistic channel counts, not just proving the 2–3 channel case.
3. **New HTML guide section** presenting the channel-count sweep results (new section 5, or folded into an existing one — decide when the numbers are in).
4. **Final text/copy review pass** on the whole guide + README once the above land — one more read-through before treating the guide as finished.
5. **PyPI packaging.** Get `how_wrong_is_your_mmm` actually pip-installable — README already says `pip install how-wrong-is-your-mmm  # coming to PyPI`, need to make that true (build, publish, update the comment).

£-translation feature and LinkedIn post are parked behind this list, not dropped.

## Status as of session 12 (2026-07-03, sampling weights removed)

- **Item 1 of the session 11 next-slice list complete: sampling weights removed entirely.**
  - `_mmm.py`: `fit_ols` no longer accepts a `weights` argument. Back to plain OLS, no WLS path.
  - `_diagnostic.py`: `CollinearityDiagnostic` no longer accepts `weights`. Docstring updated.
  - `_phaser.py`: `_compute_weights` helper deleted. `BudgetPhaser` no longer accepts `weighting`, `plan_weight`, or `half_life`. Module and class docstrings rewritten to describe plain OLS evaluation (history + phased plan), with a note that weighting schemes were evaluated and dropped per the session 7 research study.
  - Tests: removed `TestFitWls` (test_mmm.py), `TestWeightedPath` (test_diagnostic.py), `TestComputeWeights` plus the weighting-specific `BudgetPhaser` tests (test_phaser.py). **65 tests passing** (down from 87 — the 22 removed were all weighting-specific).
  - Notebook 02 (`02_phaser_walkthrough.ipynb`): dropped the "Weighting schemes" table and `weighting="binary", plan_weight=5.0` override in the `BudgetPhaser` call (this had drifted from the session 8 default-to-uniform decision anyway). Fan chart section no longer precomputes weights.
  - Notebook 03 (`03_time_to_benefit.ipynb`): removed Section 3 (weighting sensitivity) entirely — its finding (uniform wins) is what motivated this removal, so the comparison is now moot. Section 4 renumbered to Section 3. All `weights=` computations and arguments stripped from Sections 1, 2, and the renumbered Section 3.
  - Both notebooks re-executed end-to-end in FAST_MODE with no errors after the edits; `ruff check .` clean; `ruff format --check` clean for src/tests (the two notebooks have pre-existing formatting gaps unrelated to this change — confirmed by checking the pre-edit versions from git HEAD, not touched here to keep the diff focused).
- **Branch:** `feat/remove-sampling-weights`, committed and merged.
- Note: a stray `.coverage.claude.pid*` file from test runs is untracked and can't be deleted from the sandbox (permissions) — safe to ignore or delete locally; `.coverage` is already gitignored but the pid-suffixed variant doesn't match the glob exactly.
- **Workflow learning: create the feature branch *before* making changes, not after.** This session's edits were made directly on top of the still-unmerged `feat/guide-copy-pass-2` working tree, then a new branch was cut once the diff was ready. That worked out here, but it means the new branch's history included `feat/guide-copy-pass-2`'s commit as an ancestor (stacked), and it left an extra step to sort out at commit time. Next time: `git switch -c feat/<slice-name>` first, confirm you're off the intended base (usually `main`, pulled fresh), *then* start editing.
- **Workflow learning: multi-line `git commit -m "..."` messages are fragile when pasted into a terminal.** Prefer `git commit -F - <<'EOF' ... EOF` (heredoc) for anything longer than one line — avoids the message getting split into separate shell commands mid-paste.

## Status as of session 13 (2026-07-03, K-channel realism sweep)

- **Item 2 of the session 11 next-slice list complete: K-channel realism sweep.** New notebook `notebooks/04_channel_count_sweep.ipynb` validates the diagnostic + phasing story beyond the 3-channel TV/Meta/Search toy example, at N=5, 10, 15, 20 channels.
- **No package code changes were needed** — `simulate_spend`/`simulate_sales` (`_dgp.py`) and `CollinearityDiagnostic`/`BudgetPhaser` already generalise to an arbitrary `channels` list / `true_elasticities` dict (confirmed session 4, re-verified this session with a quick sanity script before building the notebook). Generic channel names (`ch01`...`chNN`) with elasticities drawn once per channel count from `Uniform(0.2, 0.6)` (fixed seed), to avoid hand-picked values acting as a confound.
- **Methodology note:** a first pass using a single random (elasticity, spend-history) draw per channel count gave a noisy, non-monotonic baseline trend that didn't match theory. Reasoned through the expected effect size analytically first (for channels sharing one demand factor at fixed pairwise correlation, per-channel VIF converges to a finite ceiling `1/(1-correlation)` as N grows, rather than increasing without bound — so only a modest, decelerating increase in band width is expected, not a large one). Since the expected effect was small, a single draw's idiosyncratic noise could easily mask or invert it. Fixed by averaging Section 1 over 5 independent draws and Section 2 over 3 draws per channel count (same principle as the `n_phasing_seeds` smoothing added in session 6) — this produced a clean, theory-consistent result.
- **Section 1 finding — baseline unreliability vs channel count** (correlation fixed at 0.7, no phasing, averaged over 5 draws):
  - Mean band width: 0.349 (N=5) → 0.342 (N=10) → 0.349 (N=15) → 0.355 (N=20) — essentially flat, ~+2% over the full range.
  - Max band width across channels: 0.413 → 0.411 → 0.433 → 0.441 — a mild ~+7% increase, matching the theoretical "modest, decelerating" shape.
  - **Channel count barely matters for reliability; channel correlation is what drives it** (per notebook 03 Section 2, where CV reduction potential scaled from 2% at correlation 0.3 to 51% at correlation 0.9 — a much bigger lever than channel count).
- **Section 2 finding — phasing benefit vs channel count** (52-week plan, 40% max weekly deviation): a first pass averaged over only 3 draws showed +13.4% (N=5), +20.9% (N=10), +24.0% (N=15), +15.5% (N=20) — the N=15 vs N=20 dip looked like it might be real (per-draw inspection showed all 3 of N=20's draws below 2 of N=15's 3 draws). Re-ran with `PHASING_DRAW_SEEDS` bumped from 3 to 8: the dip resolved into a flat, consistent benefit across the whole range — **+17.0% (N=5), +17.8% (N=10), +17.7% (N=15), +15.5% (N=20)**, all within each other's error bars. Confirms `max`-across-channels is a worst-case statistic that needs more draws to read reliably as N grows (order statistics get noisier with more channels), separate from `BudgetPhaser` optimising CV internally while the notebook reports band width — a metric mismatch that can also grow with N. Recommended alpha stayed at or near the maximum (0.98–1.00) at every N throughout. Somewhat lower on average than the ~30% figure from the 3-channel toy example (notebook 03), but phasing clearly keeps working, and the benefit doesn't decay with scale; the 3-channel result is not a special case.
- **Section 3 added — concrete N=15 example.** Weekly spend time series (original vs phased) for 4 representative channels spanning the elasticity range (not an arbitrary first-4 slice), plus a before/after correlation matrix heatmap. Recommended alpha=1.00; mean pairwise correlation dropped from 0.684 to 0.356 (roughly halved) after phasing. Makes the abstract reduction numbers tangible and shows the de-correlation mechanism itself still works visually at higher channel count, not just the downstream band-width number.
- Notebook re-executed in FAST_MODE by Claude for correctness checks throughout (imports, syntax, chart rendering), full-mode runs (`FAST_MODE=False`, and later `PHASING_DRAW_SEEDS=8`) done by Ryan locally (the sandbox's background-process execution got killed between tool calls — each sandboxed bash call is an isolated environment, so `nohup`/`disown` doesn't survive across calls; long-running notebook executions need to happen locally, not backgrounded in the sandbox).
- `ruff check .` clean, all 65 tests still passing (no package code touched this session).
- **Branch:** `feat/k-channel-sweep`, committed, pushed, and merged to `main`.
- **Item 3 of the session 11 next-slice list also complete this session: new HTML guide Section 5, "Does It Scale?"** Added to `docs/guide.html` as a standalone final section (not folded into an existing one), positioned right before the closing "Bringing it together" synthesis, which was split out of Section 4's HTML block into its own trailing wrap-up so Section 5 sits cleanly between the two.
  - Two new SVG/JS charts (chart8: baseline band width vs channel count, grouped mean/max bars; chart9: phasing benefit % vs channel count, with a dashed average-reference line), following the exact conventions of the existing chart-drawing functions (same margin/scale/gridline pattern as `drawCvByCorr`).
  - Two new key-number boxes: "15–18%" band-width reduction range across all channel counts tested, and "0.68 → 0.36" mean pairwise correlation before/after phasing at the N=15 example.
  - Nav bar updated with a 5th link (`#scale`). Closing synthesis rewritten to reference five sections (was four) and fold in the "same benefit whether 3 or 20 channels" point.
  - **Verification approach:** no headless browser available in the sandbox (playwright downloaded fine but system deps needed root, which isn't available — don't retry this in future sessions without root, it fails the same way). Validated instead via jsdom (Node) — loaded the actual HTML, ran the real `<script>` block in a virtual DOM, and confirmed every chart (including the two new ones) populated with the expected number of SVG elements and threw no JS errors. Also checked balanced `<section>`/`<div>` tags, all `chart*` ids matched between HTML and JS, and all CSS classes used are actually defined in the stylesheet.
  - **This is logic verification, not visual verification.** jsdom confirms the JS runs and produces elements, not that the chart layout/spacing/colours actually look right. Ryan should open `docs/guide.html` in a real browser and eyeball Section 5 (particularly chart8's grouped bars and chart9's reference line/label placement) before treating item 4 (final copy pass) as covering it.
- **Recurring sandbox git issue:** running `git status`/`git log` etc. from the sandbox against this mounted repo keeps leaving a stale `.git/index.lock` (the sandbox can create it but can't unlink it, and because the folder is a live mount rather than a copy, the leftover lock is real and blocks Ryan's own terminal afterward). Happened again this session despite the plan to avoid it. **Going forward: no more git commands from the sandbox on this repo, full stop** — status/diff needs checked via `Read`/`Grep` on files directly, not `git`.

## Next slice (session 11 list, items 1–3 done — items 4–5 remain, in order)

1. ~~Remove sampling weights entirely.~~ **Done, session 12.**
2. ~~K-channel realism sweep.~~ **Done, session 13.**
3. ~~New HTML guide section presenting the channel-count sweep results.~~ **Done, session 13** (Section 5, "Does It Scale?").
4. **Final text/copy review pass** on the whole guide + README once the above land — one more read-through before treating the guide as finished.
5. **PyPI packaging.** Get `how_wrong_is_your_mmm` actually pip-installable — README already says `pip install how-wrong-is-your-mmm  # coming to PyPI`, need to make that true (build, publish, update the comment).

Items 4–5 still open, now queued behind the new session 14 work below (£-translation + CMO page), not dropped.

## Status as of session 14 (2026-07-06, uncertainty bands + Bayesian/Challenges sections + £-translation planning)

- **Guide Task 1 (uncertainty bands) done:** added per-seed ranges alongside the three headline CV-reduction figures in `docs/guide.html`. Also caught and fixed a **stale figure**: the "real dataset" phasing example said 12% CV reduction, but that predated a code change and didn't reproduce. Ryan re-ran notebook 02 in full mode locally and confirmed the correct current figure is **16%** (range 8–21% across 10 phasing seeds). Guide updated to 16%, not 12%, in all three places it appeared (Section 3 body copy, Section 4 body copy, Section 4 opening paragraph).
- **Guide Task 2 (Bayesian comparison) done:** paired OLS-vs-Bayesian test on the corr=0.9 3-channel scenario (weakly-informative PyMC priors, not tuned to true values). **Key methodological catch:** a single arbitrary noise draw is itself unreliable to compare on — first attempt (seed=0) showed Bayes 3x *wider* than OLS, which turned out to be because that specific draw produced a freak, unusually narrow OLS analytic CI (verified by scanning 10 draws). Averaged properly across 10 draws, the real result is **Bayesian intervals ~8–10% narrower than OLS** (TV -10.3%, Meta -8.2%, Search -9.9%), consistent across seeds. Small, real regularisation effect, not a fix for collinearity.
- **New guide Section 6, "Won't a Bayesian Method Fix It?"** added: explains priors regularise noise but can't manufacture identifying variation (flat likelihood ridge), shows the paired comparison above as evidence, distinguishes functional-form priors (adstock/saturation, which Bayesian MMMs genuinely help with) from collinearity-driven identification variance (which they don't), and warns that strong/uncalibrated priors can narrow intervals while being confidently wrong.
- **New guide Section 7, "Common Objections"** added, three sub-sections:
  - **Geo-lift test objection:** practical costs (can't run one per channel simultaneously, one estimate per test, real blackout cost, hard to set up) plus Recast's June 2026 open-source geo-lift benchmark study (32,000 model fits, CausalPy/Google Matched Markets/Meta GeoLift/Causal Impact) — verified via web fetch, cited accurately: no tool hits both good coverage and good detection power; GeoLift misses 89–96% of real effects, Causal Impact false-alarms ~30% of the time. Message: not "tools are broken," but "the fallback isn't as clean as presented."
  - **Phasing-hurts-performance objection:** distinguishes day-parting folklore (weak evidence, publisher-promoted) from the real, checkable cost — saturation curvature via Jensen's inequality. Computed directly on the actual recommended phasing schedule (notebook 02's real-dataset example) under an illustrative (not fitted — package has no saturation model yet) power-law curve at three curvature levels. Result: cost is **under 1.3% of total response** for every channel tested, small relative to the 16–53% reliability gain.
  - **Hierarchical pooling objection:** legitimate technique, just not available to most readers — UK MMM practice is single-market, no second geography to pool across.
  - Nav bar, closing synthesis, and two new charts (chart10 paired-interval, chart11 Jensen-gap bars) added. **Bug caught and fixed:** chart10 initially hardcoded a fixed x-axis max that Search's wider interval overflowed past; fixed by rescaling each row to % of that channel's own OLS width instead of absolute units. Verified via jsdom (all 11 charts populate, no JS errors, nav/section anchors match).
- **None of today's guide.html changes are committed yet** — still sitting in the working tree. Ryan to review, commit (new branch, e.g. `feat/bayesian-and-challenges-sections`), push, and merge before the live Pages URL reflects any of this.
- **Strategic discussion, not yet built:** whether to add Bayesian *fitting* to the package itself (as opposed to the one-off illustrative comparison above). Decided **no** — the 8–10% result already shows switching estimation method doesn't fix the core problem, and MCMC would make the simulation-heavy grid searches (`CollinearityDiagnostic`, `BudgetPhaser`) impractically slow (~7–25s per fit vs near-instant OLS). If a hook is wanted later, a lightweight `fit_fn` injection point (bring-your-own-estimator) is the cheap version, not a maintained Bayesian backend.
- **New direction agreed, work queued (not yet started):**
  1. **£-translation feature, for real this time.** Confirmed as a proper package feature (not just page copy) — practitioners need to run it themselves and share output with stakeholders. Scope: `CollinearityDiagnostic` only for v1 (not `BudgetPhaser` yet, that can follow once this lands). Add a `planned_spend` argument (dict per channel) to `summary()`, returning a £ incremental-revenue range (p10/p90, from the existing simulated elasticity distribution × planned spend) alongside elasticity. No LTV parameter (session 10 decision still holds — assumes sales is already £ revenue). Needs tests (scaling correctness, missing-channel-key error, backward compatible when `planned_spend=None`) and a demo cell in notebook 01.
  2. **New CMO-facing page, `docs/cmo.html`.** Explicitly a **LinkedIn sales hook**, not a trimmed practitioner guide — short, one-scroll, no CV/OLS/statistical jargon, one big £ visual, one-line fix, CTA linking to the full `docs/guide.html`. Agreed to use a **concrete illustrative example** (not abstract framing) for maximum scroll-stopping effect on LinkedIn — reuse the existing 3-channel corr=0.7 toy example and notebook 02's realistic plan-year spend totals (TV ~£5.1M, Meta ~£4.1M, Search ~£3.1M) so the numbers are dogfooded from the actual simulation, not invented for the page.
  - Task list already created in-session for this (£ feature → tests → real number computation → notebook demo → CMO page build → verification) but **no code written yet** — picking this up is the next session's starting point.

## Status as of session 15 (2026-07-07, £-translation feature)

- **£-translation feature (item 1 of the session 14 next-slice list) done for `CollinearityDiagnostic`.** `summary()` now accepts an optional `planned_spend: dict[str, float]` argument. When supplied, each simulated `estimated_elasticity` draw (already stored per-sim in `results_`) is multiplied by the channel's planned spend, and the p10/p90 of that distribution is added as `incremental_revenue_p10`/`incremental_revenue_p90` columns. Assumes sales is already a £ value (revenue) — no LTV parameter, per the session 10 scoping decision. `planned_spend=None` (default) reproduces the exact pre-feature output — verified with `pd.testing.assert_frame_equal`, not just a column-set check.
- **`planned_spend` must cover every fitted channel** — missing keys raise `KeyError` listing what's missing. Extra keys (e.g. a channel not in the model) are silently ignored, so callers can keep one spend dict across channel-count experiments.
- **6 new tests** in `tests/test_diagnostic.py::TestPlannedSpend`: backward compatibility (`None`), columns present, unit-spend quantiles match a direct computation on `results_` (accounting for `summary()`'s `.round(4)`), 3x linear scaling, missing-channel `KeyError`, extra-key tolerance. **71 tests passing** (up from 65).
- **Notebook 01 demo added:** new closing "Section 4 — Turning elasticity into £" (markdown + code + markdown), reusing the already-fitted `diag_real` from Section 3 with a realistic plan-year spend split (TV £5.1M / Meta £4.1M / Search £3.1M, matching notebook 02's real-dataset example, per session 14's CMO-page numbers). **Cells are unexecuted in the committed file** — verified correct via a scratch copy executed in FAST_MODE (nbclient + a throwaway sandbox venv, see below), but the committed notebook needs a real `FAST_MODE=False` run locally before the outputs are worth committing, same pattern as every prior notebook change.
- `ruff format`/`ruff check` clean on `src/` and `tests/`.
- **README.md:** test count corrected 87 → 71 (was already stale before this session — last updated before the session 12/13 test-count drops to 65).
- **`value_per_unit` + CAC/ROI added (same session, follow-up to the £-translation feature above).** Ryan's prompt: "surely multiply sales by avg LTV is easy" — correct, because the DGP is linear (`sales = base + Σ elasticity_c × spend_c + noise`), so a £-per-unit LTV is just one more scalar multiply, no new machinery. `summary()` gained `value_per_unit: float | None = None`:
  - When supplied, scales `incremental_revenue` by `value_per_unit` too (sales-units → £ via LTV, then × planned spend).
  - Also adds `cac_p10`/`cac_p90` (`1 / estimated_elasticity`, £ spent per unit of sales) and `roi_p10`/`roi_p90` (`estimated_elasticity * value_per_unit`, £ back per £ spent), computed per-simulation-draw then quantiled — **both are spend-independent channel properties in this linear model** (planned_spend cancels out algebraically; verified with a dedicated test that a £1 vs £10M planned_spend produce identical CAC/ROI columns). Worth remembering if the DGP ever gains saturation/diminishing-returns — that spend-independence would break, and CAC/ROI would need planned_spend as an input again.
  - **Numerical honesty check done before committing to the design:** spot-checked real simulated draws (corr=0.65, n=50) and confirmed a few near-zero/negative elasticity draws exist (search dipped to -0.03) but don't corrupt the p10/p90 CAC range at n_sims=50 — no hard guard added, the instability is called out explicitly in the docstring and the notebook as an honest feature of the diagnostic, not a bug to hide.
  - 6 new tests in `TestValuePerUnit`: backward compat, column presence, CAC formula match, ROI formula match, spend-independence, interaction with `planned_spend`. **77 tests passing** (up from 71).
  - Notebook 01: new subsection after Section 4's closing markdown — "CAC and ROI, when sales means customers" (markdown explainer + honesty caveat), a demo cell (`value_per_unit=150.0`, illustrative — same toy elasticities reused as if `sales` meant signups, explicitly flagged as not a real estimate), and a two-panel forest-plot-style chart (CAC left, ROI right; 80% range bar + midpoint dot + true-value tick per channel, same `channels_cfg` colour convention as earlier sections). Verified via the same scratch-copy-FAST_MODE-execution approach as before (nbclient); image renders cleanly, no errors. Committed file's new cells are unexecuted, same as the rest of Section 4 — needs the same full-mode local run.
  - **Jensen's inequality callout added** (prompted by Ryan noticing, from the rendered chart, that TV's true-value tick sits far from the CAC midpoint but right on the ROI midpoint). New markdown cell right after the chart: `roi = elasticity * value_per_unit` is a linear rescaling, so its range stays centred on the truth same as elasticity's does; `cac = 1 / elasticity` is convex, so by Jensen's inequality the simulated CAC range skews right of the true value (a fixed `1 / true_elasticity`, unaffected by noise) — worse for high-CV channels (TV here) than low-CV ones (Meta here). Framed as a second, £-denominated symptom of the same unreliability story, not a separate issue. Re-verified with the same scratch FAST_MODE execution — clean.
- **CAC/ROI extended to notebooks 02, 03, and 04 too** (Ryan's call: notebook 3/4 findings should feed the planned CMO page, and any £ number used there should be traceable to a committed notebook cell, same standard as notebooks 01/02 — not just carried over as prose).
  - **Notebook 02** (`02_phaser_walkthrough`): new subsection "CAC and ROI, before and after phasing" inserted after "The recommendation" section, before "The recommended schedule". Reuses `elast_data` already computed for the existing elasticity fan chart (no refit) — a 2×3 grid (CAC row, ROI row × tv/meta/search) across the three phasing levels (no phasing / recommended / max), same fan-chart visual language as the elasticity chart above it. A second cell prints the CAC/ROI range-width change specifically for the *worst-case* channel (the one `BudgetPhaser` actually optimises for, i.e. the channel driving `max_cv` at baseline) — deliberately not averaged across all channels, since only the worst-case channel's improvement is guaranteed by the grid search's own objective. Markdown explicitly flags that other channels can move either direction, same honesty framing as the elasticity bands.
  - **Notebook 03** (`03_time_to_benefit`): new subsection "CAC and ROI at the 1-year mark" inserted at the end of Section 1 (plan period sweep), before Section 2. The sweep itself stays in band-width terms (comparable across a huge plan-length range, the right metric for a sweep) — the new cell is a single grounding data point: refits `CollinearityDiagnostic` at baseline (history only) vs a 52-week max-phased plan (the plan length already annotated on the existing chart), converts via `summary(value_per_unit=150.0)`, and reports the CAC/ROI range-width change for the widest-band channel. Explicitly framed as "one data point, not a sweep."
  - **Notebook 04** (`04_channel_count_sweep`): new subsection "CAC and ROI for the N=15 example" inserted after Section 3's correlation-matrix cell, before the closing "What this means for practitioners". Reuses the already-built N=15 concrete example (`example_history`, `example_plan`, `example_phased`, `example_elasticities`) — fits two fresh diagnostics (unphased vs phased) and reports CAC/ROI range-width change for the widest-band channel, same `value_per_unit=150.0`. Framed as one worked example proving the £ framing holds at realistic channel count, not a new sweep.
  - **Numbers not yet locked** — all three additions were verified for *correctness* (no errors, sensible magnitudes, formulas match direct computation) via scratch FAST_MODE / truncated-notebook execution in the sandbox, same approach as notebook 01. FAST_MODE is noisy by construction (n_sims=10 vs 50, fewer phasing seeds) — notebook 02's FAST_MODE test run actually showed CAC/ROI *widening* for TV despite CV improving, which is expected noise at that sim count, not a bug (documented inline in the notebook as an honesty note either way). **All four notebooks need a full-mode local run before their new cells' outputs are committed** — same as always.
  - `ruff format`/`ruff check` clean; all 77 tests still pass (no package code changed in this round, only notebooks).
- **Branch:** `feat/pound-translation`. **Ryan ran all four notebooks full-mode locally at the end of this session** — outputs for the new CAC/ROI and £-translation cells (notebook 01 Sections 4 + CAC/ROI subsection, notebook 02's before/after CAC/ROI chart, notebook 03's 1-year-mark cell, notebook 04's N=15 CAC/ROI cell) should now be populated with real numbers, superseding the FAST_MODE-noisy sandbox spot-checks described above. Not yet reviewed/committed/pushed — that's the first thing next session, before anything else.
- **Sandbox environment note (new): no network access to `python-build-standalone` releases, so `uv run ...` cannot provision a fresh interpreter in this sandbox** (`uv sync`/`uv run` failed with a GitHub download connect error). Workaround used this session: built a throwaway venv from the sandbox's system Python 3.10 (`python3 -m venv /tmp/testenv --system-site-packages`), `pip install`-ed `pytest`, `pytest-cov`, `ruff`, `nbclient`, `ipykernel`, `matplotlib` individually (installs are slow — background with `sleep N` between polls, not a blocking foreground call), and ran tests/lint/notebook-execution against `src/` via `PYTHONPATH=src` since `pip install -e .` is blocked by the `requires-python >=3.12` guard on a 3.10 interpreter. This venv is sandbox-local only, not committed, not a replacement for `uv` — Ryan's local `uv run` workflow is unaffected. Also confirmed (again) that `pytest-cov`'s coverage-file cleanup fails on this mounted repo with a `PermissionError` on `.coverage.claude.pid*` — ran with `--no-cov` to get a clean pass/fail signal instead of an INTERNALERROR obscuring real failures.
- **Recurring sandbox git issue happened again:** a bare `git status --short` from the sandbox left a stale `.git/index.lock` the sandbox can't unlink. Per the existing "no git commands from the sandbox" rule, no further git commands were attempted — **Ryan needs to clear the lock locally** (`mv .git/index.lock .git/index.lock.OLD` or just delete it) before his own `git status`/`commit` will work.
- **Traceability gap found (Ryan, 2026-07-07): the Bayesian comparison in `docs/guide.html` Section 6 ("Won't a Bayesian Method Fix It?") is not backed by any committed notebook.** Session 14 computed the paired OLS-vs-Bayesian result (corr=0.9, 3-channel, weakly-informative PyMC priors, averaged over 10 noise draws, ~8–10% tighter Bayesian intervals) via a one-off script, copied the numbers into the guide's chart data, and the script was never committed. Confirmed via `pyproject.toml`: `pymc` isn't listed anywhere, not even in the `dev` dependency group — proof the analysis ran completely outside the tracked environment. This breaks the project's own standard (every other number in `guide.html`/`cmo.html` traces to a committed notebook cell, per the "outputs committed, no nbstripout" rule and today's CAC/ROI traceability work). **Ryan's call: this needs fixing with a real notebook, not just a note.** Scoped as item 0 below — do this before anything else next session, since it's fixing an existing accuracy/reproducibility problem, not adding a new feature.

## Status as of session 16 (2026-07-08, pound-translation merged + Bayesian comparison notebook built)

- **`feat/pound-translation` merged.** Session 15's £-translation + CAC/ROI work (item 1 of the session 14 list) is now on `main`. Housekeeping caught along the way: `.gitignore`'s `.coverage` entry widened to `.coverage*` so pytest-cov's pid-suffixed stray files (`.coverage.claude.pid6....`) are actually ignored — the exact-match glob had been missing them since session 12/15.
- **Item 0 of the session 15 next-slice list (Bayesian comparison notebook) built: `notebooks/05_bayesian_comparison.ipynb`.** Backfills `docs/guide.html` Section 6, which cited an 8–10% narrower-Bayesian-interval figure from a script that was never committed (traceability gap Ryan flagged 2026-07-07).
  - `pymc`, `arviz`, `pytensor` added to the `dev` dependency group in `pyproject.toml`.
  - Reproduces the paired comparison: 3-channel, correlation=0.9, OLS analytic 95% CI (`t_crit * se` from `(X'X)^-1`) vs Bayesian 95% HDI (PyMC, weakly-informative priors not tuned to true elasticities), averaged over `N_DRAWS` noise seeds (FAST_MODE: 3, full: 10) — carries forward the session 14 lesson that a single draw is unreliable (seed=0 alone can make Bayes look wider purely from a freak narrow OLS interval).
  - **Bug found and fixed during build: an uncentred design matrix made the Bayesian interval come out *wider* than OLS**, the opposite of what a proper prior should ever do. Cause: spend values sit at 60k–100k scale, and tying the intercept prior to `mean(sales)` on uncentred predictors aliases intercept and beta, forcing beta to absorb variance it wouldn't otherwise need. Fixed by centring each channel's spend around its own mean before the Bayesian fit only (a no-op for OLS, which is centering-invariant in its point estimate). **Worth remembering for any future from-scratch Bayesian model in this project: centre predictors before setting an intercept prior**, especially when spend is on a large absolute scale.
  - Convergence guard cells included (assert zero divergences, `r_hat < 1.05`) — a comparison this collinear is exactly the kind of posterior geometry (near-flat ridge) that can silently mislead if NUTS hasn't converged.
  - **FAST_MODE mechanics verified in the sandbox** (scratch copy, nbclient, `FAST_MODE=True`: 3 draws, 300 tune/300 draws/2 chains) — executes cleanly in ~9s, zero divergences, `r_hat` 1.015 (passes the loose FAST_MODE threshold). `ruff check`/`ruff format` clean.
  - **Sanity-checked full-mode-equivalent numbers via a raw (non-notebook) script** in the sandbox (10 draws, 1000 tune/1000 draws, but chains=2 not the notebook's chains=4, to fit the sandbox's ~45s-per-call ceiling): TV −6.6%, Meta −6.3%, Search −10.4% narrower (Bayesian vs OLS), broadly consistent with the old guide figure but not identical — **this was a sanity check only, not the number to cite.** The committed notebook itself was left with cells unexecuted (same convention as notebooks 01–04): needs a real `FAST_MODE=False` run locally (chains=4, per-fit cost noted in session 14 as ~7–25s, so a 10-draw x 4-chain run will take a few minutes — trivial locally, was the actual constraint in the sandbox).
  - `docs/guide.html` Section 6 **not yet updated** — waiting on Ryan's local full-mode run so the guide cites real committed numbers, not a sandbox estimate.
  - **Dependency pin bug found post-build: `arviz>=0.18` (open lower bound) let `uv sync` resolve a too-new ArviZ that broke `pymc`'s own import chain** (`ImportError: cannot import name 'concat' from 'arviz'` — ArviZ shipped a breaking refactor recently, `pymc` hasn't caught up yet). This is the exact "open lower bounds + `uv sync` = silent major-version drift" trap already called out in the session 2 learnings above, recurring in practice within the same session it was added. **Fixed:** pinned `arviz>=0.18,<0.24` and `pymc>=5.16,<5.26` in `pyproject.toml`, verified in a clean sandbox venv that this resolves to the same tested combo (`pymc 5.25.1` + `arviz 0.23.4`). Ryan re-ran `uv sync` locally to pick up the fix. **Not yet confirmed the fix works on Ryan's machine** — first thing to check next session if the local run didn't get that far.
- **Branch:** `feat/bayesian-comparison-notebook`, committed, pushed, merged.

## Status as of session 17 (2026-07-09, Bayesian comparison merged + docs/cmo.html built)

- **Confirmed Ryan's local `FAST_MODE=False` run of `notebooks/05_bayesian_comparison.ipynb` completed cleanly** (convergence checks passed, zero divergences, max `r_hat` 1.0026). Real numbers pulled straight from the executed notebook rather than transcribed: TV 6.5% narrower (OLS 0.929 vs Bayes 0.862), Meta 5.2% (0.984 vs 0.926), Search 9.1% (1.639 vs 1.473). Per-draw range crosses zero for TV/Meta (a few isolated draws showed Bayes slightly *wider*, up to +2.6%), so the guide's honesty caveat was reworded to reflect that rather than reuse the old placeholder wording.
- **`docs/guide.html` Section 6 updated** with the real numbers above: headline figure changed from the old uncommitted-script "8–10%" to "5–9%" in three places (fig-cap, body paragraph, key-number box), `#chart10`'s JS data updated to match.
- **Notebook 05 rewritten into reader voice** per session 16's item 0b: dropped the "backfilling an uncommitted script" framing and the session-14 citation, removed the maintainer TODO telling the reader to update the guide, moved the stray mid-cell `import pandas as pd` up to the top imports cell. Edited via direct JSON manipulation (source lists only), outputs/execution counts left untouched since they were already the real full-mode run.
- **`feat/bayesian-comparison-notebook` merged to `main`** (commit, push, PR, merge all done by Ryan locally, per the standing sandbox git-lock rule).
- **`docs/cmo.html` built** (item 1 of the session 16 next-slice list): the LinkedIn sales-hook CMO page, scoped in session 14. Short, one-scroll, no CV/OLS/statistical jargon. Built on **Ryan's explicit style brief this session**: match the voice of his Medium article ("Capturing The Long-Term Causal Effect Of Brand Marketing"), and no hyphens/em dashes used as a comma substitute, not common in UK writing, use commas or colons instead. Fetched the article via web_fetch to calibrate tone (punchy, direct, first person, rhetorical questions, British spelling) before writing.
  - **Content, all traceable to committed notebook cells:** the real 3-channel plan-year scenario from notebook 02 (TV £5.1m / Meta £4.1m / Search £3.1m, £12.3m total, correlation ~0.68), incremental-revenue 80% ranges from notebook 01 Section 4 (TV £664k–£2.36m, Meta £1.28m–£2.68m, Search £583k–£2.39m), and the 16% max-CV reduction figure from notebook 02's recommendation cell (same scenario, before vs after phasing). Deliberately did **not** pull in the CAC/ROI figures from notebooks 02–04 for this page, they're all under the `value_per_unit=£150` illustrative signups scenario, a different framing (sales-as-customers, not sales-as-revenue) to the revenue chart that anchors this page, and mixing the two would misattribute a metric to a chart it doesn't belong to. Those CAC/ROI numbers are still earmarked for item 2b below (the guide.html light-touch addition), where the illustrative framing can be made explicit instead of implied.
  - **One big £ visual:** a forest-plot-style chart (same SVG conventions as `guide.html`'s chart4) showing the three channels' 80% incremental-revenue ranges in £, self-contained, no external chart library.
  - **One-line fix + 3 key-number boxes:** £12.3m same annual total, £0 extra spend, 16% uncertainty reduction, tied to the exact scenario shown in the chart above it, not a separate claim.
  - **CTA button linking to `guide.html`** for readers who want the full method.
  - **Verified:** balanced HTML tags (20 open/close `<div>`), jsdom load with `runScripts: 'dangerously'` confirms the chart populates (32 SVG child elements, no JS errors), grepped for stray em/en dashes used as comma substitutes in prose (found none outside the chart's intentional range separator `£664k – £2.36m`, which is a standard range dash, not a comma substitute, and the page title's dash which was changed to "by Ryan O'Sullivan").
- **`docs/cmo.html` reworked twice more in the same session, per Ryan's live feedback:**
  1. **Restructured from a single-chart page into 5 numbered sections** (Problem, Cause, Fix, Impact, How it works), one chart + one paragraph per beat, per Ryan's request. For the Cause, Fix and Impact charts, reproduced `notebooks/02_phaser_walkthrough.ipynb`'s exact seeded `BudgetPhaser` recipe directly in the sandbox (`PYTHONPATH=src`, same seeds, `n_sims=50`, `grid_steps=25`, `n_phasing_seeds=10`, `fast_mode=False`, confirmed to reproduce the notebook's own committed numbers exactly) to get real weekly spend arrays and real before/after revenue ranges, rather than illustrative data. Flagged one honest asymmetry at the time: Meta barely improved under phasing at the package's 40% deviation default, only Search (the channel `BudgetPhaser` actually targets) showed the full improvement, called out explicitly in copy rather than glossed over.
  2. **Polish pass:** dropped "No sales pitch, just the working" from the CTA, removed the whisker end-caps from the Problem chart to match the cleaner look of the Impact chart, numbered the closing section "5 · How it works", dropped the "For CMOs" eyebrow prefix (just "Ryan O'Sullivan" now), added x-axis titles to the two revenue-range charts ("Incremental revenue (£m)") and the phasing chart ("Week (original plan year)" + Wk 1/Wk 52 tick labels, previously missing entirely), and rewrote the tagline from "Ask your MMM twice..." (Ryan's read: confusing for a CMO) to "Your model gives you one number, but the honest answer is a range."
  3. **Bigger impact number, Ryan's call on which lever:** investigated two real levers for a stronger headline number, both grounded in already-published research (notebook 03 Section 4): `max_weekly_deviation_pct` (found the actual ceiling is 100%, above that the recommended schedule starts producing negative weekly spend, confirmed numerically at 120%) and plan-period length (phasing sustained over multiple years, not just one, per notebook 03 Section 1's compounding finding). Presented both levers with real computed numbers via `AskUserQuestion`; **Ryan explicitly declined the most dramatic combination** ("people always spin stuff in sales pitches") and chose the more conservative pairing: 80% weekly deviation (not the 100% ceiling) sustained over 3 years (not 2). Reproduced that scenario for real: recommended alpha lands at 1.0 (full 80% swing), headline "uncertainty reduction" is now **75%** (Search, still the least-reliable-at-baseline channel, TV 63%, Meta 68%, all three now improve substantially, so the earlier "Meta barely moves" caveat no longer applies). Critically, **the "before" baseline was deliberately left untouched** (still Section 1's real 1-year, pre-phasing figures) so the before/after comparison stays honest, only the "after" state changed to reflect 3 years of sustained phasing. The Fix chart's grey "original plan" line was also swapped to year 1 of the actual 3-year plan (a different underlying draw to the old 1-year-only series) so it's internally consistent with what's actually being phased, rather than reusing an unrelated array.
  - Re-verified after every round: jsdom (all 4 charts populate, no JS errors), balanced tags, no stray em dashes.
  - Not yet committed. Ryan to review, commit (new branch, e.g. `feat/cmo-page`), push, merge.

## Next slice

1. Review and commit `docs/cmo.html` (this session's work, uncommitted).
2. **Rework `docs/guide.html` next** ("the more detailed one", Ryan's words closing this session) — no specific scope agreed yet, pick this up fresh next session. Likely candidates already queued from prior sessions, folded in below, but worth checking with Ryan first whether he wants the same kind of structural/numbers pass just done on `cmo.html`, or something else.
2a. **Light-touch CAC/ROI addition to `docs/guide.html`** (session 14/16 item 2b, still open). Not a full duplicate section, `guide.html` is deliberately CV/OLS-first for a practitioner audience, `cmo.html` is the pure-£ page, don't blur that line. Scope: a short paragraph + maybe one key-number box (likely Section 2 "The Diagnostic" or Section 3 "The Fix") showing the same CV number translates into a CAC/ROI range given `value_per_unit`, plus a direct link to `docs/cmo.html` for readers who want the full £ framing. Make the `value_per_unit=£150` illustrative-signups framing explicit here (unlike cmo.html, which avoided it entirely, see session 17 note above). The Jensen's inequality point (CAC skews right of true value, ROI doesn't, because `1/elasticity` is convex and `elasticity * value_per_unit` isn't) is a good fit for this technical audience if there's room for one more paragraph.
3. Final text/copy review pass on guide + README (session 11 item 4, still open) — do this last, after 2 lands, so it covers everything in one pass. Should also cover `cmo.html` once it exists on `main`.
4. PyPI packaging (session 11 item 5, still open).

## Status as of session 18 (2026-07-12, cmo.html polish + per-channel deviation constraints)

- **`docs/cmo.html` polish pass, following Ryan's line-by-line review of the merged v1:**
  - Tagline changed to "Your model gives you one number, but the honest answer is a (very wide) range."
  - Section 1 closing paragraph rewritten: dropped the "Meta's range is at least believable" framing (Ryan's read: it didn't actually look less uncertain), replaced with a point that no channel's range is tight and uncertainty doesn't scale with spend (TV more than doubles, Search is nearly as wide in absolute terms on a third of the budget).
  - Section 3 opening trimmed, dropped the "we tested a bolder swing" methodology aside, added Ryan's line about not needing an AI agent bolted onto the MMM.
  - Section 5 ("How it works") gained an opening line naming `how_wrong_is_your_mmm` as a free, open-source package, not just a footer mention.
  - New **section 6, FAQ** ("What people ask first", no CMO wording per Ryan's request): three questions, geo-lift test objection, how-long-until-benefit, what-to-hand-the-agency — in that order per Ryan's preference. A fourth candidate (short-term performance cost) was cut entirely, Ryan didn't want it, which also resolved a traceability concern flagged at the time (that answer would have cited a `guide.html` figure computed under a different scenario to this page's).
  - **New section nav bar added**, matching `guide.html`'s pattern exactly (`.snav` CSS + static links, no scroll-tracking JS, one class="active" on the first link) — Ryan wanted the same click-to-section header the full guide has.
  - **Title/H1 changed** from "How Much Did Your TV Spend Actually Make You?" to "How Wrong Is Your MMM?", matching `guide.html`'s branding. `<title>` tag updated to "How Wrong Is Your MMM? by Ryan O'Sullivan".
  - CTA copy changed from "Want the maths, the method and the code behind this?" to "Want a deeper understanding of the research behind this?".
  - Verified after every round: tag balance (regex open/close counts on div/section/p/nav/a), rendered and visually reviewed via the visualize tool at each step rather than jsdom this session (Ryan was looking at it directly).
- **`README.md`:** new "Guides" section replaces the old single "Practitioner guide" link, now lists both `cmo.html` ("How Wrong Is Your MMM?", two-minute read) and `guide.html` (full title, the practitioner's guide) as separate entries with one-line descriptions instead of one link.
- **BudgetPhaser per-channel deviation constraints, built and tested** (the feature idea flagged earlier this session): `max_weekly_deviation_pct` now accepts either the existing single `float` (backward compatible, applies to every channel) or a `dict[str, float]` keyed by channel, e.g. `{"tv": 0, "meta": 60, "search": 100}` for an agency that won't move TV at all, allows Meta up to +/-60%, and lets Search swing fully including down to zero spend in some weeks.
  - New `_resolve_channel_caps()` helper in `_phaser.py`: normalises float-or-dict into a per-channel dict, validates every value is in `[0, 100]`, validates a dict covers every plan channel (extra keys ignored, so one spend-constraints dict can be reused across channel-count experiments, same tolerance pattern as `planned_spend` in the £-translation feature). Called once in `BudgetPhaser.__init__` for fail-fast validation, and again inside `_generate_phased_schedule` (which now takes `float | dict[str, float]` directly, so the private helper still works standalone in tests).
  - **Resolved the open design question from the original flag:** each channel's effective cap is `alpha * channel_max_pct`, so a 0%-capped channel never moves regardless of alpha, and a 100%-capped channel can swing fully (including to zero spend) at alpha=1. Confirmed backward compatible: a uniform dict and the old float signature produce byte-identical output (same resolved caps, same RNG draw sequence, since the per-channel loop replaces one precomputed scalar with a per-channel lookup that's identical when all caps match).
  - **Did not redesign the recommendation mechanism itself** — `BudgetPhaser` still grid-searches a single global alpha and reports one recommended alpha, per-channel caps just constrain how far each channel is allowed to move at any given alpha. The "should recommended alpha be presented per-channel instead" question from the original flag wasn't addressed, still open if it turns out to matter in practice.
  - **18 new tests** across three new classes in `test_phaser.py`: `TestResolveChannelCaps` (float/dict resolution, missing-channel and out-of-range errors, boundary values 0 and 100 allowed), `TestGeneratePhasedSchedulePerChannel` (0%-cap channel literally unchanged, other channels still move, monthly totals still preserved per channel, float vs. uniform-dict equivalence, 100%-cap channel can reach zero spend), `TestBudgetPhaserPerChannelCaps` (dict accepted and stored, construction-time validation errors, recommended schedule respects a 0%-cap channel). **95 tests passing** (up from 77).
  - Verified in the sandbox: system Python 3.10 (project needs 3.12, `uv sync` still can't reach `python-build-standalone` here, same blocker as session 15) with `numpy`/`pandas` already present and `pytest`/`ruff` pip-installed fresh (`--break-system-packages`), run via `PYTHONPATH=src python3 -m pytest tests/ -q -o addopts=""` (pyproject's `addopts` include `--cov` flags that need `pytest-cov`, not installed, so overridden rather than installed to keep the environment minimal). `ruff format`/`ruff check` clean after one auto-format pass on `_phaser.py`.
  - **Notebook 02 demo added** (`notebooks/02_phaser_walkthrough.ipynb`), new closing section "Per-channel deviation constraints": TV locked at 0%, Meta capped at +/-60%, Search allowed the full +/-100% (can be switched off entirely in some weeks). Code cell fits a second `BudgetPhaser` with the dict form, prints the same recommendation summary as the existing single-alpha example, and asserts `np.allclose` that TV's recommended schedule is identical to the original plan. A two-panel chart shows TV's flat line against Search's much wider swing, monthly totals still exactly preserved for both. Closing markdown frames this as the realistic case: an agency rarely lets every channel move freely, and a recommendation that respects real constraints is more likely to get adopted than one that assumes every channel is equally negotiable.
  - Edited via direct JSON manipulation (source lists only, per the standing "don't use the Edit tool on `.ipynb` files" rule), verified via a scratch copy executed with `nbclient` in FAST_MODE against a `pytestkernel` ipykernel installed fresh in the sandbox (`nbclient`/`ipykernel`/`matplotlib` pip-installed this session, not yet in the project's `dev` dependency group — same one-off-tooling pattern as session 15/16). Executed cleanly, `tv_unchanged` printed `True` as expected. FAST_MODE's 2% CV reduction figure is sandbox noise only (n_sims=10, grid_steps=10, n_phasing_seeds=1), not the number to cite — same caveat as every other FAST_MODE spot-check in this project. The committed notebook's new cells are left unexecuted, same convention as every other notebook addition — **needs a real `FAST_MODE=False` run locally before the outputs are worth committing.**
  - `ruff format`/`ruff check` clean on the notebook after one auto-format pass (wrapped a couple of long lines in the new cells).
  - **Still not done:** `guide.html`/`cmo.html` don't mention per-channel caps anywhere yet, and notebooks 03/04 (which also use `max_weekly_deviation_pct`) still only demonstrate the single-float form. Not scoped as urgent, the core demo now exists in notebook 02; revisit if the feature needs more visibility.
  - Branch: `feat/per-channel-deviation` (created before edits started, per the session 12 workflow learning). Not yet committed — package changes, tests, README, cmo.html polish, and this notebook addition are all sitting together in the working tree. Ryan to review, run notebook 02 full-mode locally, then commit and open PR.

## Correction: asymmetric per-channel bounds, and why "never above plan" was wrong (same session, 2026-07-12)

Ryan reviewed the notebook 02 demo chart and caught two problems: Meta was missing from the plot (only TV/Search shown), and he asked the sharper question — what if an agency's real constraint is "Search: no change or -100% (full blackout)", i.e. a **one-sided** bound? The v1 per-channel design only supported symmetric +/-X caps, so this wasn't expressible at all.

**Investigating the fix surfaced a genuine mathematical impossibility, not just a missing feature.** `BudgetPhaser`'s core, non-negotiable guarantee is that monthly totals are preserved exactly. If a channel's bound never allows a week to exceed its own original plan (e.g. Search capped at (-100, 0)), then by construction every week's new value <= its original value, which means the month's *total* can only ever be <= the original monthly total, with equality only if literally nothing moves. So "some weeks reduced, monthly total unchanged, no week ever above plan" cannot coexist: the moment one week is cut, some other week in that month must make up the difference to hit the exact total, and there's nowhere for that extra budget to go except onto another week of the same channel, pushing it above its own original figure. Proved this numerically before saying anything to Ryan (a 4-week toy example, confirmed `sum(n_i) <= sum(o_i)` when `high=0` for all weeks).

**Presented Ryan with the choice** (via `AskUserQuestion`, this being a genuine product-semantics decision, not something to default silently): (a) blackout some weeks and redistribute the freed budget onto the channel's other weeks, preserving the exact monthly total but giving up the "never above plan" guarantee at the individual-week level, or (b) keep the hard per-week ceiling and let that channel's monthly total drift below plan when blackout weeks are used. **Ryan chose (a).**

**Turned out (a) is what the already-written code did all along** — the "bug" was in the *tests and docs*, not the algorithm. The existing raw-draw-then-rescale mechanism already: draws each week's raw deviation within bounds, then rescales the whole month by a common factor to hit the exact target. For a channel with `high=0`, weeks that draw near the low end land near zero (genuinely blacked out) and the rescale factor (always >= 1 in this case, since the raw sum is always <= target) pushes weeks that drew near zero deviation *above* their own original plan to compensate. That's exactly the "blackout and redistribute" behaviour Ryan asked for. So the fix was: stop claiming otherwise, and prove the redistribution actually happens rather than assuming it away in tests.

**Redesign implemented (replacing the earlier float-only-per-channel version from the same session):**
- `_resolve_channel_caps` renamed to `_resolve_channel_bounds`. Returns `dict[str, tuple[float, float]]` (low, high) instead of a single magnitude per channel.
- `max_weekly_deviation_pct` now accepts, at the top level or per channel in a dict: a single float `X` (symmetric +/-X, backward compatible), or an explicit `(low, high)` tuple/list for asymmetric bounds (`low <= high`, each within `[-100, 100]`). A dict can mix both forms per channel, e.g. `{"tv": 0, "meta": 60, "search": (-100, 0)}`.
- `_generate_phased_schedule` now draws `rng.uniform(alpha * low_pct / 100, alpha * high_pct / 100, ...)` per channel instead of a symmetric `uniform(-max_dev, max_dev)`. At alpha=0 every bound scales to (0, 0) regardless of its shape, so "no change from original plan" still holds universally — this was the one open design question from the original per-channel flag, now resolved.
- **Every docstring (`_resolve_channel_bounds`, `_generate_phased_schedule`, `BudgetPhaser` class) rewritten to state the redistribution behaviour explicitly** rather than implying a hard per-week ceiling: "(low, high) bounds the raw per-week draw, not the final schedule... a hard per-week ceiling and an exact monthly total can't both hold unless nothing moves at all."
- **Tests corrected:** the two that asserted the false "never exceeds original" invariant were replaced with tests that assert the *actual* guarantees — monthly totals preserved exactly under an asymmetric bound, and (explicitly, not just tolerated) that some weeks *do* land above the original plan, proving the redistribution happens rather than assuming it away. New tests also cover the tuple form directly: mixed float/tuple dicts, list-as-tuple, low > high validation, out-of-[-100,100] validation, and the alpha=0 fixed-point property for one-sided bounds. **107 tests passing** (up from 95 after the redesign, up from 77 before any of this session's per-channel work).
- **Notebook 02 demo rebuilt**, same section, now using the actual scenario Ryan asked about: TV locked (0), Meta symmetric (+/-60), Search asymmetric `(-100, 0)`. Fixed the missing-Meta chart bug (now a 3-panel chart, one per channel). Added a new code cell that explicitly counts and prints how many weeks Search spends *above* its original plan (confirmed 24 of 52 in the FAST_MODE sandbox check — the redistribution isn't a rare edge case, it's roughly half the weeks) and a markdown cell walking through why that's expected, not a bug. Verified via the same scratch-copy-`nbclient`-FAST_MODE approach as before; committed cells left unexecuted per the standing convention, needs a real `FAST_MODE=False` local run before those outputs are worth committing.
- `ruff format`/`ruff check` clean throughout.
- Branch unchanged: `feat/per-channel-deviation`, still uncommitted, still Ryan's to review and run full-mode locally before committing.

## Second correction: `Blackout` mode as a genuinely separate mechanism (same session, 2026-07-12)

Ryan reviewed the rebuilt Search chart and clarified the ask was misunderstood again: not "a continuous range biased towards zero" (what `(-100, 0)` does), but a literal binary switch — each week is either the full planned amount or exactly £0, nothing in between. Confirmed via `AskUserQuestion` before building anything, since this is a different sampling mechanism from a `(low, high)` range, not a parameterisation of one: (1) how many weeks go dark should be controlled by a per-week probability that scales with alpha (not an explicit weeks-per-month count), and (2) it should be a separate mode a channel picks, not combined with a range on the "on" weeks.

**Implemented as a new `Blackout` marker class**, exported from the package (`from how_wrong_is_your_mmm import Blackout`):
- `Blackout(prob=1.0)`: at alpha=1, each week is independently blacked out with probability `prob`, else left at 0% deviation (unchanged pre-rescale). Scales with alpha the same way every other spec does (probability = `alpha * prob`, so alpha=0 always means no blackout, same "no change" fixed point as ranges).
- `_resolve_channel_caps` renamed a second time, now `_resolve_channel_specs` (no longer just resolves to bounds — a channel's resolved spec is either a `(low, high)` tuple or a `Blackout` instance). `DeviationSpec` type alias extended to `float | tuple[float, float] | Blackout`. A dict can mix all three forms per channel, e.g. `{"tv": 0, "meta": 60, "search": Blackout()}`.
- `_generate_phased_schedule` branches per channel: `Blackout` draws a per-week Bernoulli (dark = -100%, else 0%) instead of `rng.uniform(low, high)`; everything downstream (the monthly-total rescale) is unchanged, so the same redistribution trade-off documented for one-sided ranges applies here too, spelled out again in `Blackout`'s own docstring rather than assuming the reader remembers.
- **Found a second real edge case while testing, before it could reach Ryan:** `Blackout(prob=1.0)` (the default) means every week is dark with *certainty*, so there's nothing left "on" to redistribute the month's budget onto — the existing `sum <= 0` fallback silently left the channel completely untouched, defeating the feature at its own default setting. **Fixed:** at least one week per month is now always forced to stay "on" (picked via `rng.integers` if the random draw would otherwise black out every week), documented explicitly in `Blackout`'s docstring: "prob=1.0 does not black out every single week of every month; it blacks out every week except (at least) one."
- **Nice property, worth documenting rather than just noting internally:** a dark week's final spend lands at *exactly* zero, not just close to it (zero times any positive rescale factor is still zero) — a stronger, provable guarantee than the continuous-range case, where "nudged towards zero" is only ever approximate.
- **22 new tests**: `Blackout` class itself (default/custom prob, range validation, equality), spec resolution (dict pass-through, top-level bare `Blackout()`, mixing with float/tuple), schedule generation (alpha=0 fixed point, some weeks go dark, dark weeks exactly zero, at-least-one-on safeguard holds even at prob=1.0/alpha=1, other channels unaffected, monthly totals preserved, reproducibility, lower prob blacks out fewer weeks on average across seeds), and `BudgetPhaser`-level construction/recommended-schedule checks. **128 tests passing** (up from 107).
- **Notebook 02 extended** with a new "Blackout mode: a hard on/off switch" subsection (same TV-locked/Meta-±60% setup, Search now `Blackout()` instead of `(-100, 0)`), a chart marking exactly-zero weeks with black dots, and printed stats confirming the honesty points directly (FAST_MODE sandbox check: 34 of 52 weeks at exactly £0, one week spending GBP 289,806 against an original max of GBP 85,838 — the redistribution concentrated onto very few "on" weeks rather than spread thinly). Closing markdown frames the range vs. blackout choice as two tools for the same ask, not one superseding the other. Same verification approach as before (scratch copy, `nbclient`, `FAST_MODE=True`), committed cells left unexecuted.
- `ruff format`/`ruff check` clean throughout, including after `ruff format` auto-wrapped the notebook's now-longer import line.
- Branch still `feat/per-channel-deviation`, still uncommitted. This branch has now been through three rounds of review-driven correction in one session (float-only → asymmetric range → separate blackout mode) — worth Ryan reading the whole diff carefully rather than skimming, given how much the design moved.

## Bug found in Claude's own process: unflushed notebook write raced with `ruff format` (same session, 2026-07-12)

Ryan ran the notebook 02 `Blackout` demo locally and hit `NameError: name 'Blackout' is not defined` on the `phaser_blackout = BudgetPhaser(...)` cell, pasted the full traceback. This looked at first like it could be a leftover local kernel or stale cached import, but checking the actual host file confirmed the import cell genuinely still read `from how_wrong_is_your_mmm import CollinearityDiagnostic, BudgetPhaser, simulate_spend` (no `Blackout`), even though the `Blackout` mode section had already been added lower in the notebook and reported as complete. **A real bug in Claude's process, not a user error or environment issue.**

**Root cause:** the notebook write that added `Blackout` to the import cell used `json.dump(nb, open(path, "w"), indent=1)` — an inline `open()` with no explicit `with`/close — immediately followed by invoking `ruff format` as a separate subprocess on the same file. On this sandbox's mounted filesystem, the write had not flushed to disk before `ruff format` read the file, so `ruff format` read the *stale* pre-edit content and silently overwrote the fix when it re-saved (ruff format always rewrites the file, even when reformatting is a no-op on the content it actually saw). Every other notebook edit that session used a proper `with open(...) as f:` block and landed correctly — this was the one edit that didn't, and it happened to be the one that mattered most (the actual new import).

**What made this hard to catch in the moment:** an immediate bash re-read in the same shell session, right after the faulty write, appeared to show the fix had succeeded — printing the corrected import line. This was misleading; whatever caching or timing let that re-read see the write is not the same guarantee that `ruff format`'s separate subprocess read would see it a moment later. The bash-side "looks fine" check was not sufficient evidence, and no host-side (Read tool) verification was done at the time before reporting the work as complete.

**Fix applied and verified this session:**
- Re-did the import-cell edit using a proper `with open(path, "w") as f: json.dump(nb, f, indent=1); f.write("\n")` block, with `assert` guards before/after to catch a silent no-op (`assert "Blackout" not in src` before the replace, `assert new_src != src` after).
- Verified the fix via the **host-side Read tool** (not bash) directly on `/Users/ryanosullivan/Development/how_wrong_is_your_mmm/notebooks/02_phaser_walkthrough.ipynb` — this is the authoritative check, since it reads exactly what Ryan's editor and `git` see, not what the sandbox's bash re-read claims.
- Also cleared the stale committed error output on the `Blackout` fit cell (it still held the traceback from Ryan's failed run) so the committed notebook doesn't show a dead error once it's re-executed.
- Re-ran `ruff format`/`ruff check` (one file reformatted, all checks passed) and the full test suite (`128 passed`), then **re-verified the import cell again via host-side Read** after the reformat, specifically because this is the exact sequence (edit → separate-process reformat) that caused the original bug.

**Standing rule going forward, to add to session learnings:** any notebook write in this sandbox must use an explicit `with open(path, "w") as f:` block, never a bare inline `open()`. And whenever a notebook write is immediately followed by invoking a separate tool/process on the same file (`ruff format`, `nbclient`, etc.), verify the change survived via the **host-side Read tool** afterwards, not just a bash re-read in the same shell — bash re-reads in this environment have been shown to report success even when the write did not actually persist for a subsequent separate process to see.

Branch still `feat/per-channel-deviation`, still uncommitted. **Ryan should re-pull/re-open the notebook in his editor before re-running it** — even though the disk content is now confirmed correct, his editor may still have the old (buggy) version cached in its open buffer from before this fix.

## Status as of session 19 (2026-07-13, dropped asymmetric ranges + Blackout dark-week cap)

- **Reviewed the still-uncommitted `feat/per-channel-deviation` branch at the start of this session.** Ran the suite directly rather than trusting the notes: 128 tests passing, `ruff check`/`ruff format --check` clean (notebook 02 itself clean; the other three notebooks' formatting gaps are the pre-existing, unrelated ones from session 12). Also found the notebook was further along than NOTES implied: everything through the asymmetric-range demo cells was already executed with real `FAST_MODE=False` numbers (17% CV reduction, 23 of 52 weeks above plan) — only the `Blackout` demo cells were genuinely unexecuted.
- **Ryan flagged the asymmetric-range chart as confusing:** Search's `(-100, 0)` bound produced a chart where spend visibly spiked well above the grey "original plan" line in several weeks, which reads as a broken "never above plan" promise even though it's mathematically correct (monthly-total preservation forces it). Talked through the options (keep + document better / add an opt-out to let the monthly total drop instead / drop the one-sided range and rely on `Blackout` for "turn it down or off" asks). **Decided to drop it.** Reasoning: the overshoot is driven by how far the deviation shape's *mean* sits from zero, not by whether the range happens to be one-sided — `Blackout` has the identical mathematical trade-off. But `Blackout` reads better because it maps onto a real, recognisable media-planning concept (flighting/pulsing: full spend some weeks, dark others), so "the on weeks run hot" is an expected consequence of a known strategy rather than an arbitrary chart anomaly. A continuous partial-reduction range doesn't map to anything a practitioner already recognises, so its overshoot just looks broken.
- **Removed the `(low, high)` tuple/list form entirely** from `_phaser.py`: `DeviationSpec` is now `float | Blackout` only. `_resolve_channel_specs` raises a `TypeError` with an explanatory message ("explicit (low, high) ranges are no longer supported... Use a single float... or Blackout()") if a tuple/list is passed. Module, class, and function docstrings (`_resolve_channel_specs`, `_generate_phased_schedule`, `BudgetPhaser`) all rewritten to explain *why* this was dropped, not just that it was.
- **Ryan then looked at the (still-uncapped) `Blackout()` chart and flagged it too** — 39 of 52 weeks dark, one surviving week spiking to 3.38x the original max (GBP 289,806 vs GBP 85,838), more than a media buyer could plausibly deploy in a single week, and "too many blackouts" generally. Root cause: every week is an independent draw, so a bad month can land several dark weeks at once, and the fewer weeks left "on," the bigger the spike needed to hit the same monthly total.
- **Added `Blackout(max_dark_weeks_per_month=...)`, default `None` (legacy unlimited behaviour).** When set, a month independently "activates" blackout with probability `alpha * prob`, and if it does, exactly `min(cap, n_weeks - 1)` weeks (never all of them) are chosen at random to go dark — bounding how much any one month can divert, and so how large the compensating spike can get. **Verified numerically before touching the notebook or tests:** at the same seed, `max_dark_weeks_per_month=1` cut dark weeks from 39/52 to 12/52 (~1/month, as intended) and the spike from 3.38x down to 1.27x the original plan — confirms the cap addresses both of Ryan's complaints (too many blackouts, implausible increase) without needing a separate "stick to budget" opt-out that would relax the monthly-total guarantee (which would also conflict with `cmo.html`'s existing "£0 extra spend, same annual total" claim).
- **Tests:** removed all asymmetric-tuple tests (`TestResolveChannelSpecs`, `TestGeneratePhasedSchedulePerChannel`, `TestBudgetPhaserPerChannelCaps`), replaced with two tests confirming the tuple form now raises. Added `Blackout` validation/equality tests for the new param, a new `TestGeneratePhasedScheduleBlackoutCapped` class (cap limits dark weeks per month, cap reduces max spike vs uncapped at the same seed, monthly totals still exact, dark weeks still exactly zero, an oversized cap still leaves one week on, reproducibility), and two `BudgetPhaser`-level tests for the capped case. **132 tests passing** (up from 128), `ruff check`/`ruff format --check` clean throughout.
- **Notebook 02 rewritten:** dropped the entire asymmetric-range demo subsection (the cells that already had real executed output) rather than leave dead content behind. Kept the `Blackout` demo, switched it to `Blackout(max_dark_weeks_per_month=1)`, and rewrote the surrounding markdown to explain the cap rather than compare against the now-removed asymmetric range. Edited via the standard `with open(...) as f: json.dump(...); f.write("\n")` pattern (per the session-18 unflushed-write lesson), verified via host-side Read both before and after running `ruff format` on the file. Sanity-executed the whole notebook end-to-end in a scratch in-memory copy (`nbclient`, `FAST_MODE=True`) to confirm it runs clean top to bottom with the new code — 11 of 52 dark weeks, 1.45x spike, sensible — then re-confirmed via host-side Read that the committed file's new cells are still unexecuted (the scratch run was in-memory only, never written back). **Needs the standard full-mode local run before those outputs are worth committing**, same as every other notebook change in this project.
- Branch still `feat/per-channel-deviation`, still uncommitted. This is now its fourth round of review-driven correction in two sessions (float-only → asymmetric range → Blackout → drop asymmetric range + cap Blackout) — worth Ryan reading the whole diff carefully rather than skimming, same caution as session 18's closing note.

## Ryan's follow-up questions, same session: locking TV, Blackout's extra leverage, and all-Blackout

Ryan asked three quantitative questions after the design fixes above: (1) how much does *not* phasing TV cost, relative to phasing it normally, (2) with TV phased normally again, how much extra reliability does `Blackout` buy over a continuous range on Search, at the same nominal deviation budget, and (3) — after seeing (2) — whether `Blackout` is "the way to go" generally, i.e. what happens if *every* channel uses it. Ran all three directly rather than reasoning them out, using this notebook's own full-mode settings (`n_sims=50`, `grid_steps=25`, `n_phasing_seeds=10`) — the baseline scenario reproduced the existing "16% CV reduction" figure from "The recommendation" exactly, a good cross-check that the sandbox numbers are trustworthy.

**Findings, questions 1-2 (single data seed, same full-mode settings as the rest of the notebook):**
- Baseline (all three symmetric +/-40%): 16% max-CV reduction (TV 15%, Meta 17%, Search 16%).
- TV locked, Meta/Search symmetric +/-40%: 12% max-CV reduction (TV only 4%, Meta 11%, Search 13%). TV's own reliability gain comes almost entirely from TV itself moving — locking it removes most of that channel's benefit, with only a small residual improvement from Meta/Search decorrelating away from the now-fixed TV.
- TV/Meta symmetric +/-40%, Search `Blackout(max_dark_weeks_per_month=1)`: 26% max-CV reduction (TV 20%, Meta 12%, Search 37%). Blackout buys substantially more reliability per unit of nominal deviation than a continuous range — plus a knock-on improvement for TV and Meta, since phasing Search harder loosens its collinearity with both.

**Finding, question 3 (all-Blackout) — checked across three independent data draws before trusting it, given how large the effect looked:** switching every channel to `Blackout(max_dark_weeks_per_month=1)` beat both the baseline and the Search-only Blackout scenario at all three draws tested (max-CV reduction ~47-59% for all-Blackout vs ~16-24% baseline and ~17-49% Search-only), with a consistent, modest practical cost across draws (~12 of 52 weeks dark per channel, ~1.2-1.4x original max weekly spend — no worse than the single-channel case). **Directionally, Blackout looks like the way to go**, but this is still three toy-example draws at one correlation level (0.7) and one channel count (3): a proper robustness study (varying correlation and channel count, the way notebooks 03/04 did for phasing generally) would be needed before calling it settled rather than a strong early signal.

- **Notebook 02 subsection extended** ("Locking TV vs letting Search go dark: three follow-up questions", right after the Blackout demo) to a 4-scenario comparison table (`comparison_df`, now including per-channel max-spend ratios) and grouped bar chart, plus markdown covering all three findings above including the multi-seed robustness check. Sanity-executed the whole notebook end-to-end in a scratch `nbclient` FAST_MODE copy (noisy numbers as always, not for citation) to confirm no runtime errors; committed cells left unexecuted per the standing convention, needs the usual full-mode local run before the outputs are worth committing. No package code changed for this addition (pure notebook analysis using existing `BudgetPhaser`/`Blackout`), so the 132-test count and `ruff` status above are unaffected.

## Ryan's follow-up questions, same session: does Blackout's edge hold for time-to-benefit and channel count?

After the all-Blackout finding above, Ryan asked whether it should be checked against notebook 03's territory (does Blackout reach the same benefit faster?) and notebook 04's (does it survive at higher channel counts, or is it a 3-channel artefact?). Ran both directly.

**Time to benefit (notebook 03):** at this notebook's own seeds (history=42, plan=99), the current default (`dev=40`) needs 52 weeks for a 20% band-width reduction, 104 weeks for 30%, 156 weeks (3 years) for 50%. `Blackout(max_dark_weeks_per_month=1)` reaches 20% in 13 weeks, 30% in 13 weeks, 50% in 26 weeks — a 4-6x speed-up. Spot-checked at two more history/plan seed pairs in the sandbox, same pattern each time (Blackout consistently reaching in ~1 year what the continuous range needs 3-5 years for).

**Channel count (notebook 04):** a lighter sandbox check (n_sims=30, grid_steps=10, 3 draws, versus Section 2's own 50/15/8) found Blackout's advantage holding at every N from 5 to 20 with no dilution: ~22-31% reduction for the continuous range vs ~49-51% for Blackout at every channel count, mirroring the flat-across-N pattern this notebook already established for the baseline effect and the standard phasing benefit.

- **Notebook 03:** new "Part D — Does Blackout reach the benefit faster than a continuous range?" inserted between Part B and Part C, reusing Part B's exact sweep pattern (`plan_lengths_s4`, `N_PHASING_SEEDS`, band-width metric) with `Blackout(max_dark_weeks_per_month=1)` as a second series alongside `dev=40`. Same "weeks to reach X% reduction" reporting style as Part B. `Blackout` added to the notebook's imports.
- **Notebook 04:** new "Section 2b — Does Blackout's extra leverage hold at higher channel counts?" inserted between Section 2 and Section 3, reusing Section 2's own constants (`CHANNEL_COUNTS`, `N_SIMS`, `PHASING_DRAW_SEEDS`, `make_channels_and_elasticities`, `band_width`) so a full local run automatically matches Section 2's own 8-draw rigor, even though the sandbox spot-check used only 3 draws for time. Grouped bar chart (continuous range vs Blackout, error bars = draw-to-draw std) alongside Section 2's existing bars.
- Both notebooks re-verified: `ruff check`/`ruff format --check` clean (notebook 01 is the only remaining pre-existing gap, unrelated), 132 tests still passing (no package code touched), and both notebooks executed cleanly end-to-end in scratch `nbclient` FAST_MODE copies with no runtime errors. Committed cells left unexecuted per the standing convention — need the usual full-mode local runs before the numbers are citable.

## Same session, continued: "what does optimal Blackout phasing achieve, and what does it look like?" added to notebooks 03 and 04

Ryan asked for the Blackout equivalent of two existing "what does optimal phasing achieve, and what does it look like" sections (notebook 03's Part C, notebook 04's Section 3) before logging off. Computed real numbers first, then built both additions matching each notebook's exact structure (bar chart + weekly-spend time series, correlation matrix for notebook 04), so nothing here is placeholder text.

- **Notebook 03:** new subsection ("What does optimal Blackout phasing achieve, and what does it look like?") inserted between Part D and Part C, mirroring Part C's own two charts (per-channel band-width bar chart, weekly spend time series) but for `Blackout(max_dark_weeks_per_month=1)` at the same fixed 52-week plan, with dark weeks marked on the spend chart. **Finding: Blackout's 52-week result beats Part C's own "optimal" `dev=80%` result outright** — 64.5%/61.5%/64.6% band-width reduction (TV/Meta/Search) vs Part C's 46.8%/54.2%/53.4%, despite `dev=80%` already being double the package's 40% default. Each channel dark 12 of 52 weeks, surviving weeks at 1.2-1.3x original max — consistent with every other Blackout result this session.
- **Notebook 04:** new subsection ("What does the Blackout-phased schedule look like at N=15?") inserted after the CAC/ROI subsection, before the closing "What this means for practitioners" synthesis. Reuses Section 3's own `example_history`/`example_plan`/`example_channels`/`example_picks`, repeating Section 3's two views (weekly spend for 4 representative channels with dark weeks marked, before/after correlation heatmap) with `Blackout(max_dark_weeks_per_month=1)` instead of the continuous range. **Finding: mean pairwise correlation drops from 0.684 to 0.067 (near zero) under Blackout, versus 0.684 → 0.356 (roughly halved) for the continuous range in Section 3** — max band width fell 57.5%, well above Section 2's ~17.7% figure for the continuous range at N=15.
- Both notebooks re-verified after this round: `ruff check`/`ruff format --check` clean, 132 tests still passing (no package code touched all session), both execute cleanly end-to-end in scratch `nbclient` FAST_MODE copies with zero errors, committed cells confirmed still unexecuted via host-side Read. **One process note:** `ruff format` only normalises missing Jupyter cell `id` fields as a side effect of actually reformatting code — notebook 03's new cells needed no code reformatting, so `ruff format` reported "left unchanged" and silently skipped adding their `id`s, triggering an `nbformat` `MissingIDFieldWarning` on the next `nbclient` run. Fixed by explicitly assigning `uuid4().hex[:8]` ids to the 4 cells missing them, then re-verified clean. **Worth remembering for future notebook edits: don't assume `ruff format`'s exit message reflects whether cell metadata (like ids) got normalised — check for missing ids directly if a new cell block was inserted and the file was already lint-clean.**

## Next slice

- **Add the Blackout research to `docs/guide.html`.** Neither `guide.html` nor `cmo.html` currently mention per-channel deviation caps or `Blackout` at all (flagged already in session 18). There's now a substantial, robustness-checked body of results across all three notebooks: Search-only Blackout ~26% CV reduction vs baseline ~16%, all-Blackout ~47-59% across three data draws, and Blackout's 52-week result beating even the `dev=80%` "optimal" case (notebook 02); a 4-6x faster time-to-benefit (notebook 03, now with its own "what does it look like" chart); no dilution across channel counts 5-20, plus a correlation-matrix result showing near-total decorrelation at N=15 (notebook 04). Decide scope next session — this is easily substantial enough to be its own guide section now. Should sit alongside the still-open items from session 17/18 (guide.html rework, CAC/ROI light-touch addition, final copy pass, PyPI packaging).
- **Ryan is logging off — nothing further attempted this session.** Next step on return: pull this branch fresh, run notebooks 02, 03, and 04 full-mode locally (all three now have unexecuted Blackout-related cells needing a real run, not just one), review the diff (this branch has had a lot added across two sessions: per-channel caps → asymmetric range → Blackout → drop asymmetric range → cap Blackout → three rounds of "does it hold up" research), then commit and push `feat/per-channel-deviation`.

## Status as of session 20 (2026-07-14, Blackout research added to docs/guide.html)

- **New guide.html Section 8, "Going Further: Per-Channel Rules, and a Harder On/Off Lever," added** (item flagged at the end of session 19). Covers the per-channel deviation constraint feature and `Blackout` mode, still sitting uncommitted on `feat/per-channel-deviation` at the time of writing.
  - Content: locking a channel (TV) costs more than it looks like (16% baseline vs 12% with TV locked); `Blackout` beats a continuous range at the same nominal budget (16–24% baseline vs 17–49% Search-only-Blackout vs 47–59% all-Blackout, across 3 draws); optimal Blackout (cap=1) beats even the ±80% "aggressive" continuous range per channel (64.5/61.5/64.6% vs 46.8/54.2/53.4% for TV/Meta/Search); 4–6x faster time-to-benefit (20/30/50% reduction reached in 13/13/26 weeks vs 52/104/156 weeks); holds at higher channel counts (N=15 mean pairwise correlation 0.684 → 0.067 under Blackout vs 0.684 → 0.356 for the continuous range; a lighter, explicitly-flagged-as-preliminary 5–20 channel sweep shows Blackout roughly doubling the continuous range's reduction throughout).
  - Nav bar gained an 8th link (`#blackout`). Section 7's "up next" and the closing "Bringing it together" synthesis updated to reference eight sections and fold in the Blackout takeaway.
  - Two new charts: chart12 (bar + min/max range whisker, 3-scenario comparison), chart13 (grouped per-channel bars, ±80% continuous vs Blackout cap=1). Followed existing SVG/JS conventions exactly (same `el()` helper, margin/scale/gridline pattern as chart8/9).
  - **All numbers sourced from the still-uncommitted `feat/per-channel-deviation` branch's notebook markdown cells** (notebooks 02, 03, 04), which themselves came from live sandbox reproductions this session and last using each notebook's own full-mode settings (not FAST_MODE spot-checks), cross-checked against already-established figures (e.g. the 16% baseline reproduced exactly). The one exception, explicitly flagged both in NOTES and in the guide's own prose, is the 5–20 channel-count Blackout sweep, which notebook 04 itself labels a "lighter sandbox spot-check" at reduced simulation settings, called out as preliminary in the new guide section rather than presented with the same confidence as the rest.
  - **Important caveat, not yet resolved: none of this is backed by a committed, executed notebook cell yet.** `feat/per-channel-deviation` (sessions 18–19) still needs Ryan to pull fresh, run notebooks 02/03/04 full-mode locally, review the diff, and commit/push/merge, exactly as flagged at the end of session 19. This guide section was built ahead of that on the reasoning that the underlying numbers are well-derived and internally cross-checked (same approach as the session 14 Bayesian section draft, finalised in sessions 16–17 once Ryan's local run confirmed the real figures). **Once notebooks 02/03/04 are run full-mode and committed, the numbers in this new Section 8 need to be checked against the real committed output and corrected if they differ**, same as the Bayesian section's own history. Don't treat Section 8's numbers as final until that reconciliation happens.
  - Verified via jsdom: all 13 charts (including the 2 new ones) populate with no JS errors. Tag balance confirmed (div/section/nav/a/svg all net zero). Nav `href`s match every `section id`. Em dashes checked: the only em dashes in the new section are in the "Next:" up-next label and the HTML section-marker comment, both pre-existing exempted categories (title/nav-label/code, not prose), consistent with the session 11 style rule.
  - Not yet committed, same branch situation as the rest of `feat/per-channel-deviation` (this guide change sits on top of an already-uncommitted branch, not a clean one).
- **Renamed `docs/cmo.html` → `docs/introduction.html` and `docs/guide.html` → `docs/research.html`**, per Ryan's naming preference. Updated the one cross-link (`introduction.html`'s closing CTA, was `href="guide.html"`, now `href="research.html"`) and both `README.md` Guides-section links (GitHub Pages URLs updated to match). No other files referenced the old names (confirmed via a full-repo search; the only other hits were third-party `.venv` package docs and an unrelated ArviZ URL, neither needs touching). **Note: the live GitHub Pages URLs (`raz1470.github.io/how_wrong_is_your_mmm/introduction.html` / `research.html`) won't resolve until this rename is committed and pushed** — until then the old `cmo.html`/`guide.html` URLs are still what's actually live.

- **README.md accuracy pass, prompted by Ryan reviewing it directly:**
  - **Notebooks table was missing 04 and 05 entirely.** Added rows for `04_channel_count_sweep` and `05_bayesian_comparison`, and folded the £-translation/CAC-ROI and per-channel-deviation-constraints additions into the 01/02 descriptions (the latter is part of the still-uncommitted `feat/per-channel-deviation` branch, same caveat as everywhere else in this session's notes).
  - **Guides section subtitle inconsistency fixed:** the `introduction.html` link had no subtitle while the `research.html` link had one baked into the link text (`— A Practitioner's Guide to Collinearity Bias`). Rather than force a mismatched subtitle onto the other page (their actual in-page taglines differ a lot in length/style), removed the subtitle from the `research.html` link so both just read "How Wrong Is Your MMM?", relying on the description line below each (already present, already does the differentiating job: "two-minute read" vs "full method").
  - **Quick Start had a real bug: found `CollinearityDiagnostic(correlation=0.7, seed=0)` in the code sample.** `seed` isn't a valid parameter, the actual one is `spend_seed`. Confirmed by actually instantiating it in the sandbox (`PYTHONPATH=src`): `seed=` raises `TypeError: unexpected keyword argument 'seed'`. Fixed to `spend_seed=0`. Also verified the printed `summary()` output in the same snippet against a real run, mean_estimated/coef_of_variation values match the actual current source to 3dp exactly (0.329/0.357 TV, 0.503/0.288 Meta, 0.357/0.623 Search, at `correlation=0.7, spend_seed=0`, default `n_sims=50`) — that part was already correct. Also added two clarifying comment lines above the `BudgetPhaser(history_df=history, plan_df=plan)` line, since `history`/`plan` weren't defined anywhere in the snippet.
  - **"Key result" table checked against the current source and against `research.html`'s own chart2 caption: matches exactly** (TV [0.20, 0.46], Meta [0.34, 0.66], Search [0.11, 0.62] at correlation 0.70). The Part 1/2/3 CV figures (36%, ~30%, ~53%) also match `research.html`'s body text exactly. **Recommendation: don't pull the newer Blackout figures into this section** even though they're bigger and more dramatic, they live on the still-uncommitted `feat/per-channel-deviation` branch and haven't been through Ryan's own full-mode local run yet, same traceability standard as everywhere else this session.
  - **"Development" section test count updated 77 → 132**, per Ryan's explicit call (counted directly: `grep -rE "def test_" tests/*.py | wc -l`). Note this number is only accurate once `feat/per-channel-deviation` actually lands on `main` — until this branch is committed/pushed/merged, `main` itself still only has 77 tests, so there's a short window where README and `main` disagree.

## Same day, continued: reconciled guide.html Section 8 and README against Ryan's real full-mode notebook runs

Ryan re-ran notebooks 02, 03, and 04 full-mode locally (all cells now executed, confirmed via `execution_count` check: 0 unexecuted code cells in any of the three). Checked every number in the new `research.html` Section 8 against the real committed output before treating any of it as final, per the standing plan.

- **Confirmed exact matches, no changes needed:** the 3-scenario comparison (baseline/Search-only-Blackout/All-Blackout, chart12 ranges), the optimal-Blackout-vs-dev=80 per-channel comparison (chart13: 64.5/61.5/64.6% vs 46.8/54.2/53.4%, matches notebook 03's real output to the decimal), the N=15 correlation collapse (0.684 → 0.067) and its 57.5% band-width reduction.
- **Found real drift, corrected:**
  - **Time-to-benefit numbers were wrong.** The guide said 52wk→13wk (20%), 104wk→13wk (30%), 156wk→26wk (50%), "4–6x". Real notebook 03 output (Part D, full settings) is 39wk→8wk (20%), 39wk→13wk (30%, the continuous-range curve isn't perfectly monotonic so 20% and 30% clear at the same point), 130wk→26wk (50%), "3–5x". Updated `research.html`'s knums and surrounding prose to match.
  - **Channel-count Blackout sweep was reported as a "lighter, preliminary" spot-check with placeholder-ish numbers (~22–31% vs ~49–51%, "~2x").** Ryan's real full-settings run (this section's own `n_sims=50`/`grid_steps=15`/8 draws, no longer a lighter check) gives dev=40: 17.0/17.8/17.7/15.5% vs Blackout: 56.3/54.0/52.6/46.7% at N=5/10/15/20, roughly 3x throughout. Rewrote the guide's prose and knum to state this as the real, fully-powered result rather than a preliminary one.
- **Also found and fixed the same drift at the source: two notebook markdown cells were themselves stale relative to their own newly-executed code cells** (Ryan reran the code, but the descriptive prose above/below still described the old sandbox estimates). Fixed directly in the notebooks, following the standing `with open(...) as f: json.dump(...); f.write("\n")` pattern, verified via host-side Grep afterward (not bash) per the session 18 unflushed-write lesson:
  - `notebooks/03_time_to_benefit.ipynb`, the Part D markdown cell (right after the "weeks to reach X%" code cell): updated to the real 39/39/130 vs 8/13/26 week figures.
  - `notebooks/04_channel_count_sweep.ipynb`, the Section 2b markdown cell: updated to state the real per-N reduction figures and dropped the "lighter sandbox spot-check, not yet the fully-powered result" caveat, since it now is the fully-powered result.
- Re-verified `research.html` after the edits: all 13 charts still populate via jsdom with zero JS errors.
- **`README.md` was already checked against the same source earlier the same session and found accurate** (Key result table, Part 1/2/3 CV figures) — no further changes needed there beyond the four fixes already logged above (missing notebooks 04/05, subtitle inconsistency, the `seed=`/`spend_seed=` bug, and the 77→132 test count update).
- Notebooks 02/03/04 are still uncommitted on `feat/per-channel-deviation`, now with a) real full-mode output in every cell and b) markdown text that matches that output. This branch is much closer to ready to commit/push/merge than at the start of the day.

## Same day, continued: research.html restructured from 8 sections to 6, mirroring introduction.html

Ryan's read after seeing the finished Section 8 work: "we cover the right stuff in research but the structure is a bit all over the place and bloated," and separately, "I like the structure of introduction, almost could copy it with deep dive on each." Agreed a full restructure rather than a small tidy-up.

- **Dropped the header gauge icon from `research.html`** (Ryan: "I hate the image top right"), simplified `.hdr` back to a single block, removed the now-unused `.hdr-text`/`.hdr-icon` flex CSS.
- **`introduction.html` eyebrow updated** to "Marketing Science · Ryan O'Sullivan" (was just "Ryan O'Sullivan"), matching `research.html`'s eyebrow.
- **`research.html` restructured from 8 sections to 6**, mirroring `introduction.html`'s Problem → Cause → Fix → Impact → How It Works → Questions arc, with research's much deeper technical content as the "deep dive" on each beat:
  - **Section 1, The Problem:** now the symptom + CV concept + personalised diagnostic, absorbing the old standalone "Diagnostic" section. Ends with "Getting a personalised number" (was old Section 2's content) instead of a separate nav item.
  - **Section 2, The Cause:** split out from old Section 1, now just the correlated-spend mechanism (why channels move together, what it does to the model). Was previously blended into one long "Problem" section.
  - **Section 3, The Fix:** unchanged mechanism content, plus a new "Channel-specific constraints" subsection absorbing the locking-a-channel and Blackout-mechanism content that used to live in the standalone Section 8.
  - **Section 4, The Impact:** merges the old Timeline and Does-It-Scale sections (previously split, both about "how much/how long/how robust"), plus Blackout's speed result and Blackout's channel-count result folded in as their own subsections right next to the equivalent continuous-range numbers, instead of being repeated later in a separate section.
  - **Section 5, How It Works:** the three-step pipeline diagram, moved down here from its old spot right after the TL;DR (Ryan: "the three step pipeline diagram comes in too early," correctly guessed it should live where `introduction.html` puts its equivalent, near the end).
  - **Section 6, Questions:** merges the old standalone "Won't a Bayesian Method Fix It?" section in as the first of four objections (Ryan's call from earlier in the session), alongside the existing geo-lift/performance-cost/hierarchical-pooling objections. Flattened Bayesian's own internal h3 subheadings into plain prose so it reads at the same depth as the other three objections.
  - Old Section 8 ("Going Further") deleted entirely, its content fully redistributed into 3/4 above, not just trimmed.
  - Closing "Bringing it together" updated from "eight sections" to "six sections," content otherwise held up without changes.
- **How it was built:** given the scale (a ~2,000-line file, moving and merging six of eight sections), did this via Python scripts operating directly on the file rather than dozens of manual edits, extracting every section/chart/knum block by verified line ranges first (with assertions confirming each extraction matched expected content), then reassembling with new connective prose written fresh, then a final regex/string-based splice into the live file with an assertion that old and new text actually differed at each step (same discipline as the notebook-editing convention already used elsewhere in this project, adapted to HTML). Every chart's SVG markup and every knums/callout block was moved verbatim (never retyped), specifically to avoid corrupting numeric chart data during the reorganisation.
- **Verified:** jsdom load with `runScripts: 'dangerously'` confirms all 13 charts still populate with zero JS errors (chart-drawing JS in `<script>` was never touched, only the HTML markup above it moved), tag balance checked (div/section/p/nav/a/svg/g all net zero), nav `href`s match all 6 section `id`s exactly, grepped for stale references to the old section ids/anchors (`#diagnostic`, `#timeline`, `#scale`, `#bayesian`, `#challenges`, `#blackout`) and old section-number call-outs, none found, all "Section N" cross-references in the surviving prose checked by hand and confirmed to still point at the right thing under the new numbering.
- Not yet committed, sitting alongside the still-uncommitted `feat/per-channel-deviation` branch content (this was done in the same working tree, on the same branch).

## Status as of session 21 (2026-07-16, research.html polish + notebook 02 £ extension)

- **Confirmed `feat/per-channel-deviation` and the guide restructure are both merged** (Ryan: "the last thing I merged was guide-restructure"). Verified directly rather than trusting NOTES: `Blackout` present in `src/how_wrong_is_your_mmm/_phaser.py`, `docs/introduction.html` + `docs/research.html` both exist with the 6-section restructure (`problem`/`cause`/`fix`/`impact`/`how`/`questions` ids), 132 tests, README notebook table lists all 5 notebooks, all 5 notebooks fully executed (0 unexecuted code cells). Session 20's "Next slice" items 1–2 (close out the branch, reconcile Section 8) are therefore already done — the old numbered list below was stale and has been replaced.
- **Third-party review of `research.html` triaged, three of four points actioned:**
  - Cut two unsourced sentences ("practical inflection is around 0.6–0.7", "most real marketing datasets sit between 0.5 and 0.8") — checked against every session's notes, nothing backs these numbers, the one place the guide's own "everything traces to a committed notebook" rule was broken. Removed rather than sourced or softened (Ryan's call).
  - TL;DR now names `how_wrong_is_your_mmm` as a free, open-source package up front, linking to Section 5, instead of only surfacing that deep in the page.
  - Closing section length (reviewer's 4th point) and chart-rendering-in-a-real-browser (2nd point) were both explicitly left alone per Ryan's answers — not done this session.
- **New "From elasticity to £" subsection added to `research.html`** (end of Section 1, "Getting a personalised number"), Ryan's call after comparing against `introduction.html`'s revenue-range framing: reuses notebook 01's already-committed `diag_real` numbers (TV £664k–£2.36m, Meta £1.28m–£2.68m, Search £583k–£2.39m on the £12.3m plan split, correlation 0.65) via `planned_spend`, no new computation needed. Explicitly kept as revenue-range framing, not CAC/ROI (Ryan's choice between the two options) — links out to `introduction.html` for the full £-first version. Deliberately distinct from the still-open session 17 CAC/ROI item, which is a different (sales-as-customers) framing and remains undone.
- **Real evidence found that `introduction.html`'s intervals could be tighter, and faster:** on `introduction.html`'s exact scenario (£5.1m TV / £4.1m Meta / £3.1m Search, correlation ~0.68, notebook 02's `history`/`plan`), the already-committed `comparison_scenarios` cell shows All-Blackout at 47% max-CV reduction vs 16% for the default continuous range, at just **one year** of phasing — `introduction.html`'s current 63/68/75% headline needs **three years** of an aggressive ±80% continuous-range scenario to get there. Worth a proper investigation into whether Blackout reaches a comparable or better number in one year instead of three.
- **Notebook 02 extended** with a new closing subsection, "What does the reliability gain look like in £?": fits a fresh `CollinearityDiagnostic(planned_spend=...)` on three schedules (unphased baseline, continuous-range recommended, All-Blackout recommended) at the notebook's own full settings, reports revenue-range width per channel, plus a grouped bar chart. This is the traceable version of the finding above — makes it citable once run full-mode, rather than repeating the session-15/16 pattern of quoting a number that was never committed.
  - **Real bug found and fixed during the build, before it reached Ryan:** the new cells initially reused the module-level `sched` variable for "the continuous-range schedule", not realising the existing `comparison_scenarios` loop (cell 29, from session 19) reassigns `sched` on every iteration and leaves it pointing at whatever scenario ran last (All-Blackout) — so the first draft's "continuous range" and "All-Blackout" rows came out byte-identical. Caught by actually executing the notebook in the sandbox and reading the output, not just eyeballing the code. Fixed by referencing `phaser.recommended_schedule_` directly instead of the module-level name, with an explanatory comment left in the notebook so a future edit doesn't reintroduce it. Re-executed after the fix: the three scenarios now produce genuinely different, directionally sensible numbers (All-Blackout narrower than baseline on every channel, even under noisy FAST_MODE settings).
  - Edited via the standard `with open(...) as f: json.dump(...); f.write("\n")` pattern, verified via host-side Read (not bash) both before and after `ruff format`, per the session 18 unflushed-write lesson. No missing cell ids after the format pass (checked directly, per the session 20 lesson about `ruff format` silently skipping id normalisation when it doesn't reformat a cell's code).
  - `ruff format`/`ruff check` clean, all 132 tests still passing (no package code touched). Committed cells left unexecuted, same convention as every other notebook change in this project — **needs a real `FAST_MODE=False` local run before the £ numbers are citable or before `introduction.html` gets updated with a Blackout-based scenario.**
- Both `docs/research.html` and `notebooks/02_phaser_walkthrough.ipynb` changes are uncommitted, sitting in the working tree.

## Same day, continued: `introduction.html` switched to the Blackout scenario

Ryan ran notebook 02 full-mode locally and confirmed: "blackout based scenario as it better results", plus a request to bring the dark-week-marker chart style (shared as a screenshot of the notebook's matplotlib "SEARCH weekly spend, Blackout() mode" chart) into `introduction.html` too.

- **Pulled the real, now-executed numbers from notebook 02.** Baseline continuous range: 15.76% (≈16%) max-CV reduction. All-Blackout: 46.81% (≈47%) — at **one year**, not `introduction.html`'s previous three. Revenue-range widths (£12.3m plan, `planned_spend`): unphased TV £945k–£2.09m / Meta £1.42m–£2.60m / Search £712k–£1.89m; All-Blackout recommended TV £1.28m–£1.85m / Meta £1.79m–£2.27m / Search £1.04m–£1.52m — TV 51%, Meta 59%, Search 59% narrower. Confirmed Search is the channel driving `max_cv` both before and after phasing in this exact dataset (its own CV-reduction figure equals the overall `max_cv_reduction_pct` in every scenario row), so it's the legitimate one to headline, same logic the page already used for the old 75% figure.
- **`docs/introduction.html` Section 3 (Fix) and Section 4 (Impact) rewritten around the All-Blackout, one-year scenario**, replacing the old three-year ±80%-continuous-range story entirely:
  - New `BLACKOUT_RECOMMENDED` JS data (real `sched_all_blackout` weekly values, reproduced in the sandbox with `BudgetPhaser`'s default `seed=0` and the notebook's own full-mode settings, confirmed identical dark-week counts (12/channel) and max-spend ratios (1.26–1.44x) to the committed notebook), replacing the old `PLAN_YEAR1`/`RECOMMENDED` consts.
  - `drawFix` now plots the original plan year (`PLAN`, already used elsewhere on the page) against `BLACKOUT_RECOMMENDED`, with a solid dot marking every exactly-£0 week — the visual element from Ryan's screenshot, adapted to the page's small-multiples SVG convention rather than copied as a single matplotlib-style panel. Panel `yMax` retuned per channel to the new data's range.
  - `chartImpact`'s "after" values updated to the real All-Blackout revenue ranges; "Uncertainty reduction" knum changed 75% → 59%; all copy (section intros, fig-sub/fig-cap, legend, FAQ "how long until I see the benefit") rewritten to drop the three-year framing and describe the on/off mechanism and one-year timeframe honestly, including the trade-off (weeks that stay on carry ~30–45% more than usual).
  - Verified via jsdom: all 5 charts populate, 36 dark-week dots present (12 × 3 channels) all within chart bounds, no NaNs in any path/circle coordinate, tag balance clean, nav targets intact.
  - Rendered a standalone preview of the new Fix chart via the visualize tool so Ryan could eyeball the dark-week markers directly in chat before committing to the change.
- Not yet committed — `docs/introduction.html` joins `docs/research.html` and `notebooks/02_phaser_walkthrough.ipynb` in the uncommitted working tree.

## Same day, continued: closing-section tighten, README guide links, and an mkdocs API reference site

Three more requests from Ryan in the same session: tighten `research.html`'s closing section after all (deferred earlier in the session), fix the README's Guides section link text (both entries read "How Wrong Is Your MMM?", indistinguishable), consider moving Guides higher up, and — a bigger one — "shall we do some docs for the classes before we sort pip install".

- **`research.html` closing section tightened.** "Bringing it together" cut from an intro line + 4 paragraphs down to 3 sentences, keeping the same substance (variance not bias, phasing mechanism + geo-experiment analogy + channel-count robustness, Blackout as the stronger lever, Bayesian doesn't sidestep it) — the callout and footer line below were left as-is. Verified via jsdom, no regressions.
- **README Guides section fixed and moved.** Link text changed from duplicate "How Wrong Is Your MMM?" to "Introduction" and "Research". Section moved from between Notebooks and Development up to right after "The three-part solution", before Quick start — Ryan's reasoning (unstated but implied) holds up: a reader who isn't ready to install anything yet should hit the guides before the code snippet, not after.
- **Docs scope question: checked the actual state of the docstrings before proposing anything.** Read all four `src/` modules directly rather than assuming — `CollinearityDiagnostic`, `BudgetPhaser`, `Blackout`, `simulate_spend`/`simulate_sales`, `fit_ols` all already have thorough numpy-style docstrings (every param documented, design rationale included, not just "what" but "why" — e.g. `Blackout`'s docstring explains the at-least-one-week-on safeguard and the monthly-total-preservation trade-off in full). **There was no docstring-writing gap.** What's missing is a rendered reference someone can read without opening the source. Presented three real options (expand README, build a docs site, skip it) rather than assuming scope — **Ryan chose the docs site.**
- **mkdocs + mkdocstrings site built.** `mkdocs.yml` at repo root: `docs_dir: mkdocs_src` (new source folder, kept separate from `docs/` since that's already the GitHub Pages branch-folder root holding the committed `introduction.html`/`research.html`), `site_dir: docs/api` (so the built site lands at `docs/api/` and is served at `raz1470.github.io/how_wrong_is_your_mmm/api/` under the existing Pages setup, no new Pages configuration needed). Material theme, `mkdocstrings` python handler pointed at `src`, numpy docstring style.
  - Source pages: `mkdocs_src/index.md` (overview, links out to `introduction.html`/`research.html`/notebooks/GitHub — as absolute URLs, not relative paths, since mkdocs' strict-mode link checker doesn't know about sibling static files outside its own doc tree and flags relative links to them as broken even though they resolve correctly once deployed) and one page per source module: `api/diagnostic.md`, `api/phaser.md` (covers both `BudgetPhaser` and `Blackout`), `api/dgp.md`, `api/mmm.md`, each just a short intro + a `mkdocstrings` `:::` directive pulling the real docstring.
  - `pyproject.toml`: added `mkdocs`, `mkdocs-material`, `mkdocstrings[python]` to the `dev` dependency group.
  - **Real sandbox bug hit and worked around: the exact same class of mount-permission issue already documented for `.git/index.lock`, now showing up in `mkdocs build`.** First `--strict` build wrote files then aborted on two link warnings (before the absolute-URL fix above); the *second* build attempt failed outright with `PermissionError: Operation not permitted` trying to delete those just-written files during its "clean site directory" step — a separate process on this mount can't unlink files a prior process created, same root cause as the standing git-lock rule. **Workaround found and verified:** build into a throwaway sandbox path (`--site-dir /tmp/docs_api_build`) where clean/delete works normally, confirm it's correct, then `cp -rf /tmp/docs_api_build/. docs/api/` to lay the files over the mounted directory — `cp` truncates-in-place rather than unlinking, so it isn't blocked by the same restriction. `diff -rq` confirmed the copied tree is byte-identical to the clean build. **Worth adding as a standing rule alongside the git one:** never `mkdocs build` (or anything else that deletes-then-recreates a directory) directly against the mounted repo path from the sandbox after a prior build already wrote there — build to `/tmp` and `cp -rf` over instead.
  - Verified: `mkdocs build --strict` clean (zero warnings after the link fix), spot-checked rendered HTML for real docstring content (`planned_spend`, `max_dark_weeks_per_month` etc. all present) and confirmed no actual build errors (a few `grep` hits on "Error" were legitimate `RuntimeError` raises shown in the rendered source code via `show_source: true`, not failures). `ruff check`/`format --check` and the full test suite (132 passed) re-run clean afterward — no package code touched.
  - README: added a third Guides entry, "API Reference", linking to `docs/api/`. Development section gained `mkdocs serve` (local preview) and `mkdocs build --strict` (rebuild-before-release) instructions, with a note that this repo has no CI build step for Pages — the site has to be built and committed locally, same convention as the notebooks.
- All of this session's changes (`docs/research.html`, `notebooks/02_phaser_walkthrough.ipynb`, `docs/introduction.html`, `docs/api/` built site, `mkdocs.yml`, `mkdocs_src/`, `pyproject.toml`, `README.md`) are still uncommitted, sitting together in the working tree.

## Same day, continued: branch merged, launch sequence agreed

`feat/blackout-and-docs-site` reviewed and merged by Ryan locally. Two things caught during his own pre-commit check, worth remembering:

- **`uv run pytest` was interrupted (Ctrl+C) on the first attempt**, 128 of 132 completed — not a hang, just a manual interrupt. Re-run came back clean, all 132 passing. Worth a reminder for future sessions: always confirm a full, uninterrupted local run before treating "tests passed" as settled.
- **`uv run ruff format .` reformatted two notebooks unrelated to the session's work** (`01_dgp_diagnostic_walkthrough.ipynb`, `04_channel_count_sweep.ipynb`) — the same pre-existing formatting gap flagged and deliberately left alone since session 12. **Decision: excluded from this commit** (`git restore --staged` + `git checkout --`) to keep the PR focused, consistent with the project's standing preference. The debt itself is still there, unresolved — trivial to clear later as its own one-line commit whenever it's convenient, just not bundled into unrelated work.

**Launch sequence agreed:** finalise `research.html` (Ryan is still pondering further changes, unspecified) and `introduction.html` → publish to PyPI → share on LinkedIn. Confirmed PyPI packaging and the `research.html`/`introduction.html` content work are genuinely decoupled — PyPI publishes `src/`, the guide pages are independent static GitHub Pages content — so there's no technical reason to sequence one before the other. Ryan's chosen to do content first anyway.

**Gaps found while scoping "what's left before that sequence":**

- **No `LICENSE` file in the repo.** `pyproject.toml` declares `license = { text = "MIT" }` but there's no actual `LICENSE` file checked in — GitHub won't show a license badge without one, and it's usually the first thing checked before trusting a `pip install`. Confirmed via direct `ls`, not assumed.
- **`pyproject.toml` has no PyPI-listing metadata** — no `classifiers`, `keywords`, or `[project.urls]` (homepage/repository/docs links). Doesn't block publishing, but it's what makes the PyPI project page look like a real package rather than a bare listing, worth having before a LinkedIn-driven traffic spike hits it.
- **`introduction.html`'s Blackout chart (this session's change) still needs a real-browser check** — folded into the general research/introduction review pass rather than tracked separately, per Ryan's call. Don't treat it as done until that pass explicitly covers it.
- **PyPI publish checklist, not yet started:** decide on version for first release (0.1.0 is a reasonable default, Ryan's call), build, sanity-install the built wheel in a clean venv before publishing, need a PyPI account + API token, then flip the README's `# coming to PyPI` comment once live.
- **LinkedIn post not yet drafted.** Parked until the guide content is locked, per the agreed sequence above.

## Next slice

1. Ryan to finish pondering and land whatever changes he wants on `research.html` and `introduction.html` (including a real-browser check of the Blackout chart from this session) — this gates everything below.
2. Add the missing `LICENSE` file (MIT, matching `pyproject.toml`'s declared license).
3. Add PyPI-listing metadata to `pyproject.toml` (`classifiers`, `keywords`, `[project.urls]`).
4. PyPI publish: version decision, build, clean-venv sanity install, publish, update README's `# coming to PyPI` comment.
5. Draft and post the LinkedIn share.
6. Separately, whenever convenient and not bundled with unrelated work: clear the long-standing `ruff format` debt on notebooks 01 and 04.

## Workflow reminder

Pushes to `main` are rejected once branch protection is set up. Always work on a feature branch:

```bash
git switch -c feat/<slice-name>
# ...commits...
git push -u origin feat/<slice-name>
# Open PR, wait for CI, merge
git switch main && git pull && git branch -d feat/<slice-name>
```

## Status as of session 22 (2026-07-22, merge-status correction + PyPI-prep started)

- **Corrected a false alarm at the start of this session: `feat/blackout-and-docs-site` (session 20-21's Blackout/research-restructure/mkdocs work) IS merged to `main`, via PR #22, confirmed live.** Claude initially read the sandbox's local git clone (`git log --all`, `git branch --merged main`) and concluded the branch was still unmerged, contradicting NOTES.md's own session-21 log. Ryan corrected this with a GitHub screenshot of PR #22 merged into `main`. Root cause, confirmed directly: **the sandbox's git remote has no network access at all** — `git fetch --all` fails outright (`socat... Forbidden`, `Connection closed by remote host`), so the sandbox's local `main`/`origin/main` refs are permanently stale and were never actually re-synced with GitHub this session or, likely, in prior sessions either. The sandbox's git history was last accurate as of whenever the mounted clone was last pulled from Ryan's own terminal.
  - **Standing rule, extending the existing "no git commands from the sandbox" rule: never trust the sandbox's local git refs (`git log`, `git branch --merged`, etc.) for anything beyond the immediate uncommitted working-tree diff (`git status --short`, `git diff <file>`).** For "is X merged / is Y live" questions, verify against GitHub directly — WebFetch a raw file with a cache-busting query param (`raw.githubusercontent.com/.../<file>?cachebust=<anything>`, since the un-busted URL can serve a stale CDN cache) or the rendered GitHub page itself. This is a stronger rule than "don't run git commands" — it's "don't trust what's already there either."
  - Verified against GitHub directly (not the sandbox clone): `main` has the `introduction.html`/`research.html` rename, the Blackout section, `docs/api/` mkdocs site, 132-test README, all of it. Confirmed via cache-busted `raw.githubusercontent.com` fetches of `pyproject.toml` and `README.md`.
- **Plan reconfirmed with Ryan, in order:** (1) Ryan finishes his own review/final-changes pass on `research.html` and `introduction.html` — his call what changes, not yet specified, he's doing this pass himself rather than delegating it; (2) publish to PyPI; (3) **new step, added this session, not previously scoped anywhere in this project**: validate the package against real Monzo spend data before the LinkedIn post, not just synthetic/toy examples — this has been the aspirational "long-term validation story" since session 1's original notes but was never turned into an actual task. Needs its own scoping conversation next time it's picked up: what Monzo spend data Ryan can actually access and share, what the deliverable is (a new notebook? a section added to `research.html`/`introduction.html`? both?), and whether it blocks or just precedes the LinkedIn post; (4) LinkedIn post(s).
- **PyPI-prep work started this session (both items are genuinely decoupled from the research/introduction review and from the Monzo step, per session 21's own note that PyPI publishing and guide content are independent):**
  - **Added the missing `LICENSE` file** (MIT, copyright Ryan O'Sullivan 2026) — confirmed missing via a live 404 on `github.com/.../blob/main/LICENSE` before adding it, matches `pyproject.toml`'s already-declared `license = { text = "MIT" }`.
  - **Added PyPI-listing metadata to `pyproject.toml`:** `keywords` (marketing-mix-modeling, mmm, collinearity, econometrics, marketing-analytics, bayesian, media-planning), `classifiers` (Development Status :: 4 - Beta, MIT License, Python 3.12/3.13, Science/Research audience, Scientific/Engineering + Office/Business :: Financial topics), and `[project.urls]` (Homepage → `introduction.html`, Repository → GitHub, Documentation → `docs/api/`, Research → `research.html`).
  - **Also fixed a stale word found in passing:** `pyproject.toml`'s `description` field still said "budget perturbation" — the terminology was renamed to "phasing" project-wide back in session 4, this one field was missed. Changed to "budget phasing tools for Marketing Mix Models".
  - **Both changes were written directly to the files on Ryan's machine via the device bridge (stage → edit → commit-back), not committed to git** — per the project's standing rule, git commits happen from Ryan's local terminal, never the sandbox. `LICENSE` is a new untracked file; `pyproject.toml` has these changes sitting as an uncommitted working-tree diff, same as `NOTES.md` itself.
  - **Not yet verified:** these changes haven't been run through `ruff check`/`ruff format` or the test suite (neither `LICENSE` nor `pyproject.toml`'s metadata fields are code, low risk, but should still be confirmed clean before committing, same standard as every other change in this project). No PyPI build/publish attempted yet — that's step 2 of the plan above, and still needs a version decision (currently `0.1.0`, already set, Ryan's call whether that's the real first-release number), a clean-venv sanity install of the built wheel, a PyPI account + API token, and flipping README's `# coming to PyPI` comment once live.

## Next slice (updated session 22 — this supersedes the session-21 list above, which is now stale)

1. **Ryan:** commit `LICENSE` + `pyproject.toml` (new branch, e.g. `chore/pypi-metadata`) — run `ruff check`/`format`/`pytest` first, same standard as every other change.
2. **Ryan:** finish reviewing `research.html`/`introduction.html` and land whatever final changes he wants — no changes made by Claude yet, waiting on Ryan's specifics.
3. **Scope the real-Monzo-data validation step** (new, added this session) — a conversation, not a coding task yet: what data, what deliverable, where it lives.
4. **PyPI publish**, once 1 is committed: build, clean-venv sanity install, publish, flip README's `# coming to PyPI` comment.
5. Monzo validation itself (once scoped in item 3).
6. LinkedIn post(s), last — explicitly gated on the Monzo validation landing first, per Ryan's instruction this session.
7. Still parked, low priority, whenever convenient: the long-standing `ruff format` debt on notebooks 01 and 04 (flagged since session 12, deliberately excluded from every commit since to keep diffs focused).

## Status as of session 23 (2026-07-27, introduction.html copy + chart pass, in progress)

- **Corrected session 22's own "Next slice" item 1 at the start of this session: `chore/pypi-metadata` (LICENSE + PyPI classifiers/keywords/urls) is already merged to `main`.** Verified directly against GitHub (`raw.githubusercontent.com` with cache-busting, not the sandbox/device's local git refs, per the standing rule from session 22): `pyproject.toml` on `main` already has the classifiers/keywords/`[project.urls]` and the corrected "budget phasing" description, and `LICENSE` exists there too. **Not yet published to PyPI** — `README.md` on `main` still reads `pip install how-wrong-is-your-mmm  # coming to PyPI`, confirmed live. No Monzo content anywhere yet (that item is still unscoped, as expected).
  - Ryan's local `main` branch on his own machine is behind (still at the pre-merge tip, `54b862d`) — his local `chore/pypi-metadata` branch is actually current with the real `main` content, confirmed via `git diff` against the cached `origin/chore/pypi-metadata` ref showing zero difference. Worth Ryan doing a `git pull` on `main` next time he's at his own terminal; not blocking anything this session.
- **New environment note, worth remembering:** this session reached Ryan's actual machine via the device bridge (`mcp__remote-devices__device_bash`), not the old network-isolated cloud sandbox referenced throughout earlier sessions' "no git commands from the sandbox" rule. `device_bash` still has **no network** (confirmed: `git fetch origin` fails with `socat... Forbidden`), so GitHub-truth checks still have to go through `WebFetch` against `raw.githubusercontent.com`, same as session 22 established. But **the repo's actual dependencies (numpy, pandas) are present against the system `python3.10`** on this machine (the project's own `.venv` symlinks don't resolve inside the device-bridge mount, but `PYTHONPATH=src python3` works fine) — meaning real `how_wrong_is_your_mmm` computations (`BudgetPhaser`, `Blackout`, `CollinearityDiagnostic`) can be run directly from a session for the first time, not just read from already-committed notebook output. Used this repeatedly below; worth reusing in future sessions rather than assuming everything has to wait for Ryan's own local run.
- **Created branch `feat/introduction-copy-pass`** (off the current `chore/pypi-metadata` tip, since that has everything `main` has and Ryan's local `main` doesn't) before making any changes, per Ryan's explicit ask.
- **Went through Ryan's line-by-line critique of `introduction.html`** (his own review pass, item 2 from session 22's list) and implemented, after drafting and getting sign-off first:
  - Lede reworded to name the MMM acronym and open on "the marketing mix model (MMM) you use to allocate your marketing budget across channels," per Ryan's own draft.
  - "Take a real £12.3m annual plan" / "Look at an actual planned year of spend" / the Cause chart's "Real planned weekly spend" fig-sub all changed **real/actual → example**, for consistency (Ryan confirmed all three, not just the two he first flagged).
  - Both "Incremental revenue, 80% range..." fig-subs (Problem and Impact sections) changed to "...the honest range..." — the unexplained "80%" (p10/p90) jargon was Ryan's flag, "the honest range" reuses language the body copy already established.
  - "You need to stop spending the same amount every week" → "You need to break the correlation between channels." The following "clearest version of this is blunt" sentence rewritten for clarity.
  - **Real bug fixed, not just reworded:** the Fix chart's caption previously said each channel was "either spent at its full planned amount or switched off entirely" — Ryan caught that this is wrong, on-weeks are actually boosted *above* the planned amount to cover what the dark week would have spent, not left unchanged. Caption rewritten to state the mechanism correctly.
  - Impact section H2 changed from "The least reliable number gets more reliable" to **"What one year of pacing buys you"** (Ryan's pick from three options offered).
  - FAQ section H2 changed from "What people ask first" to **"Frequently asked questions"** (Ryan's pick between the two options offered).
- **Fix chart (section 3) rebuilt as a stacked 3-row layout** (TV/Meta/Search each their own full-width row, closer to the notebook's own `plt.subplots(3, 1, ...)` layout) instead of the old cramped 3-column small-multiples — Ryan's pick after seeing three mockup options, prompted by him flagging the old chart as visually weaker than a notebook matplotlib screenshot he shared.
- **New idea from Ryan mid-session, now built: the Fix chart illustrates mixing levers per channel, not every channel on the same setting.** TV set to a continuous ±40% range (never goes to £0), Meta and Search on `Blackout(max_dark_weeks_per_month=1)` — "just to illustrate this is an option too." Computed for real (`PYTHONPATH=src python3`, full-mode: `n_sims=50, grid_steps=25, n_phasing_seeds=10`, history seed=0/plan seed=1/correlation=0.7, same as the rest of the page): TV narrows 40.0% (1yr) / 34.4% (2yr), Meta 53.1%/53.2%, Search 56.8%/61.0%. **TV's 1yr number landed almost exactly on the "40% maybe" Ryan floated before the number was computed — coincidence of this seed, not tuned for it, flagged to him as such rather than presented as a designed-in figure.**
- **Impact section (section 4) deliberately kept on the separate, stronger all-Blackout ("ceiling") scenario, not the new mixed one** — Ryan's explicit call ("we can keep the impact section as is... it's just an illustration [in the Fix section] that you can make reductions as well as blackouts"), avoiding a mismatch between what the Fix chart shows and what the Impact numbers are computed from. Impact intro paragraph now says this explicitly ("The example above shows you can mix levers per channel. To see the ceiling, we ran the model with every channel switched to the harder on/off lever...").
- **Impact chart extended from 2 states (today/1yr) to 3 (today/1yr/2yr)**, after Ryan asked to see a mockup before committing to it. Real full-mode 2-year numbers, same recipe as the existing 1yr figures but with a 104-week plan (`simulate_spend(n_obs=104, ...)`) in place of 52: **TV 51%→56%, Meta 59%→71%, Search 59%→67%** narrower. The 1yr reproduction came out at 50.8%/59.1%/59.0% against this notebook's own already-published 51%/59%/59% — near-exact match, a strong cross-check that the recipe/seeds were reproduced correctly before trusting the new 2yr number. Headline kbox changed to quote **67%** (2yr) with 59% (1yr) noted underneath, per Ryan's "you can quote the 2 year improvement to sell it." FAQ "how long until I see the benefit" updated to cite both figures.
  - **Important, not yet resolved: the 2-year (104-week) figure is sandbox-computed, not backed by any committed notebook cell.** Unlike every other number on this page, there's no `notebooks/02_phaser_walkthrough.ipynb` cell that runs this specific 104-week version of the £12.3m scenario — notebook 03's Part D sweep uses a different, generic 3-channel toy setup (history seed=42, plan seed=99) for its own time-to-benefit numbers, not this page's actual TV/Meta/Search plan. **If this 2-year framing sticks after Ryan's local review, notebook 02 should get a companion cell for the 104-week run**, same traceability standard as everywhere else on the site (echoes the session 18-20 Blackout numbers' own path from sandbox-computed to notebook-committed).
- **Verification, two levels:** a headless jsdom check (all 4 charts populate, 0 NaN coordinates, 0 JS errors) plus, new for this project, an actual **real-Chromium render via Playwright** (`/opt/pw-browsers/chromium-1194`, launched directly since the project's own `.venv`'s pinned Playwright version isn't reachable from this environment) — screenshotted the full page and the two rebuilt charts close up, confirmed layout, colors, and dark-week dot placement (12 on Meta, 12 on Search, 0 on TV) all look right. **Note: this was not literally Ryan's own daily Chrome** — the `claude-in-chrome` browser extension reported "not connected" when tried, so the long-standing "needs a check in a real browser" item (flagged since session 20/21) is now closed via a genuine browser engine rather than jsdom, but not via the literal extension-based path; worth trying the extension again next session if Ryan wants that specific check too.
- **Written to Ryan's machine via the device bridge (stage → edit → commit-back), left uncommitted** — same standing rule as every other change in this project, commits happen from Ryan's own terminal. `git status --short` on his machine confirms only `docs/introduction.html` changed, on branch `feat/introduction-copy-pass`.
- **Not done this session, explicitly deferred:** `research.html` itself untouched — Ryan chose to start with `introduction.html` first. **Ryan has a couple more changes in mind for `introduction.html` he'll share next session** — this pass is not finished, don't treat `introduction.html` as closed out yet.

## Status as of session 24 (2026-07-28, introduction.html copy pass finished + new algorithm section + nav bug fix)

- **Reached Ryan's machine via the device bridge** (`mcp__remote-devices__device_*`, not the old network-isolated cloud sandbox) — same access pattern session 23 established, no network here either (GitHub-truth checks still have to go through cache-busted `raw.githubusercontent.com` fetches if ever needed, not attempted this session). All edits below were staged, changed, and committed back to `docs/introduction.html` directly on Ryan's disk, one file-round-trip per change, verified (tag-balance check + a real Chromium render via Playwright, `/opt/pw-browsers/chromium-1194`) before every write-back. Still uncommitted to git, still sitting on `feat/introduction-copy-pass` (the branch session 23 was working on) — same standing rule as always, commits happen from Ryan's own terminal.
- **Finished the `introduction.html` copy pass session 23 left open**, working through Ryan's line-by-line notes one at a time:
  - Section 3 (Fix) H2: "Pace the same budget differently" → **"Same budget, a smarter phasing algorithm"**.
  - Section 3 body paragraph reworked: dropped the "two levers, same idea" framing and the "here's what that looks like for this plan" lead-in (now redundant with the chart's own fig-sub), named the mechanism explicitly ("break the correlation between channels using a phasing algorithm"), and added a closing sentence explaining *why* it works ("gives the model weeks where each channel finally moves on its own, so it can tell which one is actually driving your sales").
  - Section 5 (Impact, was 4 before today's renumbering — see below) H2: "What one year of pacing buys you" → "What pacing buys you" → **"What the phasing algorithm buys you"** (two rounds, Ryan's own follow-up).
  - Impact fig-cap: dropped the trailing "Search stays the channel the model is least sure about..." sentence per Ryan's request.
  - Impact intro paragraph rewritten to state plainly that this is a simulation exercise: pace the budget, refit, check how much tighter the confidence gets, rather than the vaguer "ran the model... retrained on the result" phrasing.
  - Header tagline replaced entirely, several rounds: started at "Your model gives you one number, but the honest answer is a (very wide) range" (unchanged since session 18), through a few Claude-drafted options built around the real 67% two-year figure, to Ryan's own line, landing on **"How using a budget phasing algorithm can dramatically tighten the confidence in your MMM results."** (fixed a typo in his draft, "of" → "in", and added "results" per his own final follow-up).
  - Footer package description: "...built on real simulation, not a rule of thumb" → **"...built on real simulation using your own spend data"**, per Ryan's edit.
- **Real bug found and fixed: the sticky section nav's "active" highlight never tracked scroll position.** Ryan noticed after asking for the nav to be made sticky (see below) that clicking a link jumped to the right section but the highlight stayed stuck on whichever link loaded first. Root cause, confirmed by reading the file: the nav's `active` class was hardcoded onto the first link in the markup and there was never any JS driving it, harmless while the nav scrolled out of view with the rest of the page, but an obvious bug once pinned to the top. **Fixed with an `IntersectionObserver`-based scrollspy** (new IIFE at the end of the `<script>` block): tracks whichever `<section id>` sits in the top third of the viewport, offset for the sticky nav's own height via `rootMargin`, and toggles `.active` accordingly. Verified in a real Chromium instance two ways: clicking a nav link updates the highlight immediately, and `scrollIntoView` on each of the seven sections in turn lights up the correct tab every time, zero JS errors.
  - **Prompted by a separate ask earlier in the session: made the section nav itself sticky** (`position: sticky; top: 0` on `.snav`, plus a white background and z-index so content doesn't show through while scrolling underneath it) — Ryan noticed it disappeared once you scrolled past the header and asked whether it should move to a side rail instead. Recommended against a sidebar: the page is a deliberately narrow, centred single column (700px) meant to read identically on mobile and desktop, a sidebar only has room on wide desktop viewports, and this page's whole audience arrives from LinkedIn, mostly on phones. Sticky-top was the uniform, low-complexity fix; verified via a real Chromium screenshot after scrolling to confirm the nav actually holds in place.
  - **`docs/research.html` has the identical non-sticky, non-scroll-tracking nav CSS/markup** (confirmed by checking, not assumed) — same bug, same fix needed, not yet applied. Flagged to Ryan at the time; carry over to whenever the `research.html` pass happens.
- **New standalone Section 4, "The algorithm," added** — Ryan felt the mechanism wasn't actually explained anywhere on the page and asked for a way to show it, ideally with a simple equation. First pass was a small callout box inline in Section 3 (a two-line formula: `raw week = spend × (1 + nudge) → or £0 if dark`, then `phased week = raw week × (original month total ÷ raw month total)`) — Ryan's read: needs its own section, and the two-step formula alone "isn't that clear." Asked Ryan where it should live (new section right after The Fix / folded into the existing "How it works" pipeline section / new section right after Impact) — **he chose a new section right after The Fix.** Rebuilt as a full section with a **concrete worked numeric example** instead of symbols-only: a channel planning £100k/week for four weeks (£400k for the month), the nudge landing on £100k/£120k/£0/£100k (raw total £320k), then the rescale step (×1.25) bringing it back to £125k/£150k/£0/£125k (exactly £400k again) — chosen so the arithmetic is clean and round. Explains in the same walkthrough why "on" weeks in the Fix chart often run hotter than their original plan (they're covering for the dark week, not just moving). The compact equation is still included underneath, as a reference for anyone who wants the literal formula, not as the primary explanation.
  - **This required renumbering every section from Section 4 onward**: Impact 4→5, How it works 5→6, Questions (FAQ) 6→7, both in the `s-label` text and the nav bar (`<nav class="snav">`). New nav link reads "4 · The algorithm", distinct from "6 · How it works" (the existing Diagnose→Phase→Retrain pipeline section) to avoid two identically-worded nav items. Checked first that no prose anywhere in the page cross-references section numbers (none found), so the renumbering was safe. Re-verified section ids exactly match nav `href`s (`problem/cause/fix/algorithm/impact/how/faq`, 7 of each) and re-ran the full scrollspy check across all seven sections post-renumbering — all correct.
- **Two new FAQs added, initially appended at the end, then reordered per Ryan's follow-up** (both answers already landed within his 4-sentences-max ask on the first draft, no trimming needed):
  - **"Does it scale to all of my channels?"** — sourced from `research.html`'s channel-count sweep (notebook 04): reliability stays essentially flat from 5–20 channels, phasing's benefit holds roughly steady across the same range.
  - **"Doesn't a Bayesian model already fix this?"** — sourced from `research.html`'s Bayesian comparison (notebook 05): priors help with functional form (adstock/saturation) but can't manufacture identifying variation, real paired test found only 5–9% narrower intervals.
  - Final order: geo-lift → scale → Bayesian → how long → what to hand the agency (moved up from the end, after "why not geo-lift", per Ryan's request).
- **A fifth FAQ added, this one Claude-initiated**, after Ryan asked whether the page needed to mention that the phasing impact numbers rely on *supplied* channel elasticities. Confirmed the gap was real: `introduction.html` shows real-looking £ ranges without ever saying where the underlying "true" elasticity numbers come from, while `research.html` already has this exact explanation (the line committed back in an earlier session: "you don't know the true elasticities, which is the whole reason you ran the MMM... assigning plausible elasticities, simulating what sales would look like if those were true, then measuring how consistently OLS recovers them from your actual spend data"). Added as the **new first FAQ**, "Doesn't this need me to already know my elasticity?", ahead of the geo-lift question, on the reasoning that it's the most fundamental "wait, but—" a skeptical reader would raise before any of the others matter.
- **Verification standard held throughout:** every single change was checked for HTML tag balance (div/section/p/nav/a regex counts) before writing back, and every structural or visual change (sticky nav, scrollspy, the new algorithm section, the new FAQ) was rendered in a real Chromium instance via Playwright and screenshotted for Ryan to see before or immediately after committing to it, not just described in prose.
- **Ryan is calling it a day, planning one more review pass tomorrow** before treating `introduction.html` as finished. Nothing else attempted this session.

## Status as of session 25 (2026-07-29, introduction.html finished + notebook 02 companion cell + notebook renames + README overhaul)

- **Reached Ryan's machine via the device bridge** (`mcp__remote-devices__device_*`), same access pattern as sessions 23-24. Files were staged into the session workspace, edited/verified there, then written back via `SendUserFile` + `device_commit_files` one file at a time. Everything below is still uncommitted on Ryan's machine — commits/branching are his to do from his own terminal, per the standing rule.
- **Finished session 24's "one more review pass" on `introduction.html` (item 1 of the session-24 list):**
  - FAQ reordered: "Does it scale to all of my channels?" moved to after "Doesn't a Bayesian model already fix this?" (Ryan's call — Bayesian now comes before scale).
  - Section 4 ("The algorithm") rewritten twice more, per Ryan's live feedback:
    - Dropped "with real numbers" from the H2 (now just "What the algorithm actually does").
    - Ryan's key note: the section read as underselling the mechanism ("we want people to think this is clever and want to pay for it") and the single-line equation was incomplete (only showed the rescale step, not the nudge step). Rewrote the equation as two proper steps with subscript notation: `raw_week = plan_week × nudge_week` then `phased_week = raw_week × (Σplan_month ÷ Σraw_month)`, replacing the old one-liner.
    - Rewrote the closing paragraph to frame the actual "clever" part as the search over nudge size (refitting against thousands of simulated histories per setting to find whichever narrows the least reliable channel most), rather than presenting the nudge-and-rescale arithmetic itself as the whole story. Ryan then trimmed this paragraph further, cutting the "on weeks run hotter" aside and the closing "not a fixed rule applied blindly" sentence, leaving just: the arithmetic is the easy part, the harder part is the search.
    - Swapped "barely-there" for "small" per Ryan's wording preference (in "searches the nudge size, from small to the maximum you're willing to tolerate").
  - Verified every round via a real Chromium instance (Playwright, `/opt/pw-browsers/chromium-1194`) — screenshotted the FAQ and algorithm sections, confirmed tag balance (div/section/p/nav/a counts) and that all 5 charts still populate with zero JS errors each time.
- **Item 2 of the session-24 list complete: 2-year Impact figure now backed by a committed notebook cell.** Added to `notebooks/02_phaser_walkthrough.ipynb`, right after the existing £ analysis:
  - Fixed a stale markdown note (the old closing cell, now cell 35) that still referenced the abandoned "three-year, ±80% continuous-range" scenario — `introduction.html` moved to the All-Blackout framing back in session 21, this note was never updated to match.
  - New markdown + code + markdown block: reruns the exact 1-year All-Blackout recipe (`history_df=history`, correlation 0.7, `true_elasticities`, `planned_spend`, same `n_sims`/`grid_steps`/`n_phasing_seeds`, all-channel `Blackout(max_dark_weeks_per_month=1)`) against a 104-week plan instead of 52.
  - **Computed for real** directly against `src` via the device bridge (`n_sims=50, grid_steps=25, n_phasing_seeds=10, fast_mode=False`, full settings, not a FAST_MODE spot-check): TV narrows 55.7%, Meta 70.8%, Search 66.8% — confirms `introduction.html`'s existing ~56%/71%/67% headline almost exactly. Cross-checked the method against the already-committed 1-year `revenue_df` cell: recomputed 50.8%/59.1%/59.0%, matching the notebook's own committed 51%/59%/59% closely, a good sanity check the recipe reproduces correctly.
  - `ruff format`/`ruff check` clean on the notebook (one auto-format pass wrapped a long f-string print line). Notebook validated with `nbformat` and smoke-tested end-to-end in FAST_MODE via `nbclient` with zero errors (needed to stage `src/how_wrong_is_your_mmm/*.py` into the session workspace to get this running, since only individual files had been staged up to that point).
  - **New cell left unexecuted in the committed file**, same standing convention as every other notebook addition in this project — needs a real `FAST_MODE=False` local run before its output is worth treating as final, even though the numbers above were already verified directly against `src`.
- **Notebook files renamed, per Ryan's request:**
  - `notebooks/01_dgp_diagnostic_walkthrough.ipynb` → `notebooks/01_diagnostic_walkthrough.ipynb` (dropped "dgp").
  - `notebooks/04_channel_count_sweep.ipynb` → `notebooks/04_channel_scaling_walkthrough.ipynb`.
  - Done as plain `mv` on the device (not a git operation, so no conflict with the "no git from the sandbox" rule — though this means `git status` will likely show these as delete+add rather than a detected rename until staged; `git add` should still resolve them as renames since content is untouched).
  - Checked `src/`, `tests/`, `docs/`, `mkdocs_src/`, `pyproject.toml`, `mkdocs.yml` for other references to the old filenames — found none. Only `README.md`'s notebook table referenced them (fixed, see below); `NOTES.md`'s own historical entries still use the old names, left as-is since those are a log of what was true in past sessions, not live references.
- **`README.md` overhaul, several rounds of Ryan's feedback:**
  - Opening rewritten to match `introduction.html`'s tone: same tagline ("How using a budget phasing algorithm can dramatically tighten the confidence in your MMM results") and the same rhetorical-question lede style ("Would it give you the same answer? For most brands, no.").
  - **"Key result" section (the 80% range table) dropped entirely**, per Ryan's call.
  - Notebook table's "What it shows" column rewritten in plain language for all five rows (Ryan: too jargon-heavy before — dropped phrasing like "CV curve", "correlation sweep 0.1→0.9", "condition number").
  - Guides section descriptor lines for Introduction and Research rewritten — Ryan's read was that the originals ("A two-minute read: the headline numbers and the fix, no maths" / "The full method, the research behind it, and how to run it yourself") sounded like generic AI-written marketing copy. Rewritten to state content plainly; the Introduction line went through a second round to Ryan's own wording, "An introduction to the problem and the fix." API Reference line left untouched (not flagged).
  - **"The three-part solution" tightened and updated** — Ryan's read: too long, and stale (didn't mention `Blackout` at all, only the continuous-range lever). Part 1 and Part 3 trimmed. Part 2 rewritten to cover both levers: the continuous nudge (package default ±40%, ~30% CV reduction over a year) and `Blackout` (the harder on/off switch, capped at one dark week a month, 47–59% CV reduction) — the 47–59% figure sourced from `research.html`'s already-published, three-independent-draws all-channel Blackout study, not a new number.
- **Not yet committed.** Today's changes span a few different concerns — finishing `introduction.html`, the notebook 02 companion cell, the notebook renames, and the README overhaul — flagged to Ryan as possibly worth splitting into separate commits/branches rather than one, but that decision and the actual commit/merge is his, from his own terminal.

## Status as of session 26 (2026-07-29, notebook pruning + ruff format debt cleared + branch recovery + runner/report idea scoped)

- Reached Ryan's machine via the device bridge, same pattern as sessions 23–25.
- **Notebook pruning done — session 25's next-slice item 2, scoped down to notebooks 02, 03, 04 per Ryan** ("i think we do the pruning next. i think you right its 2/3/4. if we need it fine, just worried some wasn't needed"):
  - **02:** deleted 2 redundant cells — old cell 18 (a one-line CAC/ROI recap duplicating output already shown earlier) and old cell 22 ("## What this achieves" markdown, redundant with cells 0/7). 39 cells → 37. Nothing cited in `introduction.html`/`research.html` was touched.
  - **03:** fixed a real structural bug — the "Part C — What does optimal phasing actually achieve" block (3 cells) sat *after* "What does optimal Blackout phasing achieve", even though earlier cells already referenced "Part C" by name before it existed in reading order. Moved the block to directly after Part B. Also rewrote the Section 3 intro cell to preview all four parts (A/B/C/D) instead of just A/B. Verified via a full `nbclient` FAST_MODE execution, zero errors.
  - **01 & 04:** cleared the long-standing `ruff format` debt flagged in earlier sessions — missing trailing newlines on a few cells in 01, one long list-comprehension needing a wrap in 04. Verified `ruff format --check`/`ruff check` clean afterward (staged `pyproject.toml` alongside so the notebook-specific per-file-ignores applied).
  - **New finding, not yet actioned:** notebooks 01 and 04 both have duplicate Jupyter cell IDs (several cells literally sharing id `"code_0"` or `"md_0"`). Confirmed pre-existing, not introduced this session — 02/03 (already through proper `ruff format` passes) show none. Flagged to Ryan; no decision yet on whether/when to fix.
  - All of this landed on a fresh `chore/notebook-cleanup` branch. **Not yet committed/merged** — blocked on Ryan finishing a full Restart & Run All of notebook 02 (see next slice below).
- **Branch mixup while merging session 25's work, caught and recovered with no lost work:** a pasted multi-command block caused `git switch main` to fail silently (uncommitted local changes at the time), so the follow-on `git switch -c chore/session-25-updates` actually cut from `feat/introduction-copy-pass`'s tip instead of fresh `main`. Fixed via `git stash -u` (the `-u` mattered — it's what caught the untracked renamed notebook files) → `git switch main` → `git pull` → `git branch -D chore/session-25-updates` (safe, no unique commits yet) → `git switch -c chore/session-25-updates` → `git stash pop`. Also surfaced in passing that `feat/introduction-copy-pass` itself (sessions 23–24's branch) still hadn't been merged — Ryan confirmed and is handling that separately.
- **New idea raised by Ryan before merging `chore/notebook-cleanup`, scoped but not started — logging per his request:**
  - **A new generic, committed runner-template notebook** (e.g. `notebooks/06_run_your_own_data.ipynb`) — blank config cells for channels/elasticities/spend file path, built on the existing `spend_df=`/`history_df=`/`plan_df=` API surface, no new package code required. Purpose: a clean starting point for any practitioner to point at their own real spend data. Ryan's own first use will be against real Monzo spend data, done locally/privately — **the committed version stays generic, no Monzo specifics baked in** (this was the explicit decision from the scoping question).
  - **A new public package class** — working name `Runner` or `ReportBuilder`, not fixed yet — that takes spend data + config, runs the full Diagnose → Phase pipeline end to end, and outputs a polished PDF report summarising the simulations and results. **Scoped as a genuine v-next feature**, exported from the package alongside `CollinearityDiagnostic`/`BudgetPhaser`/`Blackout`, not just a notebook helper (this was the explicit decision from the scoping question).
  - Relationship to the existing "Scope the real-Monzo-data validation step" backlog item: the runner notebook is the vehicle for that validation once built; the PDF-report class is a separate, larger feature the runner notebook could eventually call, not a prerequisite for it — notebook 06 can ship first with plain printed output and pick up PDF export later once the class exists.
  - **Still to pin down in a future session:** the PDF report's exact contents/sections, the class's final name and API shape, and the build order between notebook 06 and the report class.

## Status as of session 27 (2026-08-09, introduction.html FAQ additions + Impact section averaging fix)

- **Reached Ryan's machine via the device bridge**, same pattern as sessions 23–26. Files staged, edited/verified in the session workspace, written back via `SendUserFile` + `device_commit_files`. Confirmed merged this session (verified directly against GitHub via cache-busted `raw.githubusercontent.com`, not local git refs, per the standing session-22 rule): `docs/introduction.html` and `notebooks/02_phaser_walkthrough.ipynb` both live on `main`, and the notebook's new cells carry Ryan's own real `FAST_MODE=False` output, which matches this session's device-computed numbers almost exactly (e.g. 1yr Search 54.29% vs 54.3% computed here) — a clean cross-check, no drift to correct.
- **Two new FAQs added to `docs/introduction.html`**, both drafted, revised through a few rounds of Ryan's feedback, then implemented:
  - **"Won't this put my channels into learning mode?"** Ryan's framing: this is a trade-off between how much reliability you want and platform learning-phase disruption, decided jointly by data science and marketing, not something the model can settle alone. Deliberately avoids naming `Blackout` as code (this page is CMO-facing) and instead describes the flexibility spectrum in plain language: 0% (untouched) through a gentle nudge up to 80%, or a full on/off switch.
  - **"Isn't 12 months a long time to wait for this to pay off?"** Answers with a real 3-month (13-week) figure on introduction.html's own scenario, not a generic number, plus a positioning point about the common MMM-vendor-switching cycle (two years in, no value, switch, repeat, never compound). Placed both new FAQs between the existing "How long until I see the benefit?" and "What do I actually hand my media agency?" questions.
- **Real bug found and fixed while sourcing the 3-month figure: the Impact section's £ numbers were computed from a single `recommended_schedule_` draw, not averaged, unlike the CV metric they sat next to (which `BudgetPhaser` already averages internally across `n_phasing_seeds`, per the session 6 smoothing fix).** This was quietly overstating how unevenly channels benefited: the old 1-year figures (51%/59%/59% TV/Meta/Search) and 2-year figures (56%/71%/67%) came from one lucky/unlucky draw each. Recomputed properly, averaged across the same 10 phasing-seed draws the CV grid search uses, directly against `src` on Ryan's machine at full settings (`n_sims=50`, `grid_steps=25`, `n_phasing_seeds=10`, `fast_mode=False`), cross-checked by exactly reproducing the already-committed 1-year figure with the old single-draw method first (confirmed the recipe was right) before running the new averaged version:
  - **3 months (new data point):** TV 32%, Meta 37%, Search 39% narrower.
  - **1 year (corrected):** TV 54%, Meta 54%, Search 54% narrower — was 51%/59%/59%. Channels now converge tightly, which makes more sense than the old uneven split.
  - **2 years (corrected):** TV 58%, Meta 62%, Search 61% narrower — was 56%/71%/67%. The old "71%" headline was real drift, not noise, and is gone.
  - Search still drives `max_cv` at every horizon (its own CV reduction always equals the overall `max_cv_reduction_pct`), so it's still the right channel to headline.
- **`docs/introduction.html` updated throughout to match:** Impact chart data (today/1yr/2yr bounds per channel), fig-cap, the "Uncertainty reduction" kbox (67% → 61%), the "How long until I see the benefit?" FAQ (59%/67% → 54%/61%), and a new sentence in the Impact section's intro paragraph disclosing the averaging (plain language, no jargon: "averaged across many equally-valid ways the schedule could land, not just one"). JS comment above the chart's data array rewritten to explain why the methodology changed.
- **Structural question from Ryan, answered rather than just actioned: should there be a dedicated notebook for introduction.html's own numbers, separate from the topic-based notebooks?** Recommended no. Notebook 02 already *is* that notebook, built around the exact £12.3m TV/Meta/Search scenario the page uses throughout; the actual problem was that its £-analysis cells had accumulated in three disconnected places across sessions (original 1yr cell, session-25's 2yr companion cell, now this 3-month one) using inconsistent methodology between additions. The generic findings backing the FAQ's broader claims (channel-count scaling from notebook 04, Bayesian comparison from notebook 05, correlation sensitivity from notebook 03) correctly live in their own topic notebooks since they aren't about this specific scenario. Fix was consolidation, not a new notebook.
- **`notebooks/02_phaser_walkthrough.ipynb` restructured accordingly:** replaced the old scattered single-draw £ cells (old cells 30–36: one markdown intro, the 1yr revenue cell + chart, a markdown cross-reference, the 2yr companion markdown + code + confirmation markdown) with one consolidated section, "What does the reliability gain look like in £, and how fast does it show up?" — a `blackout_impact(n_plan)` helper that generates `n_phasing_seeds` independent phased schedules at the recommended alpha and averages their revenue-range bounds, called for 13/52/104-week plans, a results table (`impact_df`), a grouped bar chart, and a findings markdown cell that explicitly states this supersedes the old figures and why. Notebook shrank from 37 to 34 cells (net effect of replacing 7 old cells with 4 new ones).
  - Built and verified in the cloud session workspace this time (not the device), since it needed `ruff`/`nbformat`/`nbclient` which aren't installed on Ryan's machine's system Python: `ruff format`/`ruff check` clean, `nbformat.validate` passes, executed end-to-end via `nbclient` in FAST_MODE with zero errors (FAST_MODE's own numbers are noise as always, not for citation, per every prior session's caveat). New cells assigned proper `uuid4().hex[:8]` ids directly in the build script rather than relying on `ruff format` to add them, per the session-20 lesson about `ruff format` silently skipping id normalisation when it doesn't reformat a cell's code. Committed cells left unexecuted per the standing convention, same as always.
  - **Ryan ran it full-mode locally and merged the same session** (branch `feat/introduction-faq-and-robust-impact`) — confirmed live on `main` via GitHub, numbers match this session's device-computed figures almost exactly.
- No package code touched this session (`src/`, `tests/` untouched), so the 132-test count is unaffected.

## Next slice (updated session 27 — supersedes the session-26 list above)

1. Decide whether to fix the duplicate Jupyter cell IDs found in notebooks 01 and 04 (flagged session 26, metadata-only, not a content bug, but 02/03 are already clean).
2. **Build the new generic runner-template notebook** (`notebooks/06_run_your_own_data.ipynb` or similar) — blank, committed, uses the existing `spend_df=`/`history_df=`/`plan_df=` API, for any practitioner (including Ryan's own Monzo validation) to run against real spend data.
3. **Design and build the new `Runner`/report-generating package class** — takes spend + config, runs Diagnose → Phase end to end, outputs a PDF report. Bigger scope than item 2; needs its own design pass (contents, name, API) before building.
4. **`research.html`** — the equivalent review/finalisation pass, still not started. Still needs the sticky-nav + scrollspy fix carried over from session 24 (currently has the identical bug `introduction.html` had before that session's fix). Worth checking whether `research.html`'s own £-translation subsection ("From elasticity to £", session 21) has the same single-draw-vs-averaged issue just fixed on `introduction.html` — not checked this session.
5. PyPI publish, once 1–4 above are settled: build, clean-venv sanity install, publish, flip `README.md`'s `# coming to PyPI` comment. Version `0.1.0` already set in `pyproject.toml`.
6. Monzo validation itself, using notebook 06 once built (item 2).
7. LinkedIn post(s), last.

## Status as of session 28 (2026-08-10, ReportBuilder design pass — HTML mockups, no package code yet)

- **Reached Ryan's machine via the device bridge**, same pattern as sessions 23–27. Chat-only session (no persistent-session file round-trips needed until the very end) — picked up session 27's next-slice item 3, **"Design and build the new `Runner`/report-generating package class,"** and spent the whole session on design, per Ryan's steer once he saw the first sketch: *"i wonder whether html output would work better/look better? can you sketch out what it will look like with fake data?"* Two rounds of mockup, iterated live off Ryan's reactions, before any package code.
- **Format decided: HTML, not PDF.** Built as a single self-contained document with an `@media print` block, so print-to-PDF via the browser's own print dialog is the intended delivery path — Ryan to confirm this actually looks good enough in a real print preview before ruling out a dedicated PDF-rendering pipeline (`weasyprint` etc.) as unnecessary. Not yet tested by Ryan at time of writing.
- **Class name: leaning `ReportBuilder` over `Runner`** — matches the existing noun-based public API naming (`CollinearityDiagnostic`, `BudgetPhaser`, `Blackout`). Not yet locked, but no objection raised.
- **Positioning confirmed by Ryan: this is the client deliverable**, not just an internal Monzo-validation tool — Monzo validation is now a secondary use of the same output, not the primary driver of its design.
- **Two-page report structure locked**, built against a fake client ("Northwind Outdoors," FY2027, £8.4m across four channels — TV/Meta/Search/OOH, OOH added specifically to pressure-test the layout beyond the site's usual three-channel examples):
  - **Page 1 (client-facing headline):** cover block (client name, plan year, generated date, package version) → headline callout stat → **Diagnose** (new: opens with a channel-correlation matrix — sequential grey→ink heatmap, magnitude not identity, explaining *why* reliability is low, added mid-session per Ryan's ask — then the existing honest-range forest plot + a data table) → **Phase** (recommended weekly schedule chart, original vs recommended per channel, plus a lever/amplitude table) → **Retrain/Impact** (before/after forest chart, kbox summary, and a new caveat box — see below) → Methodology appendix.
  - **Page 2, "Scenario exploration"** (explicitly framed as supplementary DS/marketing background, not headline numbers, with its own mini page-break header): **Section 4, Channel sensitivity** (CV reduction vs. phasing amplitude, 0% → 100% continuous → `Blackout`, one curve per channel — **this is what actually picks each channel's recommended lever**, per Ryan's confirmed intent) leads; **Section 5, Time to benefit** (CV reduction over time — today/3mo/6mo/1yr/2yr — split by intensity: `Blackout`/±80%/±40%/±20%, for the headline least-reliable channel) follows it. Deliberately in that order, not the order first drafted — Ryan's read: time-to-benefit "doesn't help us make the decision," channel sensitivity does, so decision-driving content leads and context follows.
- **Real open question surfaced, not resolved: how to compute the channel-sensitivity curve for real.** Today's `BudgetPhaser` (locked session 4) runs one shared grid search over amplitude α applied to every channel at once, and picks whichever α minimises the *worst* channel's CV — it does not produce independent per-channel curves, and the mixed-lever illustration on `introduction.html` (TV continuous, Meta/Search `Blackout`) was explicitly flagged in session 24 as "just an illustration," never a real recommendation path. This session's mockup treats per-channel lever selection as if it already existed as a feature, which isn't true yet. Two build options scoped, neither chosen:
  - **(a) Marginal sensitivity** — vary one channel's amplitude at a time, holding the others fixed at a baseline (0, or their own recommended setting), rerun the diagnostic per channel. Cheap, a small addition to the existing grid-search loop, honestly labelled as "if you only touch this channel" — but doesn't capture interaction effects from multiple channels being phased at once.
  - **(b) True joint per-channel optimisation** — coordinate-descent over every channel's amplitude together. More correct, a real `BudgetPhaser` rearchitecture, bigger scope.
  - Leaned toward (a) as the pragmatic first version in conversation with Ryan; **not yet confirmed by him.**
- **Second open question, also not resolved: does page 1's Diagnose section need its own elasticity caveat?** Only the Impact section got one this session (see below) — the same "these numbers depend on your supplied elasticity" dependency applies to Diagnose's £ ranges too, since they're built the same way. Flagged to Ryan explicitly; no decision yet.
- **New caveat box added to the Impact section**, prompted by Ryan asking whether it needed "a small box with red exclamation marks saying THIS IS BASED ON SUPPLIED COEFFICIENTS." Built instead as a muted amber/warning callout (single ⚠ glyph, amber-50 background, amber-800 text) rather than red alarm styling — reasoning given to Ryan and accepted: a client report flagging what looks like an error state next to numbers it wants trusted undermines its own credibility. Copy makes the precise distinction that matters: the reliability gain (the CV reduction itself) doesn't depend on the supplied elasticity being right, only the specific £ amounts do — same substance as the existing `introduction.html` FAQ ("you supply a plausible one, not a proven one"), reused here in report form.
- **Data honesty in the mockup itself:** the weekly spend and phasing charts aren't static images — they're generated client-side from a seeded PRNG demand-driven DGP (deliberately mirroring the package's own DGP mechanism: a shared latent signal correlates the channels, each channel adds its own noise) and then run through the real nudge+rescale / `Blackout`+rescale arithmetic in JS. So the chart *mechanics* are honest even though the CV/revenue *numbers* are hand-picked illustrative placeholders — every such block is commented in the mockup's own script as illustrative, to be replaced wholesale by real `CollinearityDiagnostic`/`BudgetPhaser` output once `ReportBuilder` exists.
- **Verification held to the usual standard:** every round re-rendered headlessly via Playwright, checked for zero JS errors and zero NaN SVG coordinates, screenshotted section-by-section. Caught and fixed two real layout bugs this way: end-of-line labels clipping off the right edge of the two new page-2 charts, and two labels ("TV · 35%," "Meta · 36%") overlapping when their values landed close together — fixed with shorter label text plus a vertical collision-avoidance pass on the label positions (sort by y, enforce a minimum 13px gap), not just a one-off nudge.
- **Environment note for next time:** the Playwright Chromium executable path needed `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` this session — `/opt/pw-browsers/chromium-1194` alone (the bare versioned directory, which worked in earlier sessions' phrasing) gave `EACCES` here. Worth trying the fuller path first next time rather than rediscovering this.
- **Nothing committed or written to the repo this session** — the mockup HTML was delivered to Ryan via `SendUserFile` only (iterated across two rounds on the same file), not persisted as a Cowork artifact, not staged into the actual `docs/` folder. Purely a design sketch to align on before writing any package code, per the standing rule that real changes get written back via the device bridge but this session never reached that point until the NOTES.md update itself, right at the end.
- **Housekeeping while checking git state at the end of the session:** a `git status` run via `device_bash` left a stale `.git/index.lock` behind again — same long-standing bridge limitation (can't unlink files over the mount, only move them). Moved aside to `.git/index.lock.OLD` before finishing, so it doesn't block Ryan's next local git command; worth Ryan deleting `.git/index.lock.OLD` next time he's at the repo, and worth remembering that even a read-only `git status` over the bridge leaves this behind, not just writes.
- **Ryan's call at the end of the session: proceed with the real `ReportBuilder` build.** New branch to be created from Ryan's own terminal before any package code is written, per the standing rule (git operations never run from the sandbox/bridge, only `mv`/file writes).

## Next slice (updated session 28 — supersedes the session-27 list above)

1. **Ryan:** create `feat/report-builder` off an up-to-date `main` (commands given in chat this session) — nothing to build until this exists.
2. **Resolve the two open design questions from this session before/while building:** (a) marginal-sensitivity vs. true joint per-channel optimisation for `BudgetPhaser`'s per-channel amplitude curves — leaning marginal sensitivity as the pragmatic first version, Ryan to confirm; (b) whether page 1's Diagnose section needs its own elasticity caveat box alongside Impact's.
3. **Pin down `ReportBuilder`'s actual API** — only sketched conversationally so far (`history_df=`/`plan_df=`/`true_elasticities=` in, `.fit()`, then some `.to_html()`/`.to_pdf()` output side), not designed in detail. Needs its own pass once the branch exists.
4. **Build `ReportBuilder`** against this session's locked two-page structure, swapping every illustrative data block in the mockup for real `CollinearityDiagnostic`/`BudgetPhaser` output.
5. **Settle HTML-print-to-PDF vs. a dedicated PDF pipeline** — depends on Ryan actually trying the mockup's print preview, still outstanding.
6. **Decide whether `notebooks/06_run_your_own_data.ipynb` (session 26's runner-template idea) is still needed as a separate step**, or whether it folds into/gets superseded by `ReportBuilder` directly — session 26 scoped notebook 06 as shipping first with plain printed output, ahead of the report class; that ordering may no longer make sense now that the report class is what's actually being built first. Not discussed this session, worth a quick decision before item 6 of the session-27 list (Monzo validation) is scoped for real.
7. Carried over, untouched this session: duplicate Jupyter cell IDs (01/04), the `research.html` pass, PyPI publish, LinkedIn post(s) last — same status as session 27 left them.

## Status as of session 29 (2026-08-10, `ReportBuilder` built for real — package code + tests, branch `feat/report-builder`)

- **Same session as 28's mockup review, continued after Ryan created `feat/report-builder` from his own terminal and said "ready."** Moved from design to a real, tested implementation, grounded in the actual package source (`_diagnostic.py`, `_mmm.py`, `_dgp.py`, `_phaser.py`) rather than NOTES.md's own summaries of it — that check-in surfaced two corrections to session 28's framing, both good news for scope:
  - **Per-channel differentiated phasing levers are not "just a front-end illustration."** `BudgetPhaser.max_weekly_deviation_pct` already fully supports a dict of per-channel `float`/`Blackout` values — session 24 had flagged the `introduction.html` mixed-lever example as illustrative-only, but the underlying package capability was real all along.
  - **The correlation matrix needed no new computation.** `CollinearityDiagnostic.correlation_matrix` already exists as a public property — the mockup's matrix was recreated with fake data for no reason other than not having checked first.
- **Resolved session 28's open question (a) — marginal vs. joint channel sensitivity — by building the marginal/isolated version, per the session-28 leaning.** Added `BudgetPhaser.channel_sensitivity(channel, alphas=..., magnitude_pct=40.0, blackout=None, ...)`: locks every other channel at 0, sweeps the target channel's amplitude (plus an optional `Blackout` point), and returns a `DataFrame` of `label`/`magnitude_pct`/`is_blackout`/`cv`. Docstring is explicit that this is a marginal, isolated curve — "if you only touch this channel" — distinct from `fit()`'s joint grid search, so nobody mistakes one for the other later. To build it without duplicating `fit()`'s simulation logic, refactored `fit()`'s inner per-alpha loop out into a new `_evaluate_spec_at_alpha()` helper, verified byte-for-byte identical `results_`/`recommended_schedule_` output before/after via a dedicated reproducibility test.
- **Resolved session 28's open question (b) — does Diagnose need its own elasticity caveat — implicitly, by Ryan's mockup approval.** The approved mockup ("ok love the design!") only ever had one caveat box, on Impact; `ReportBuilder`'s template carries that forward unchanged. No separate Diagnose-section caveat was added. Worth a deliberate second look later if it turns out to matter, but not treated as still-open — the approved design answered it.
- **New `BudgetPhaser.impact_over_horizons(horizons_weeks=[13,52,104], ..., include_revenue=False)` added** for the Impact section's before/after-at-multiple-horizons table. One design correction caught and fixed *before* it reached any real usage: the first draft took a static `planned_spend` dict param forwarded unchanged to every horizon, which would have scaled revenue wrong (a 13-week and a 104-week horizon have very different total spend). Fixed by computing `planned_spend` fresh per horizon from `_tile_plan(self.plan_df, h).sum()` instead.
- **New `_tile_plan(plan_df, n_weeks)` helper** (extends/truncates a plan via `np.tile`, builds a continuing `DatetimeIndex`) added to support both `impact_over_horizons` and `ReportBuilder`'s own time-to-benefit sweep.
- **`ReportBuilder` class built in `src/how_wrong_is_your_mmm/_report.py`:** `__init__(history_df, plan_df, true_elasticities=None, max_weekly_deviation_pct=40.0, client_name="", plan_year="", seed=0)`, `.fit(n_sims=50, grid_steps=20, n_phasing_seeds=3, horizons_weeks=None, sensitivity_magnitude_pct=40.0, sensitivity_blackout=Blackout(max_dark_weeks_per_month=1), ttb_horizons_weeks=None, fast_mode=False)`, `.to_html(path=None)`. `.fit()` runs the real pipeline end to end — today's unphased `CollinearityDiagnostic` (correlation matrix + honest range), `BudgetPhaser.fit()`, `impact_over_horizons()`, `channel_sensitivity()` per channel, and a new time-to-benefit sweep (isolated to the least-reliable channel, four intensities — ±20/±40/±80% continuous and `Blackout` — across the ttb horizons, reusing `BudgetPhaser`'s private `_evaluate_spec_at_alpha` deliberately rather than adding another public method for a computation specific to this report's own narrative) — then assembles one JSON-serialisable `report_data_` dict. `.to_html()` substitutes that JSON into a single self-contained HTML template (`_HTML_TEMPLATE`) ported near-verbatim from the approved mockup: same cover, headline callout, correlation matrix, forest plots, schedule chart, Impact section with the amber caveat box, and page 2's channel-sensitivity/time-to-benefit charts — every chart-drawing function rewired from the mockup's fake-data generators to read the real `REPORT` JSON. No new runtime dependencies (still just `numpy`/`pandas`); JSON substituted via `.replace()` on a placeholder token, not an f-string, since the template's own CSS/JS braces would collide with `.format()`.
- **Exported from the package** — `ReportBuilder` added to `how_wrong_is_your_mmm/__init__.py`'s imports and `__all__`, alongside `CollinearityDiagnostic`/`BudgetPhaser`/`Blackout`.
- **Real bug found and fixed via a from-scratch smoke test (not caught by writing the code alone):** `ReportBuilder.fit()`'s time-to-benefit month labels were built by indexing a `horizon_labels` dict that only had entries for `horizons_weeks` (the Impact section's horizons), against `ttb_horizons_weeks` (a different, overlapping-but-not-identical set) — a `KeyError` on any horizon in one list but not the other (e.g. the default 52-week plan's `ttb_horizons_weeks` includes 26 weeks, which `horizons_weeks` doesn't). Fixed by extracting a standalone `_horizon_label(h)` function used by both, rather than a dict scoped to one call site.
- **Other cleanup from a first-draft pass that hadn't been tested yet:** removed two genuinely-unused imports (`datetime`, `numpy`) that had been left in with placeholder `_ = datetime`/`_ = np` lines; simplified a dead `hasattr(date.today(), "strftime")` branch (date objects always have `strftime`) in the cover's generated-date field; removed a `# noqa: SLF001` comment on the `_evaluate_spec_at_alpha` call — `SLF001` isn't in this project's enabled ruff rule set (`select = ["E","F","I","N","UP","RUF"]`), so the suppression was for a rule that was never going to fire, and worse, `RUF100` (unused-noqa, which *is* enabled) would have flagged the stale suppression itself; replaced with a plain explanatory comment.
- **`pyproject.toml` gained one new per-file-ignore**, same pattern as the existing notebooks entry: `src/how_wrong_is_your_mmm/_report.py` now ignores `E501` (the HTML/CSS/JS template is a single large string literal with inherently long markup lines, not a Python style issue) and `RUF001`/`RUF002` (a couple of intentional en-dashes in client-facing revenue-range display text, e.g. "£1.53m–£2.40m").
- **Verification, in order:**
  1. `ruff format`/`ruff check` clean across the whole package (`_phaser.py`, `_report.py`, `__init__.py`, both test files) after the fixes above.
  2. A from-scratch smoke test — `ReportBuilder(...).fit(fast_mode=True).to_html(path=...)` against real `simulate_spend` data — caught the `horizon_labels`/`KeyError` bug above. Fixed, reran clean.
  3. Headless Playwright verification of the actual generated report (not the mockup) at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`: zero JS console errors, zero `NaN` in any chart SVG, full-page screenshots reviewed section by section. Confirms the real `CollinearityDiagnostic`/`BudgetPhaser` output renders correctly through every ported chart function — correlation matrix, honest-range forest plots (Diagnose and before/after Impact), the weekly schedule chart, channel-sensitivity curves, and the time-to-benefit chart, plus the amber caveat box. Also spot-checked that the sensitivity insight-box text (e.g. "Still improving well past the midpoint...") is genuine conditional logic keyed off each channel's own Blackout-vs-continuous-ceiling ratio, not a hardcoded string — all three test channels happened to land in the same branch for this particular simulated dataset, which is a property of the data, not a bug.
  4. New `tests/test_report.py` written (33 tests: `_horizon_label` behaviour; `ReportBuilder.fit()`'s `report_data_` structure, channel/correlation/schedule/impact/sensitivity/ttb contents, continuous/`Blackout`/mixed-lever handling, blank vs. set client metadata; `to_html()`'s document structure, embedded-JSON correctness, placeholder substitution, file-writing). One early test failure was a test bug, not a code bug: comparing `report_data_` against its own round-tripped JSON with plain `==` fails on the `Blackout` row's legitimate `NaN` `magnitude_pct` (`nan != nan`) — fixed by comparing serialised JSON strings instead. Full suite: **185 tests passing** (was 132 at the start of session 28; +53 across `_phaser.py`'s `channel_sensitivity`/`impact_over_horizons`/`_tile_plan`/refactor-reproducibility tests and this new file), **99% coverage on `_phaser.py`, 100% on `_report.py`**.
- **Written back to Ryan's machine via the device bridge** (`SendUserFile` + `device_commit_files`, same pattern as every prior session): `src/how_wrong_is_your_mmm/{_phaser.py, _report.py, __init__.py}`, `tests/{test_phaser.py, test_report.py}`, `pyproject.toml`. **Left uncommitted on `feat/report-builder`**, per the standing rule — commit/push/merge is Ryan's call from his own terminal, never the sandbox.
- **Not yet done:** Ryan hasn't reviewed the actual rendered HTML output himself yet (only this session's own Playwright screenshots have been checked) — worth him generating and opening a real report before merging. The print-to-PDF question from session 28 is also still untested by him. `ReportBuilder`'s revenue/£ figures haven't been cross-checked against `introduction.html`'s or notebook 02's own numbers the way session 27 did for the Impact section fix — worth doing once Ryan's run it against something closer to real data.

### Flagged by Ryan right after this session, investigated same session before he logged off: report shows "basically no benefit, basically no spend changed"

Ryan opened the sample report sent this session and flagged it looked wrong — the Impact section showed almost no CV improvement, and the schedule chart barely differed from the original plan. Reproduced with real numbers (not guessing) against the same `simulate_spend(seed=0/1, correlation=0.7)` three-channel data used throughout the package, before logging off. **Two distinct things are going on, one confirmed as a fast_mode artifact and one that looks like a real methodological issue worth a closer look:**

1. **The specific sample report sent this session used `fast_mode=True`** (`grid_steps=10, n_sims=10, n_phasing_seeds=1`), which is noisy enough that the grid search picked `best_alpha ≈ 0.22` (only 22% of the max amplitude) — hence the schedule chart barely moving (mean weekly diff ~3.5% of spend). This is expected/documented behaviour of `fast_mode` ("not for numbers to actually hand a client," per the docstring) — the sample should have been generated at default settings, that's on me for sending a `fast_mode=True` output as if it were representative.
2. **Rerunning at full default settings** (`n_sims=50, grid_steps=20, n_phasing_seeds=3`, same data) fixed the schedule-barely-changes symptom (`best_alpha≈0.84`, ~12–14% mean weekly diff — a real, visible change), **but the Impact section's own 1-year CV reduction was still tiny and inconsistent: TV +0.07%, Meta 5.0%, Search 6.3%** — nowhere near the ~30%+ reductions `introduction.html`/notebook 02 report for a comparable continuous-lever scenario. Traced this to a real discrepancy between two numbers that should roughly agree: `phaser.results_`'s own CV at the recommended alpha (Search: 0.2788) vs. `impact_over_horizons()`'s independently-recomputed "after" CV at that *same* alpha (Search: 0.3881) — a ~40% relative gap. Likely cause, not yet confirmed: **the grid search's recommended alpha is chosen as the argmin over 20 noisy CV estimates (only `n_phasing_seeds=3`/`n_sims=50` each), so the selected "best" alpha is itself somewhat likely to be a lucky low draw (the CV curve genuinely bounces ±0.05 between adjacent alphas in the printed `results_`) — a classic "optimizer's curse" / selection-bias pattern. `impact_over_horizons()` then re-evaluates that same alpha with fresh independent seeds, and the optimistic bias doesn't reproduce, regressing toward a less impressive (but more honest) number.** If this diagnosis is right, it means the headline CV figure the grid search reports for its own recommended alpha may be *systematically optimistic* everywhere it's used (not just in `ReportBuilder`) — worth checking whether `introduction.html`/notebook 02's existing headline figures have the same issue, or whether they happened to avoid it (e.g. because they use `Blackout`, a much stronger lever, where the effect may be large enough to swamp this noise).
   - Not yet root-caused for certain, not yet fixed. Options to weigh next session: increase `n_phasing_seeds`/`n_sims` in the grid search specifically (more expensive per report); re-evaluate the winning alpha with a larger, independent sample once selected, and report *that* number as the headline (a standard fix for the optimizer's-curse pattern — split "selection" and "evaluation" onto different data); or something else once the cause is confirmed rather than just plausibly diagnosed.
   - Reproduction recipe for next session: `simulate_spend(n_obs=208, correlation=0.7, seed=0, start_date="2019-01-07")` (history) + `simulate_spend(n_obs=52, correlation=0.7, seed=1, start_date="2023-01-09")` (plan), `ReportBuilder(..., seed=0).fit(n_sims=50, grid_steps=20, n_phasing_seeds=3, fast_mode=False)` — compare `rb.phaser_.results_` (per-alpha CV, noisy across neighbours) against `rb.report_data_["impact_horizons"]` at the 52-week horizon (much worse CV at the same alpha).

## Next slice (updated session 29 — supersedes the session-28 list above)

1. **Investigate and fix the weak-benefit issue above — top priority, blocks a real review of `ReportBuilder`'s output.** Confirm whether the grid-search-selection-bias diagnosis is right (e.g. rerun the recommended alpha many independent times and check whether the mean CV across those runs sits close to `results_`'s reported value or closer to `impact_over_horizons`'s), then decide the fix (bigger grid-search sample vs. a dedicated re-evaluation step vs. something else). Worth also checking whether `introduction.html`/notebook 02's existing published figures are exposed to the same bias.
2. **Ryan:** once the above is resolved, review the actual `ReportBuilder` output again — generate a report against real or synthetic data locally, open the HTML, sanity-check it against the approved mockup design, and try the browser's print-to-PDF path (session 28's still-open question on whether that's good enough or a dedicated PDF pipeline is needed).
3. **Ryan:** review, commit, and merge `feat/report-builder` from his own terminal once happy — nothing here has been committed yet.
4. Once merged, decide whether `notebooks/06_run_your_own_data.ipynb` (session 26's runner-template idea) is still worth building separately, or whether `ReportBuilder` now covers that use case directly — session 26 scoped notebook 06 to ship *before* the report class; that ordering has now inverted, worth a deliberate decision rather than defaulting either way.
5. Cross-check `ReportBuilder`'s £ figures against `introduction.html`/notebook 02's own already-validated numbers once run against a comparable scenario, same spirit as session 27's Impact-section averaging fix — now doubly important given item 1 above.
6. Monzo validation, using `ReportBuilder` directly now that it exists (supersedes the old notebook-06-first plan from session 26, pending item 4's decision).
7. Carried over, untouched this session: duplicate Jupyter cell IDs (01/04), the `research.html` pass, PyPI publish, LinkedIn post(s) last — same status as sessions 27–28 left them.

## Status as of session 30 (2026-08-11, root-caused and fixed the "weak benefit" report problem — three distinct issues, not one)

- **Reached Ryan's machine via the device bridge**, same pattern as sessions 23–29. Picked up session 29's top-priority item: the flagged "report shows basically no benefit" problem. Investigated properly (reproduced with real numbers directly against `src`, `n_sims=50, grid_steps=20, n_phasing_seeds=3, fast_mode=False`, the standard `simulate_spend(seed=0/1, correlation=0.7)` history/plan recipe) rather than accepting the session-29 diagnosis at face value — Ryan explicitly pushed back mid-session ("are you sure it's not a problem with one of the new methods you wrote") and asked for the diagnosis confirmed independently of `impact_over_horizons`/`channel_sensitivity` before trusting it. Turned out to be **three separate issues**, only one of which was the optimizer's-curse story session 29 flagged:
  1. **The specific sample report sent session 29 used `fast_mode=True`** — expected/documented noisy behaviour, not a bug, but nothing distinguished that report from a real one once sent.
  2. **Session 29's own investigation note mis-cited a table row**: the "TV +0.07%, Meta 5.0%, Search 6.3%" figures it called "the 1-year figures" are actually the **3-month** row — confirmed by rerunning the exact `ReportBuilder` pipeline. The real 1-year figures were meaningfully better (TV 18.8%, Meta 11.8%, Search 7.6%) even before any of today's fixes.
  3. **The real, non-cosmetic bug**: the headline callout always quotes the *least-reliable* channel (Search), but `ReportBuilder` applied one flat +/-40% continuous lever to every channel regardless of how well that lever actually suits each one — and Search happened to be the channel that lever helps least (7.6% CV reduction at 1yr vs TV's 18.8%). Headline read "±35% -> ±32%", which reads as "nothing happened" even though the number is real. Traced this all the way to the actual JS in `_report.py`'s `_HTML_TEMPLATE`, not just the Python side.
  4. **Also confirmed, smaller effect**: `BudgetPhaser.fit()`'s grid-search argmin is a genuine "optimizer's curse" — picking the min of `grid_steps` noisy 3-phasing-seed-averaged estimates is systematically optimistic. Verified two ways: (a) 40 independent re-evaluations of `_evaluate_spec_at_alpha` at the exact winning alpha (bypassing `impact_over_horizons` entirely) landed near the *maximum* of the observed range, not near the grid's own reported value; (b) diffed session 29's actual refactor against `main` and confirmed `_evaluate_spec_at_alpha` is a byte-for-byte lift of the old inline loop, and `_diagnostic.py` (where the bias-independent CRN sampling lives) has zero diff — ruling out a refactor bug as the cause, isolating this cleanly to sampling variance in alpha selection.
  - Also verified `notebooks/02_phaser_walkthrough.ipynb`/`introduction.html`'s own headline figures are **not** more exposed to issue 4 than `ReportBuilder` — their `blackout_impact()` helper already re-evaluates the recommended alpha on fresh independent seeds before reporting, the same pattern `impact_over_horizons()` uses. Not a new source of concern, and not touched this session.
- **Ryan's call once all three were laid out plainly: "fix everything ... this is a robust report clients are going to use."** All three fixed and tested (not just proposed):
  - **`BudgetPhaser.fit()` gained a selection-bias confirmation pass** (new `confirm_top_k`/`confirm_n_phasing_seeds` params, default top-3 candidates re-evaluated on 3x the phasing-seed sample, fresh seeds well clear of the grid search's own range). New `confirmation_` attribute holds the honest re-evaluation; `recommended_schedule_` and `recommend()` now use the confirmed winner, not the raw grid argmin — `results_` is kept unchanged for inspecting the raw (noisier) grid. `fast_mode` forces `confirm_top_k=1` (still confirms the single winner, skips the extra cost of checking neighbours).
  - **New `BudgetPhaser.recommend_levers()` method**: compares each channel's own marginal CV (via `channel_sensitivity`) at full continuous amplitude vs. Blackout, and picks Blackout only when it's a *meaningfully* better result (default 10% relative-improvement threshold, comfortably above the method's own sampling noise) — avoids reaching for the operationally harder lever on noise alone.
  - **`ReportBuilder.fit()` gained `auto_lever=True` (default) and `lever_improvement_threshold_pct=10.0` params**: when `max_weekly_deviation_pct` was left at its default and `sensitivity_blackout` isn't disabled, calls `recommend_levers()` (using the same `sensitivity_magnitude_pct`/`sensitivity_blackout` as the page-2 chart, so they can't disagree) and uses the resolved per-channel spec for the actual phaser, the channel/lever table, and every downstream number. An explicit caller-supplied lever (float, Blackout, or dict) is always respected as-is — confirmed via the existing `test_blackout_lever_reflected_in_channel_rows`/`test_per_channel_mixed_levers` tests, which still pass unchanged.
  - **Draft watermark for `fast_mode=True` reports**: new `report_data_["meta"]["fast_mode"]` flag and a red (not the existing amber elasticity-caveat colour) `#draftBanner` div, shown via a JS toggle, `display: flex` even under `@media print` so it survives a PDF export. Methodology text also now mentions the confirmation pass and, when `auto_lever_applied`, the per-channel lever selection — so the report explains its own more-honest process rather than presenting the corrected numbers as if they'd always worked this way.
- **Verified on the real reproduction scenario** (full settings, not fast_mode): all three channels' marginal sensitivity cleared the 10% threshold and auto-escalated to Blackout (matches `introduction.html`'s own finding that Blackout dominates continuous for this dataset). Headline flipped from "±35% -> ±32%" (old, flat continuous lever) to **"±35% -> ±20%"** (new, auto-selected Blackout) — a visibly real number instead of a rounding-error-looking one. 1-year reductions: TV 53.1%, Meta 48.9%, Search 43.1% (was 18.8%/11.8%/7.6%), now in the same range as `introduction.html`'s own published 54%/54%/54% for a comparable all-Blackout scenario — a good cross-check. Interesting secondary finding: the confirmed `max_cv` (0.1835) came in *below* the raw grid argmin (0.1859) here — not a contradiction, the confirmation pass doesn't just regress the raw winner upward, it independently re-measures the top-3 candidates and can find that a near-neighbour was the honestly-better alpha all along.
- **Verification held to the project's usual standard:** `ruff format`/`ruff check` clean; full suite **211 tests passing** (was 185 at the end of session 29 — +26: `TestSelectionBiasConfirmation`, `TestRecommendLevers`, `TestReportBuilderFastModeWatermark`, `TestReportBuilderAutoLever`, plus fixing the one existing test — `test_recommend_is_min_cv` — that encoded the old biased behaviour), 99% coverage on both `_phaser.py` and `_report.py`. Real end-to-end smoke test (not just unit tests) run against the full-settings reproduction scenario above, rendered through a real headless Chromium (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, same path session 28 pinned down): zero JS console errors, zero NaN SVG coordinates, both the real report and a deliberately-generated `fast_mode=True` report screenshotted and visually confirmed — draft banner correctly hidden on the real one, correctly shown (red, "DRAFT — generated in fast mode...") on the fast-mode one.
- **Not yet done:** `impact_over_horizons()`'s revenue (£) fields weren't specifically re-verified against the new confirmed-alpha/auto-lever path today — only CV was checked in depth. Nothing committed — same standing rule as always, still on `feat/report-builder`, commit/push/merge is Ryan's call from his own terminal. `.git/index.lock` appeared again after a `git status` over the bridge — moved aside to `.git/index.lock.OLD` as usual (session 28 flagged this same recurring bridge limitation).

### Same session, continued: Ryan's review raised two more real mismatches, both fixed

After seeing the first round of fixes, Ryan flagged two things while reviewing rather than just approving — both turned out to be genuine gaps, not just questions:

- **Channel-sensitivity chart (page 2, Section 4) didn't match its own approved design.** Session 28's locked mockup spec says this chart should sweep "0% -> 100% continuous -> Blackout," but the shipped default (`sensitivity_magnitude_pct=40.0`, even 0.2-step alphas) only swept 0-40%. Ryan's own recollection ("I thought we'd go up to 80%, like 10/20/40/80%") was the more accurate one. **Fixed:** `ReportBuilder.fit()`'s `sensitivity_magnitude_pct` default raised 40.0 -> 80.0, new `sensitivity_alphas` param (default `[0.0, 0.125, 0.25, 0.5, 1.0]`) gives checkpoints at 0/10/20/40/80% instead of even steps — both overridable. Since `sensitivity_magnitude_pct` is also what `recommend_levers()` compares Blackout against for the auto-lever decision, this has a real side effect beyond the chart: auto-lever now asks "does Blackout beat the *strongest* continuous option (80%), not just a middling one (40%)" — a harder, more honest bar, consistent with the site's own "gentle nudge up to 80%, or a full on/off switch" framing (session 24's FAQ copy). Verified on the real scenario: all three channels still clear the harder bar and escalate to Blackout (TV's 80%-continuous CV is 0.1834 vs Blackout's 0.14, a 24% relative gap, comfortably over the 10% threshold).
- **Grid-search precision now matches notebook 02's own full-mode settings**, not just an arbitrary internal default. `grid_steps` 20 -> 25, `n_phasing_seeds` 3 -> 10 (both still overridable). Directly relevant to today's earlier selection-bias fix: a larger `n_phasing_seeds` reduces the underlying noise the confirmation pass exists to correct for, so this is a second, complementary fix for the same root problem, not just a consistency tidy-up. Cost: a full `fit()` on the standard reproduction scenario went from ~7s to ~24s — still well within reason for a report a person generates and reviews, not something run in a tight loop.
- **Also confirmed, no code change needed:** per-channel "this channel can never use Blackout" already works today via an explicit dict (`max_weekly_deviation_pct={"tv": 40.0, "meta": Blackout(...), "search": 40.0}`) — pre-existing `BudgetPhaser` functionality, not new. Flagged one real gap while confirming it, though: there's no way yet to say "auto-pick for every channel except X, which must never get Blackout" — an explicit dict opts *every* channel out of auto-selection, not just one. Ryan explicitly said not to build this, just wanted to confirm the current capability — logged as a possible future ask, not scoped.
- **Verified again end-to-end** with both changes together on the real reproduction scenario: sensitivity checkpoints came back exactly `{0%, 10%, 20%, 40%, 80%, Blackout}` as intended, monotonically decreasing CV (0.3216 -> 0.3186 -> 0.3092 -> 0.2748 -> 0.1834 -> 0.14 for TV), all three channels still auto-escalate to Blackout, headline now reads "±35% -> ±19%" (1yr). Real Chromium render: zero JS errors, zero NaN, 5 charts, page-2 sensitivity chart visually confirmed showing the new 5-point-plus-Blackout curve. Two new tests added (`test_sensitivity_default_checkpoints_are_10_20_40_80_pct`, `test_sensitivity_alphas_and_magnitude_are_overridable`) — **full suite now 213 tests passing**, `ruff format`/`ruff check` clean, 99% coverage across `_phaser.py`/`_report.py`. Files re-sent and re-committed to Ryan's machine via the device bridge; still uncommitted on `feat/report-builder`, per the standing rule.

### Same session, continued again: docs/onboarding polish — `schedule_csv()`, `reports/` convention, README, and a public example report

Ryan's follow-up round, once the report itself was fixed: "do we need to update README with example of how to run html report? where is the html report saved? should we also output a csv with phased budgets? given the html report, is there anything you would add to introduction.html?" — plus a mid-round question, "if we put the html we created in the location with introduction.html will people be able to see it? would be a good example report." All confirmed via follow-up questions and actioned:

- **`ReportBuilder.schedule_csv(path=None)` added** — a proper method, not just pointing people at `phaser_.recommended_schedule_` directly. Builds a `week`-indexed `DataFrame` with three columns per channel (`{ch}_original_plan`, `{ch}_recommended`, `{ch}_dark_week`), the last a boolean flag (`recommended == 0.0`) so a client-facing CSV doesn't need `Blackout` internals explained to be read. Writes to `path` if given, always returns the `DataFrame` either way. New `TestReportBuilderScheduleCsv` class, 8 tests — caught and fixed three bugs while writing them: a missing `import pandas as pd` in `test_report.py` (`NameError`), a broken `pytest.approx(...).all()` pattern (should compare the arrays directly, not call `.all()` on the bool `approx` already produces), and a test that used `max_weekly_deviation_pct=40.0` to mean "explicit continuous lever" — collided with the `_DEFAULT_LEVER` sentinel and silently triggered `auto_lever` instead of testing what it meant to; fixed by using `55.0` (off-sentinel) plus an explicit `auto_lever_applied is False` assertion.
- **`reports/` output folder convention added**, with a `.gitignore` entry (`reports/*` / `!reports/.gitkeep`) — generated client reports/schedules are real client data and have no business landing in the public repo. `reports/.gitkeep` committed so the folder exists for anyone cloning fresh; `reports/example_schedule.csv` is the one deliberate exception, git-ignored like everything else in the folder but hand-copied into `docs/` for the public example (see below), never generated straight into a tracked location.
- **README updated:** `ReportBuilder`/`to_html()`/`schedule_csv()` example added directly after the existing `BudgetPhaser` quick-start snippet, a line explaining the `reports/` git-ignore convention, the stale "132 tests" fixed to the real current count (**222 tests** — was 213 at the end of the last round, +9 for `schedule_csv`), and a new "Example report" entry in the Guides list linking to the live public report (below).
- **Public example report shipped**, prompted by Ryan's GitHub Pages question — confirmed `docs/` is already how `introduction.html`/`research.html`/`docs/api/` are served live (per `mkdocs.yml`'s `site_dir: docs/api` plus the existing hand-placed pages), so anything placed there and pushed to `main` is public. Generated `docs/example-report.html` against the project's standard reproduction scenario (`simulate_spend` history seed=0/plan seed=1, correlation=0.7 — same recipe as `introduction.html`/notebook 02) with `client_name="Example Brand", plan_year="Next year"`, full settings (`fit(fast_mode=False)`, so no draft banner). Confirms the new defaults end to end on a real run: 25 grid rows, 3 confirmation rows, sensitivity checkpoints `{0%, 10%, 20%, 40%, 80%, Blackout}`, all three channels auto-escalate to Blackout — consistent with every other verification this session. `reports/example_schedule.csv` generated alongside it via the new `schedule_csv()` method, as the CSV's own real usage example. Both rendered via the same Playwright/Chromium check as every other change this session: zero JS console errors, zero NaN SVG coordinates, 2 pages, draft banner correctly absent.
- **`docs/introduction.html` callout drafted, reviewed, and approved by Ryan same session ("looks good please finish off")**: one new sentence at the end of Section 6 ("How it works"), after the existing pipeline-diagram fig-cap, pointing at `ReportBuilder` and linking to `example-report.html`: *"Run all three steps against your own spend and `ReportBuilder` packages the result into a single client-ready HTML report: today's honest range, the recommended weekly schedule, and how much it narrows things, no code required to read it. See a full example report →"* Rendered and screenshotted for review (zero JS errors) before Ryan signed off — same review pattern as every other `introduction.html`/`research.html` copy addition in this project's history. Now finalised and written back.
- **Mid-round device disconnect:** the desktop bridge dropped right as `README.md`/`NOTES.md` were being written back, so that pair landed via `SendUserFile` only, not the device, for one exchange. Bridge reconnected next turn (confirmed via `get_device_info`, `connectedFolders` still correct) — `introduction.html` (with the now-approved callout), `README.md`, and this `NOTES.md` update all (re)written back in the same pass once the connection returned. `docs/example-report.html`, `reports/example_schedule.csv`, `reports/.gitkeep`, `.gitignore`, `_report.py`, `test_report.py` were all confirmed already present and correct on Ryan's machine via `device_list_dir` before resending anything, so they weren't re-sent.
- **Written back to Ryan's machine via the device bridge, this final pass:** `docs/introduction.html` (approved callout), `README.md`, `NOTES.md`. Combined with everything already on his machine from earlier in the session, the full `ReportBuilder` + docs/example-report + README/schedule_csv/reports-folder slice is now completely on disk. **Nothing committed** — same standing rule as always, still on `feat/report-builder`, commit/push/merge is Ryan's call from his own terminal.

## Next slice (updated session 30, end of session — supersedes the list above)

1. **Ryan:** review, commit, and merge `feat/report-builder` from his own terminal — nothing here has been committed yet, including this round's README/`schedule_csv`/`reports/`/example-report/`introduction.html` additions. Worth a `git status` first pass to see everything that landed this session.
2. **Ryan:** review the regenerated `ReportBuilder` output himself — generate a report locally, open the HTML, sanity-check the new checkpoints/auto-selected levers/headline number against his own judgement, try the browser's print-to-PDF path (still outstanding since session 28).
3. Cross-check `impact_over_horizons()`'s £ revenue fields specifically (not just CV) against the new confirmed-alpha/auto-lever/wider-sensitivity path — verification has been CV-focused throughout.
4. Once merged, decide whether `notebooks/06_run_your_own_data.ipynb` (session 26's runner-template idea) is still worth building separately, or whether `ReportBuilder` now covers that use case directly.
5. Monzo validation, using `ReportBuilder` directly now that it exists.
6. Consider whether `recommend_levers()`'s 10% improvement threshold, `fit()`'s `confirm_top_k=3`/`3x` phasing-seed multiplier, and the 25/10 grid-precision defaults are right long-term, or just reasonable first choices — not stress-tested against other datasets/channel counts yet.
7. **New, logged not scoped:** a way to auto-select levers for most channels while explicitly excluding one or more from ever being offered Blackout (today it's all-auto or all-manual, no in-between) — Ryan confirmed he doesn't need this yet, just wanted the current capability confirmed.
8. Carried over, untouched this session: duplicate Jupyter cell IDs (01/04), the `research.html` pass, PyPI publish, LinkedIn post(s) last.

## Status as of session 31 (2026-08-15, `feat/report-builder` merged + API-docs/`research.html` gap pass)

- **`feat/report-builder` reviewed and merged by Ryan from his own terminal** (item 1 of the session-30 list) — confirmed via `git log`/`git status` on his machine: `main` now has commits `d2f0d12` (`ReportBuilder` core) and `61a1b43` (docs/onboarding polish), working tree clean, branch tracking `origin/feat/report-builder`. Before this, helped split the uncommitted working tree into those two commits (feature code + tests in one, docs/example/schedule-csv/README polish in the other) since nothing from sessions 28–30 had been committed yet.
- **Ryan asked for a "what's missing" review rather than picking a specific next-slice item.** Found and fixed one real gap, then two more once asked to "do it now":
  1. **`ReportBuilder` was entirely absent from the mkdocs API reference** — no `mkdocs_src/api/report.md`, not in `mkdocs.yml`'s nav, not in `index.md`'s class table (still headed "The three classes"). Added `api/report.md` (same pattern as `phaser.md`/`diagnostic.md`), added it to the nav between Phaser and DGP, renamed the table heading to "The classes" and added the `ReportBuilder` row, and added a `ReportBuilder` snippet to `index.md`'s minimal example. **Verified with a real `mkdocs build --strict`** (deps installed fresh in the sandbox, full `src/` copied in so mkdocstrings could introspect real docstrings) — clean build, `to_html`/`schedule_csv`/`auto_lever` all render in the generated page. Written back via the device bridge.
  2. **`docs/research.html` never got the `ReportBuilder`/`example-report.html` callout** that `introduction.html` got last session — added the identical sentence to Section 5 ("How It Works"), right after the pipeline-diagram `fig-cap`, before the "up next" box.
  3. **`docs/research.html` had the same sticky-nav/scrollspy bug `introduction.html` had before session 24's fix** — `.snav` was missing `position: sticky`/`top: 0`/`z-index: 20`/`background: #fff`, and there was no scrollspy JS, so the nav scrolled out of view and the active tab never moved. Ported the exact CSS + `IntersectionObserver` scrollspy block from `introduction.html` verbatim (`research.html`'s sections already used the same `id`s the nav links point to, so no markup changes needed there).
  - **Verified in a real headless Chromium** (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, Playwright): zero JS console errors, zero NaN SVG coordinates (32 SVGs), tag balance confirmed (div/section/nav/a/svg all net zero), nav `position: sticky` confirmed via computed style, scrollspy confirmed functionally (active tab moved from `#problem` to `#how` after scrolling `#how` into view, not just visually plausible), and a full-page screenshot of the new callout + pinned nav reviewed before sending.
- **README `Quick start` code block split on Ryan's request** — the `ReportBuilder` example was previously tacked onto the end of the same fenced block as `CollinearityDiagnostic`/`BudgetPhaser`; now its own separate ` ```python ` block with its own `from how_wrong_is_your_mmm import ReportBuilder` import line, right after the `BudgetPhaser` block.
- **`docs/api/` (the built mkdocs site) was NOT rebuilt this session** — attempted via `device_bash` but the repo's `.venv` has a stale/broken Python interpreter link inside the bridge's sandbox environment, and `uv` has no network access there to fetch a replacement. **Ryan needs to run `uv run mkdocs build --strict` from his own terminal** and commit the regenerated `docs/api/` alongside `mkdocs_src/`/`mkdocs.yml` — until that happens, the live Pages site still doesn't show `ReportBuilder` even though the source files are fixed.
- **Written back to Ryan's machine via the device bridge:** `mkdocs.yml`, `mkdocs_src/index.md`, `mkdocs_src/api/report.md`, `docs/research.html`, `README.md`. All uncommitted on his machine at time of writing — same standing rule as always, commit/push/merge is his call from his own terminal.

## Next slice (updated session 31 — supersedes the session-30 list above)

1. **Ryan:** run `uv run mkdocs build --strict` locally to regenerate `docs/api/` with `ReportBuilder` included, then commit everything from this session (`mkdocs.yml`, `mkdocs_src/`, `docs/api/`, `docs/research.html`, `README.md`).
2. **Ryan:** review the regenerated `ReportBuilder` output himself — generate a report locally, open the HTML, sanity-check the new checkpoints/auto-selected levers/headline number against his own judgement, try the browser's print-to-PDF path (still outstanding since session 28).
3. Cross-check `impact_over_horizons()`'s £ revenue fields specifically (not just CV) against the new confirmed-alpha/auto-lever/wider-sensitivity path — verification has been CV-focused throughout.
4. Decide whether `notebooks/06_run_your_own_data.ipynb` (session 26's runner-template idea) is still worth building separately, or whether `ReportBuilder` now covers that use case directly.
5. Monzo validation, using `ReportBuilder` directly now that it exists.
6. Consider whether `recommend_levers()`'s 10% improvement threshold, `fit()`'s `confirm_top_k=3`/`3x` phasing-seed multiplier, and the 25/10 grid-precision defaults are right long-term, or just reasonable first choices — not stress-tested against other datasets/channel counts yet.
7. **New, logged not scoped:** a way to auto-select levers for most channels while explicitly excluding one or more from ever being offered Blackout (today it's all-auto or all-manual, no in-between) — Ryan confirmed he doesn't need this yet.
8. Carried over, untouched this session: duplicate Jupyter cell IDs (01/04), PyPI publish, LinkedIn post(s) last.

## `feat/report-builder` merged (2026-08-15, PR #28)

Ryan reviewed and merged from his own terminal. Real merge commit `ecb1d75` (`Merge: 9365480 61a1b43`, not a squash), so `main` cleanly contains everything through session 30's work. The session-31 API-docs/`research.html` fixes (commits `7ad063e`/`a8e551d`, still on `feat/report-builder` after the first merge) went through a second PR (#28) opened straight from the same branch — GitHub's compare view only showed the two new commits since `main` already had the rest, confirmed via a real tree diff (`git diff origin/main...feat/report-builder --stat`) before opening it. Also confirmed live: `raz1470.github.io/how_wrong_is_your_mmm/api/` now lists `ReportBuilder` in both the nav and class table, and `api/report/` renders `fit`/`to_html`/`schedule_csv`/`auto_lever` correctly.

## Status as of session 32 (2026-08-15, £ revenue cross-check — found and fixed a real horizon-scaling bug)

- **Picked up session 30's item 3: cross-check `impact_over_horizons()`'s £ revenue fields against already-published numbers.** Verification until now had been CV-focused only. Ran the actual standard reproduction scenario (`history` seed=0/208wk, `plan` seed=1/52wk, correlation=0.7 — same as `introduction.html`) through a real `ReportBuilder.fit()` at full settings in the sandbox (fresh `numpy`/`pandas` install, no venv needed since this is pure simulation).
- **The core revenue arithmetic is correct.** `impact_over_horizons()`'s "today" £ range at the 52-week horizon matched `introduction.html`'s published TV range (£945k–£2.09m) almost to the pound, and also matched `diagnose_today`'s own numbers exactly (same combined dataset, as expected) — no calculation bug in `CollinearityDiagnostic.summary(planned_spend=...)` itself.
- **Real bug found: horizon-scaled `planned_spend` didn't match the rest of the package's own methodology.** `impact_over_horizons(include_revenue=True)` priced each horizon against *that horizon's own tiled-plan total* (a session-29 fix, deliberate at the time) — so the "2 years" row priced against roughly double the "1 year" row's spend, purely from `_tile_plan` doubling the plan length. But `notebooks/02_phaser_walkthrough.ipynb`'s own `blackout_impact()` helper — the actual source of `introduction.html`'s published Impact-section numbers — always prices every horizon against the **same fixed annual plan total**, using longer horizons only to represent "more experience/history accumulated," never "more spend." Confirmed by comparing the actual numbers: my 104-week "after" range for TV came out £2.63m–£3.44m (roughly double the 1-year range) against `introduction.html`'s published £1.343m–£1.738m (barely different from its 1-year range) — a genuine mismatch, not simulation noise. `_report.py`'s own Diagnose section already used the fixed-total approach (`self.plan_df.sum()`), so this was also an internal inconsistency within `ReportBuilder` itself, not just a mismatch against the public site.
- **Why this mattered for real:** a client report's Impact section (built on `impact_over_horizons()`) would show "after 2 years" £ figures that look roughly doubled versus "after 1 year," inviting a reader to conclude "phase for longer, get more revenue" — when the intended message throughout this package (see the "£0 extra spend needed" kbox on `introduction.html`) is "same plan, £0 extra spend, just a tighter range."
- **Ryan's call: match `introduction.html`'s framing.** Fixed `BudgetPhaser.impact_over_horizons()` so `planned_spend` is computed once from `self.plan_df.sum()` and reused at every horizon, not recomputed from each horizon's tiled plan. Docstring rewritten to explain why (session-29's original reasoning is called out and superseded, not silently dropped). `_report.py` needed no changes — it already builds `ReportBuilder`'s own Diagnose-section `planned_spend` the same way, so this fix actually makes `impact_over_horizons()` consistent with the rest of the report, not just with the public site.
- **Verified the fix numerically**, not just by re-reading the code: 104-week "today" TV revenue moved from [£2.02m, £3.81m] (old, ~2x the 1yr range) to [£1.01m, £1.90m] (new, same scale as the 1yr range), and 104-week "after" moved from [£2.63m, £3.44m] to [£1.31m, £1.72m] — now within ~2-3% of `introduction.html`'s published [£1.343m, £1.738m]. CV numbers are unaffected either way, as expected (CV is dimensionless, never touched `planned_spend`).
- **Two new regression tests added** to `tests/test_phaser.py`'s `TestImpactOverHorizons`: `test_revenue_uses_fixed_plan_total_across_horizons` (13-week vs 104-week revenue centres must stay within 3x of each other, not ~8x) and `test_revenue_matches_diagnostic_summary_at_plans_own_length` (the horizon equal to `plan_df`'s own length must price against the same `planned_spend` a standalone `CollinearityDiagnostic` call would use). Full suite **224 tests passing** (was 222), `ruff format`/`ruff check` clean on every touched file.
- **`docs/example-report.html` (the live public example) was regenerated** with the fix, same recipe as before (`client_name="Example Brand", plan_year="Next year"`, full settings, no draft banner) — it was generated under the old buggy behaviour and would otherwise have shown the same misleading 2-year doubling to anyone viewing it. Re-verified via headless Chromium: zero JS errors, zero NaN SVG coordinates, draft banner correctly absent. `reports/example_schedule.csv` regenerated alongside it (schedule CSV itself was never affected by this bug, regenerated only for consistency with the new report run).
- **README's test count updated** 222 → 224.
- **Written back to Ryan's machine via the device bridge:** `src/how_wrong_is_your_mmm/_phaser.py`, `tests/test_phaser.py`, `README.md`, `docs/example-report.html`, `reports/example_schedule.csv`, this `NOTES.md` update. All uncommitted at time of writing — same standing rule as always, commit/push/merge is Ryan's call from his own terminal.

## `feat/report-builder` round 3, same day (2026-08-15)

Ryan committed the £-revenue-horizon fix (`_phaser.py`, `test_phaser.py`, `README.md`, `docs/example-report.html`, `NOTES.md`) and pushed — confirmed via `git log`/`git status` on his machine (`HEAD` matches `origin/feat/report-builder` at `31f7163`). Given the same compare-link pattern as the API-docs round (`https://github.com/raz1470/how_wrong_is_your_mmm/compare/main...feat/report-builder`) to open a third PR from the same branch. **Ryan confirmed pushed and merged** ("i pushed a merged so cross that off") — not independently re-verified against GitHub this time (no network from the device bridge), but taking his word for it same as the two earlier merge confirmations this project. `main` should now have all of session 31–32's work: the API-docs fix, the `research.html` nav/callout fix, and the £-revenue-horizon fix. Ryan then reviewed the regenerated `example-report.html` himself and confirmed it looks right (session-32 next-slice item 2, closed) — no further changes requested.

**Notebook 06 decision made (session-32 item 3, resolved): dropped.** Ryan confirmed directly ("i dont think we need this notebook") — `ReportBuilder(history_df=, plan_df=).fit().to_html()` already is the clean, generic entry point for real spend data that notebook 06 (session 26) was scoped to provide, and it's more polished (auto-lever selection, client-ready output) than a blank runner notebook would have been. `notebooks/06_run_your_own_data.ipynb` is not being built. Ryan's own Monzo validation (see below) will go through `ReportBuilder` directly, not a notebook.

**Monzo validation scoping attempt, same session: Ryan waved it off ("forget this it doesnt need scoping").** Tried to scope it properly (data access, deliverable shape, whether it gates PyPI/LinkedIn) per the standing backlog item — Ryan declined to engage with any of the three questions. Not resolved, not being actively pursued right now; dropped back into the plain carried-over bucket below rather than treated as a live next step. Don't re-raise this as its own scoping conversation next session unless Ryan brings it up himself.

**"The research.html pass" resolved into two separate things, one closed, one still open — Ryan asked directly whether it was still outstanding.** It had silently fallen off the session-31/32 carried-over lists despite never being fully done, worth flagging that it was dropped by omission, not because it was finished:
- **Session 27's specific flag — checked for real this time, turns out to be a non-issue, closing it out.** The concern was whether `research.html`'s "From elasticity to £" subsection (Section 1, the `£664k–£2.36m` etc. kboxes) had the same single-draw-vs-averaged-across-`n_phasing_seeds` bug that was found and fixed on `introduction.html`'s Impact section in session 27. Read the actual subsection: it's a single unphased `CollinearityDiagnostic.summary(planned_spend=...)` snapshot (today's honest range only) — there's no phased-schedule averaging happening in it at all, so the bug pattern doesn't apply structurally, not just "wasn't checked." Also checked `research.html`'s own Impact section (Section 4) while at it — entirely CV-based, no £ figures there either. Nothing to fix.
- **Ryan's own line-by-line copy/critique pass — still genuinely open, unlike the item above.** `introduction.html` got a proper multi-session review (sessions 23–25: Ryan's own read-throughs and feedback rounds, a new algorithm section, FAQ additions). `research.html` never got the equivalent treatment, only the narrow nav-bug-fix + missing-`ReportBuilder`-callout patch from session 31. This is Ryan's own pass to drive whenever he wants to sit down with it, same pattern as `introduction.html`'s — not something to do unprompted.

## Next slice (updated session 32, continued — supersedes the list above)

1. **Ryan's own research.html copy/review pass** — whenever he wants to do it, same pattern as introduction.html sessions 23–25 (his read-through and feedback, actioned as he flags things).
2. Consider whether `recommend_levers()`'s 10% improvement threshold, `fit()`'s `confirm_top_k=3`/`3x` phasing-seed multiplier, and the 25/10 grid-precision defaults are right long-term, or just reasonable first choices — not stress-tested against other datasets/channel counts yet.
3. **New, logged not scoped:** a way to auto-select levers for most channels while explicitly excluding one or more from ever being offered Blackout — Ryan confirmed he doesn't need this yet.
4. Carried over, untouched this session: duplicate Jupyter cell IDs (01/04), Monzo validation (declined scoping this session), PyPI publish, LinkedIn post(s) last.

## Status as of session 33 (2026-08-16, two small source-reading fixes)

Ryan flagged two things while reading through the source himself (not a full review pass, just noticed in passing):

- **`_dgp.py`: stale `BudgetPerturber` reference.** `simulate_spend`'s `start_date` docstring said "Required when using the output with `BudgetPerturber`" — a leftover from the session 4 `BudgetPerturber` → `BudgetPhaser` rename that missed this one docstring. Fixed to `BudgetPhaser`.
- **`_report.py`: two comments read oddly for a public package.** The module docstring cited "the two-page design locked in session 28's mockup review with Ryan" and "ported near-verbatim from that approved mockup" — internal process references with no meaning to an outside reader. The `_PALETTE` comment cited `node scripts/validate_palette.js`, a script that was run once during that same mockup review but was never committed to the repo — a broken pointer if anyone went looking for it. Both rewritten to keep the substance (two-page layout description; the slot-4 CVD-accessibility check and its one WARN caveat) without the session/person/nonexistent-script references.
- Also grepped `_phaser.py` for the same pattern: two more `session N` references there (line ~23, the uniform-vs-binary-weighting research citation; line ~783, `recommend_levers`' docstring citing "session 29's follow-up investigation"). Left alone — Ryan only flagged `_report.py`, and these two are lower-key (no named person, no reference to anything missing from the repo), but worth a look if the same instinct applies there.
- Verified: `ruff format --check`/`ruff check` clean, full suite **224 tests passing**, no code paths touched (docstring/comment-only changes).
- Written back to Ryan's machine via the device bridge. **Not committed** — same standing rule as always, commit is Ryan's call from his own terminal.

## Same day, continued: full `session N` sweep across the repo, Ryan said "sort them out"

Widened the search (case-insensitive, `session[- ][0-9]+` to also catch `Session-31`-style hyphenated forms) across every `.py`/`.md`/`.html`/`.toml`/`.yml` file in the repo, excluding `NOTES.md` itself (an internal working log, not a public artifact). Found five more in `_phaser.py` beyond the two already flagged, plus four in the test suite. Also checked every `Ryan` mention repo-wide (`pyproject.toml`'s `authors`, the `introduction.html`/`research.html` eyebrow/title) — all legitimate author bylines, none of those are leftovers.

- **`_phaser.py`, five docstrings fixed** (module docstring line ~23 "session 7" weighting-study citation; `fit()`'s selection-bias-correction paragraph "session-29 follow-up investigation"; `recommend_levers()`'s "session 29's follow-up investigation"; `impact_over_horizons()`'s "notebooks/02_phaser_walkthrough.ipynb, session 27" and "Session-31 fix:" lead-in). All rewritten to keep the substantive design rationale, drop the session numbering. **Three of these (the `fit()`, `recommend_levers()`, and `impact_over_horizons()` ones) are live right now on the public API reference site** (`raz1470.github.io/how_wrong_is_your_mmm/api/phaser/`) — mkdocstrings renders docstrings straight onto the published page, so those weren't just source comments, they were already visible to anyone reading the docs.
- **`tests/test_phaser.py` and `tests/test_report.py`, four test docstrings fixed** (same pattern — session numbers cited as provenance for why a test exists). Tests don't render anywhere public, lower stakes than the `_phaser.py` ones, but same smell, so fixed too since Ryan said sort them all out.
- **Verified two ways:** `ruff format --check`/`ruff check` clean, full suite **224 tests passing** (no behaviour touched, docstrings/comments only) — and, new this round, did a real `mkdocs build --strict` in the sandbox (network access here, unlike the device bridge) against the fixed source to confirm the `session N` text is actually gone from the rendered `api/phaser/` page, not just the source. **Did not write that rebuilt site back to `docs/api`** — it was built with whatever mkdocs-material/mkdocstrings versions the sandbox resolved, which won't byte-match Ryan's own `uv`-pinned versions (different asset hashes etc.), so pushing it over his committed `docs/api` would add unrelated diff noise. `docs/api` still needs Ryan's own `uv run mkdocs build --strict` next time he touches docs, same standing item from session 31 — this session's fix just guarantees that rebuild will come out clean once he runs it.
- Written back to Ryan's machine via the device bridge (`_phaser.py`, `test_phaser.py`, `test_report.py`). **Not committed** — same standing rule as always.

## Same day, continued: general housekeeping sweep, Ryan rebuilt `docs/api` himself and asked what else was left

Ryan ran `uv run mkdocs build --strict` locally and asked what other housekeeping was outstanding. Rather than just reciting the carried-over backlog, ran a fresh mechanical sweep of the repo (grep-based, `device_bash`, no network needed) for the kinds of loose ends this session had already been finding:

- **No `TODO`/`FIXME`/`XXX`/`HACK` markers anywhere** in `.py`/`.md`/`.html`/`.toml`/`.yml`.
- **No stale references to retired class names** (`BudgetPerturber`, `CollinearityWeighter`) anywhere outside `NOTES.md`'s own historical log.
- **README's "224 tests" claim matches the real count** (`grep -c def test_` over `tests/*.py` = 224).
- **README's notebook links match the real filenames** on disk, all five.
- **`mkdocs.yml` nav is complete** — all five modules (`diagnostic`/`phaser`/`report`/`dgp`/`mmm`) listed.
- **`.gitignore`'s `reports/*` convention and `LICENSE` are both in place**, as expected.
- **Found and fixed the one real item: `notebooks/01_diagnostic_walkthrough.ipynb` still had duplicate Jupyter cell ids** (`md_0` x6, `code_0` x10, on cells 0–15 — the notebook's original cells, never uniquified; cells 16–28, added later, already had proper ids), flagged since session 26 and left unresolved every session since. `notebooks/04_channel_scaling_walkthrough.ipynb`, flagged alongside it at the time, turned out to already be clean (a later, unrelated edit must have picked up unique ids incidentally) — checked all five notebooks directly rather than assuming, only 01 had the problem.
  - Fixed by assigning fresh `uuid4().hex[:8]` ids to the 16 affected cells (collision-checked against every existing id in the file), same convention used everywhere else in this project. Metadata-only change — no cell content, output, or execution_count touched.
  - Verified three ways: all 29 ids now unique, `nbformat.validate()` passes clean, `ruff format --check`/`ruff check` clean, and confirmed 0 unexecuted code cells before and after (outputs genuinely untouched).
- Written back to Ryan's machine via the device bridge (`notebooks/01_diagnostic_walkthrough.ipynb`). **Not committed.**
- **This closes out the "duplicate Jupyter cell IDs (01/04)" item that had been carried over unresolved since session 26.**

**What's actually left, sorted by how mechanical it is:**
1. **PyPI publish** — the one remaining item with real mechanical work left (build, clean-venv sanity install, publish, flip README's `# coming to PyPI` comment), gated only on Ryan having a PyPI account + API token. Version `0.1.0` already set.
2. Everything else carried over is a judgement call, not cleanup: Ryan's own `research.html` copy/review pass, whether `recommend_levers()`'s 10% threshold and grid-precision defaults hold up beyond the 3-channel toy example, the auto-lever-exclusion idea (Ryan doesn't need it yet), Monzo validation (Ryan waved off scoping), and the LinkedIn post (last in the sequence, gated on the above).

## Same day, continued: `recommend_levers()` defaults stress-tested for real, one bug found and fixed

Ryan asked to actually dig into the still-open session-32 item — whether `recommend_levers()`'s 10% threshold and the grid-precision defaults hold up beyond the 3-channel toy example — framed around one bar: **is this trustworthy enough for a real client to rely on**, i.e. would the same underlying data ever produce a different recommendation across two runs.

**Method:** ran real Monte Carlo checks directly against `src` in the sandbox (fast — OLS-based sims, no MCMC, ~0.2-0.7s per evaluation even at 20 channels), using `BudgetPhaser._evaluate_spec_at_alpha` directly with disjoint seed offsets to get independent repeated measurements of the same underlying scenario, rather than reasoning about it abstractly.

- **Part A — noise floor on the continuous-vs-Blackout comparison `recommend_levers()`'s threshold is judged against.** Swept N ∈ {3, 5, 10, 20} channels x correlation ∈ {0.5, 0.7, 0.9} (generic channels, elasticities ~Uniform(0.2, 0.6), same convention as notebook 04's channel-count sweep), 2-3 representative channels per scenario (min/median/max true elasticity), 10-15 independent repeats per setting.
  - **Real effect size is large and consistent:** Blackout beats continuous by ~15-25% on average, at every N and correlation level tested — the 10% threshold clears this comfortably on average.
  - **Real bug found: the docstring's noise claim for `recommend_levers()`'s own standalone default was wrong by about 2x.** It claimed "a few percentage points" of run-to-run noise at `n_phasing_seeds=3` (the method's own prior default); the measured std was actually 6-10 percentage points, and 1-4 out of 15 repeats at that setting landed *below* the 10% threshold even though the true effect was a robust ~15-25% — i.e. the lever recommendation could flip on pure noise roughly 10-25% of the time at that setting. `ReportBuilder` never hit this in practice because it always overrides `n_phasing_seeds` up to its own default (10) when calling `recommend_levers()` — but that safety was accidental, resting entirely on the caller happening to pass a bigger number, not on the method being safe at its own defaults. Since `recommend_levers()` is public API, anyone calling it directly (a notebook, a future feature) would have gotten the flaky version.
  - At `n_phasing_seeds=10`, noise drops to 2-5 points and the threshold holds in the large majority of cases — but N=20 still crossed under 10% on 1-3 out of 10 repeats for a couple of channels, hinting the noise floor may widen somewhat with channel count. **Not resolved, flagged honestly rather than fixed** — only one correlation level and a handful of seeds checked at N=20, not enough to know whether the fix is a bigger n_phasing_seeds, a channel-count-scaled threshold, or nothing (could be this particular scenario).
- **Part B — does `confirm_top_k=3` (the selection-bias correction from session 30) actually catch the true-best alpha, and does it de-bias the raw grid argmin?** For N ∈ {3, 10, 20}, 3 independent seeds each: ran a real `fit()` (`grid_steps=25, n_phasing_seeds=10`, `ReportBuilder`'s own settings) and compared the raw grid argmin, the confirmed winner, and a "clean" ground-truth re-grid (same 25 alphas, `n_phasing_seeds=15`, seeds far outside anything `fit()` touched).
  - **The true best alpha was inside the noisy top-3 in all 9 runs** — `confirm_top_k=3` isn't missing the real winner, at least at these settings. The confirmed alpha matched the true-best alpha exactly in 6 of 9 runs.
  - Confirmation does pull the estimate toward the ground truth in most cases, but not uniformly — at N=20 the confirmed CV was slightly *more* biased than the raw grid argmin in 2 of 3 seeds (small sample, plausibly noise itself, not a clear pattern worth chasing yet).
- **Fixed, evidence-based:** `recommend_levers()`'s own default `n_phasing_seeds` raised from 3 to 10, matching what `ReportBuilder` already forces (so this changes nothing for the actual report path — every existing test and `ReportBuilder` call already passes `n_phasing_seeds` explicitly, confirmed by grep before making the change). Docstring corrected to state the real measured noise (2-5 points at `n_phasing_seeds=10`, 6-10 at `n_phasing_seeds=3`) instead of the old understated claim, and to flag the unresolved N=20 hint honestly rather than pretend it's settled.
- **Not changed, deliberately:** `confirm_top_k=3`/3x-seed-multiplier and the `improvement_threshold_pct=10.0` value itself — both look reasonable on the evidence so far, no clear case for a different number, and the N=20 confirmation-bias wobble is too thin a signal (3 seeds) to act on yet.
- Verified: `ruff format --check`/`ruff check` clean, full suite **224 tests passing** (no test relied on the old `n_phasing_seeds=3` default — checked directly).
- Written back to Ryan's machine via the device bridge (`_phaser.py`). **Not committed.**
- **Grid-precision defaults (`grid_steps=25`, `n_phasing_seeds=10`) themselves were not separately stress-tested this session** — used as fixed inputs for Part B, not evaluated as their own question. Still open if it matters later.

## Same day, continued: real input-validation guardrails added — "is this safe for a real client's messy CSV"

Ryan asked what else was worth doing before release, beyond finishing `research.html` and PyPI. Rather than list ideas, ran real inputs through the package (same instinct as the `recommend_levers()` check) — every scenario tested against clean synthetic data throughout this project's history, never anything a real client spreadsheet would actually contain:

- **A single NaN in spend data** → crashed with a raw LAPACK error (`SVD did not converge`, `DLASCL parameter number 4 had an illegal value`) — not remotely client-facing.
- **A channel with zero spend for the period** (a paused channel — completely realistic) → didn't crash, silently returned `coef_of_variation = NaN` with no explanation.
- **A short history (5 weeks)** → didn't crash, silently returned a CV of 4445% for one channel — mathematically "correct" for an underdetermined fit, but would read as broken, not honest, with zero context.
- Grepped the whole `src/` for any `isna`/`dropna`/`fillna`/NaN handling: **zero hits**, confirmed before writing anything.

**Fixed with a new shared validator, `_validate_spend_data()` in `_diagnostic.py`:**
- Checks (in order): non-numeric columns, NaN values (names the affected column(s)), non-finite values (inf/-inf), zero-variance channels (names them — constant spend, an elasticity is provably inestimable), then a hard floor on row count (`len(channels) + 2` — one param per channel, one for the intercept, one residual degree of freedom left over, below which OLS is mathematically undefined, not just unreliable).
- Below that hard floor is `_MIN_OBS_PER_CHANNEL_WARNING = 10` observations per channel — a `UserWarning`, not an error, since a legitimately short pilot window is a real use case; just flags that estimates below this are noise-dominated even before collinearity is a factor.
- **Wired in at two points, both real fitting paths:** `CollinearityDiagnostic.fit()` (whenever `spend_df` is supplied — the real-spend entry point) and `BudgetPhaser.__init__` (eagerly, on `pd.concat([history_df, plan_df])`, so a data problem fails fast at construction rather than deep inside the first grid-search iteration). `ReportBuilder` gets this for free — it always builds a `BudgetPhaser` internally. The synthetic-spend path (no `spend_df`/generated data) is deliberately never validated — it's generated internally and always well-formed; confirmed via a dedicated test that it never raises a spurious warning either.
- **16 new tests** across `test_diagnostic.py::TestValidateSpendData` (11: NaN raises and names the column, inf raises, zero-variance raises and names the column, non-numeric raises, too-few-observations raises, the exact minimum length passes with the expected warning, low-obs-per-channel warns without raising, ample data raises nothing, synthetic path is never validated) and `test_phaser.py` (5: NaN in history/plan each raise at construction — before `fit()` is ever reachable — zero-variance raises at construction, and a plain regression check that the standard fixture data still constructs cleanly). **240 tests passing** (up from 224).
- **Re-verified against the exact four scenarios from the investigation**, end to end, not just via the unit tests: NaN and zero-variance now raise clear, specific `ValueError`s (both from `CollinearityDiagnostic` directly and from `BudgetPhaser`'s construction-time check); the 5-week case now raises the expected `UserWarning` instead of silently returning a nonsense number. Also ran a full `ReportBuilder.fit(fast_mode=True).to_html()` smoke test on valid data afterward to confirm nothing downstream broke.
- Verified: `ruff format --check`/`ruff check` clean, full suite 240 passing, no stray warnings in the test run (one test's expected warning was explicitly captured with `pytest.warns` rather than left to float in the summary).
- Written back to Ryan's machine via the device bridge (`_diagnostic.py`, `_phaser.py`, `test_diagnostic.py`, `test_phaser.py`). **Not committed.**
- **Not done, worth a future look:** validation only covers the failure modes actually tested (NaN, inf, non-numeric, zero-variance, too-short). Didn't check duplicate/non-monotonic DatetimeIndex values, extreme outliers, or negative spend — flagging as untested rather than claiming they're handled.

## Same day, continued: `research.html` reconciled to `introduction.html`'s exact canonical scenario

Ryan asked to look at `research.html` before PyPI. He'd pasted a close read comparing it against `introduction.html` and flagged three breaks: the two pages' "real dataset" examples used the same £12.3m/TV-Meta-Search split cosmetically but different underlying data (different correlation, different answer); the headline Impact result never reappeared in `research.html`; and `research.html`'s own "real dataset" reference landed somewhere disconnected from that headline. Ryan's instruction: **"research should basically be a deeper dive on the exact intro scenario"** — not two different-looking "real" examples, one dataset used throughout both pages.

**First, verified the premise rather than assuming the pasted analysis was right in every particular.** One claim in it was wrong: it said `introduction.html`'s 54%→58/61/62% headline came from a specific mixed-lever schedule (TV continuous, Meta/Search Blackout). Checked `introduction.html` directly — that mixed-lever setup is a separate, smaller illustration in its own §3 ("The Fix"); the actual headline numbers come from switching **every** channel to Blackout, confirmed by the page's own text ("we simulated what happens when every channel uses the harder on/off lever"). Flagged this correction before proceeding, since it changes what "the exact intro scenario" means.

**Root-caused the actual mismatch:** `research.html`'s "Your elasticity uncertainty report" chart and "From elasticity to £" section were built from **notebook 01**'s separate real-spend demo (mean correlation 0.65) — not notebook 02's scenario that `introduction.html` is built around (history seed=0, plan seed=1, correlation ~0.68/0.69, £12.3m plan split £5.1m TV/£4.1m Meta/£3.1m Search). The same £ split was reused cosmetically across two genuinely different datasets, which is exactly the kind of inconsistency this project's "always trace to a committed notebook cell, never invent numbers" rule exists to catch.

**Fixed by re-deriving every number in the affected sections from `introduction.html`'s actual dataset, not by editing the prose to match:**
- Re-ran the notebook 02 recipe in the sandbox (`simulate_spend(n_obs=208, correlation=0.7, seed=0, ...)` history + `simulate_spend(n_obs=52, correlation=0.7, seed=1, ...)` plan) and fit `CollinearityDiagnostic` on it directly.
- Cross-checked the resulting £ incremental-revenue ranges against `introduction.html`'s own published numbers before using them anywhere: matched to the pound for TV and Search, within ~1% for Meta.
- Derived exact elasticity-level p10/p90/mean by dividing the £ revenue quantiles by planned spend (a quantile commutes with a positive scalar multiplier, so this is exact, not approximate).
- **Chart 4 ("Your elasticity uncertainty report")**: replaced the notebook-01-sourced channel data and comment with the real `introduction.html`-scenario numbers (TV mean 0.295/p10 0.185/p90 0.411, Meta 0.497/0.346/0.633, Search 0.425/0.230/0.608), comment now explains the derivation and cross-check.
- **fig-sub caption**: now reads "same £12.3m plan as introduction.html (mean pairwise correlation = 0.69)" instead of the old notebook-01 correlation figure, and links to `introduction.html`.
- **"From elasticity to £" section**: prose now explicitly says "the same £12.3m annual plan as introduction.html"; the three kbox values corrected from the old notebook-01-derived ranges (TV £664k–£2.36m, Meta £1.28m–£2.68m, Search £583k–£2.39m) to the real intro-scenario ranges (TV £945k–£2.09m, Meta £1.42m–£2.60m, Search £712k–£1.89m); closing sentence now states these are the exact numbers `CollinearityDiagnostic.summary(planned_spend=...)` returns on `introduction.html`'s dataset directly.
- **§3's "In a real dataset..." paragraph**: this one turned out to already be using the correct real dataset (its ±37%/16% continuous-baseline figure matches a fresh reproduction of `comparison_df`'s Baseline row), and §3's later "Channel-specific constraints" subsection (charts 12/13) already matched notebook 02 cell 27-29's real comparison numbers almost verbatim (16-24% baseline / 17-49% Search-Blackout / 47-59% all-Blackout across three draws) — so no numbers needed correcting there. What was missing was the link: added a sentence connecting this paragraph explicitly to `introduction.html` and its 54% headline, and pointing forward to the Blackout comparison later in the same section, since the 16% figure and the 54% headline are different metrics (CV reduction vs. incremental-revenue-range narrowing) on the same underlying real dataset, not competing answers.
- **Left alone, deliberately:** `research.html`'s Section 4 ("The Impact") synthetic multi-deviation, multi-channel-count sweeps (chart 7-9, the ±20/40/80% time-to-benefit curves, the 5-20 channel scaling study). These are a genuinely broader research question than `introduction.html`'s single real-scenario headline, not a duplicate that needed to match it — that's the actual "deeper dive" this page adds beyond the intro page's one worked example.
- **Not raised or acted on:** noticed a small (~1%) pre-existing Meta £ discrepancy within `introduction.html` itself, between its Problem-chart forest data (1.406/2.574) and its Impact-chart "today" array (1.418/2.595). Out of scope for this pass (about `research.html`, not `introduction.html`) — flagging here so it doesn't get lost.
- Verified: div tag balance check (167 open/167 close), then a real headless-Chromium render via Playwright — zero console/page errors, zero `NaN` SVG coordinates, and spot-checked the rendered DOM text for the corrected kbox values and fig-sub caption to confirm the JS data changes actually reached the page, not just the source.
- Written back to Ryan's machine via the device bridge (`docs/research.html`). **Not committed.**
- **Not yet confirmed with Ryan:** the pasted analysis also suggested (a) moving/mirroring the algorithm nudge/rescale worked example from `introduction.html` into `research.html`, and (b) standardising the three-step pipeline copy wording between the two pages. Not done this round — Ryan's explicit instruction was specifically about the real-dataset scenario matching, and these two are separate, larger editorial calls worth a dedicated pass rather than folding in silently.

## Same day, continued: mirrored the algorithm walkthrough into `research.html` and standardised the pipeline copy

Ryan confirmed "do both" for the two items left open above.

**1. Mirrored `introduction.html`'s nudge/rescale worked example into `research.html`'s "The mechanism" section.** Previously that section only described the mechanism in prose (no numbers). Inserted the exact same two-step walkthrough used in `introduction.html` — Step 1 (nudge: £100k/£120k/£0/£100k off a £400k monthly plan, £320k total) and Step 2 (rescale: ×1.25 back to £125k/£150k/£0/£125k = £400k exactly) — plus the same `raw_week`/`phased_week` formula callout, with a line pointing back to `introduction.html` as the source of the worked numbers rather than presenting it as new content.
- Added one paragraph beyond a straight mirror, since `research.html` is the technical page: explained that the grid search described earlier in this same section (`grid_steps`, default 20 — checked against `BudgetPhaser.fit()`'s actual signature, not assumed) is what picks the nudge amplitude, and that `Blackout()` runs the identical nudge-then-rescale arithmetic with the nudge constrained to exactly 1.0 or 0.0. **Verified against `_generate_phased_schedule` directly** before publishing that claim (`new_weeks = orig_weeks * (1 + raw)`, then rescaled by `monthly_total / new_weeks.sum()`, with Blackout's `raw` being `-1.0` or `0.0` — same arithmetic, confirmed by reading the function, not assumed from the docstring).
- Caught and fixed one near-miss before it shipped: first draft said "grid_steps, default 25" from memory of an earlier session's stress-test config (which explicitly passed `grid_steps=25`); checked the actual default in `_phaser.py` and it's 20, corrected before writing back.

**2. Standardised the three-step pipeline copy between the two pages.** Found a real terminology drift, not just a style difference: `introduction.html` used "pacing"/"repace" in 6 spots (chart title, legend, two fig-caps, two pipeline-diagram box labels) interchangeably with "phasing" (11 uses) for the exact same operation, while `research.html` used "phasing"/"phased" exclusively (63 uses, confirmed by grep, its handful of "pacing"-looking hits were all CSS `letter-spacing`). Since `phasing` is the term the actual codebase uses (`BudgetPhaser`, `max_weekly_deviation_pct`, notebooks, README), standardised `introduction.html`'s 6 stray "pacing" instances to "phasing".
- Also brought the two pages' pipeline-diagram step boxes (Diagnose / Phase / Retrain) to literally identical sub-caption text, where before each page independently worded them: Step 1 now "Simulate plausible histories, measure how wide the range is" on both; Step 2 "De-correlate the weekly split, monthly totals stay identical" on both (already matched on `research.html`, applied to `introduction.html`); Step 3 "Refit on the phased data, get a tighter answer back" on both. Confirmed via Playwright DOM extraction that the rendered `<text>` content is word-for-word identical between the two pages' diagrams, not just the source.
- Left the surrounding prose (h2 titles, intro paragraphs, fig-caps) un-forced to match — those already didn't contradict each other and appropriately differ in depth/register between the two pages (friendly headline page vs. technical deep-dive page); the closing `ReportBuilder` paragraph was already verbatim-identical on both pages before this session, confirmed rather than assumed.
- Verified: div tag balance on both files (`research.html` 168/168, `introduction.html` 40/40), Playwright headless-Chromium render on both — zero console/page errors, zero `NaN` SVG coordinates on either page.
- Written back to Ryan's machine via the device bridge (`docs/research.html`, `docs/introduction.html`). **Not committed.**
- **Still open, not raised again this round:** the small pre-existing ~1% Meta £ discrepancy inside `introduction.html` (Problem-chart forest data vs. Impact-chart "today" array) flagged last entry — Ryan hasn't confirmed he wants it chased, left as-is.

## Same day, continued: fixed the Meta £ discrepancy inside `introduction.html`, then Ryan's ready to commit

Ryan confirmed he wanted the ~1% Meta discrepancy chased down.

**Root-caused rather than guessed.** The Problem chart (§1, "before phasing" forest plot) and the Impact chart's "today" baseline (§5, same no-phasing scenario) are supposed to show the exact same number, by the code's own comments: the Problem chart's comment says "no-phasing scenario, p10/p90 of the resulting distribution", the Impact chart's says "today" is the pre-phasing baseline from the same real reproduction. Re-ran the exact recipe (`CollinearityDiagnostic` on `history+plan`, seeds 0/1, correlation 0.7, `n_sims=50`, real package code in the sandbox, not by hand) and got TV 0.945/2.094, Meta 1.418/2.595, Search 0.712/1.885 — which matched the Impact chart's numbers exactly, and the Problem chart's TV/Search too, but not the Problem chart's Meta (1.406/2.574, off ~1%) or TV's p90 (2.093 vs 2.094, a rounding-level miss). **The Impact chart was right; the Problem chart had the stale numbers** — likely never updated after some earlier revision to the Impact chart's own numbers (its comment already flags one prior revision, "a single-draw version of this chart overstated Meta/Search").
- Fixed: `chartProblem`'s data array, Meta p10/p90 corrected to 1.418/2.595, TV p90 corrected to 2.094. Search was already correct. The chart's `spend` fields (5.098/4.066/3.100, the actual simulated plan-year totals, not the rounded £5.1m/£4.1m/£3.1m planning figures) were separately verified as still correct — `plan.sum()` on the real reproduction matches to 3 decimal places, no change needed there.
- Grepped both `introduction.html` and `research.html` for the old stale values (`1.406`, `2.574`, and the old research.html kbox ranges from the earlier fix this session) to confirm no other copy anywhere quoted the wrong Meta number. Clean.
- Verified: div tag balance (40/40), Playwright headless-Chromium render — zero console/page errors, zero `NaN` SVG coordinates, and confirmed via DOM text extraction that the Problem and Impact charts now render the identical Meta range (£1.42m–£2.60m) side by side.
- Written back to Ryan's machine via the device bridge (`docs/introduction.html`). **Not committed** — Ryan's committing this batch himself next, from his own terminal, per the standing rule.

**This closes out every item from Ryan's original `introduction.html`/`research.html` consistency pass**: the real-scenario reconciliation, the mirrored algorithm walkthrough, the standardised pipeline copy, and this last numeric discrepancy. Nothing outstanding on `docs/` before PyPI publish.

## Same day, continued: tagline copy tweak on both pages

Small copy request from Ryan: `introduction.html`'s header tagline ("How using a budget phasing algorithm can dramatically tighten the confidence in your MMM results.") reads better framed as **"An introduction"** to that claim, and `research.html`'s header tagline (previously "A practitioner's guide to collinearity bias in Marketing Mix Models", a different sentence entirely) should use the same claim framed as **"The research behind"** it — with "An introduction" / "The research" bolded in each.

- `introduction.html`: `<p class="tagline">An introduction to how using a budget phasing algorithm can dramatically tighten the confidence in your MMM results.</p>`, "An introduction" wrapped in `<strong>`.
- `research.html`: tagline replaced with `The research behind how using a budget phasing algorithm can dramatically tighten the confidence in your MMM results.`, "The research" wrapped in `<strong>`. This is a bigger change on the research side since it swaps out the old, unrelated tagline sentence entirely — flagging that explicitly in case Ryan wants the old "practitioner's guide to collinearity bias" framing kept somewhere else on the page (it wasn't repeated elsewhere, checked via grep).
- Verified: div tag balance on both (`introduction.html` 40/40, `research.html` 168/168), Playwright render clean on both (zero console errors, zero `NaN` SVG coordinates), and confirmed via DOM extraction (`innerHTML` of `.tagline`) that the bold wrapping actually rendered, not just present in source.
- Written back to Ryan's machine via the device bridge (`docs/introduction.html`, `docs/research.html`). **Not committed** — part of the same batch Ryan's committing himself next.
